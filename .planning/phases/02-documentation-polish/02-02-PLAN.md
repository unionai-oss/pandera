---
phase: 02-documentation-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - pandera/api/checks.py
  - pandera/backends/polars/register.py
  - pandera/backends/ibis/register.py
  - pandera/backends/narwhals/checks.py
  - pandera/backends/narwhals/base.py
  - pandera/backends/narwhals/container.py
  - pandera/backends/narwhals/components.py
  - pandera/engines/narwhals_engine.py
autonomous: true
requirements:
  - DOCS-02
must_haves:
  truths:
    - "Every prose reference to 'narwhals' (the library name) in comments and docstrings across `pandera/` is capitalized as 'Narwhals'"
    - "Code identifiers (variable names, import paths, backticked symbols like `nw.col`, `narwhals.stable.v1`) remain lowercase and unchanged"
    - "No runtime imports, symbol names, or behavior change — the code still parses, imports, and runs"
  artifacts:
    - path: "pandera/backends/polars/register.py"
      provides: "Capitalized 'Narwhals' in function docstring at lines 15-17"
      contains: "Narwhals"
    - path: "pandera/backends/ibis/register.py"
      provides: "Capitalized 'Narwhals' in function docstring at lines 15-17"
      contains: "Narwhals"
    - path: "pandera/backends/narwhals/checks.py"
      provides: "Capitalized 'Narwhals' in inline comments and docstring (lines 54, 82)"
      contains: "Narwhals"
    - path: "pandera/backends/narwhals/base.py"
      provides: "Capitalized 'Narwhals' in inline comments and docstrings (lines 83, 124, 142, 192, 203, 358, 426)"
      contains: "Narwhals"
    - path: "pandera/backends/narwhals/container.py"
      provides: "Capitalized 'Narwhals' in inline comments (lines 161, 162, 263)"
      contains: "Narwhals"
    - path: "pandera/backends/narwhals/components.py"
      provides: "Capitalized 'Narwhals' in inline comments (lines 144, 245, 332, 333, 335)"
      contains: "Narwhals"
    - path: "pandera/engines/narwhals_engine.py"
      provides: "Capitalized 'Narwhals' in inline comment and docstring (lines 32, 61, 97)"
      contains: "Narwhals"
    - path: "pandera/api/checks.py"
      provides: "Capitalized 'Narwhals' in :param native: docstring (line 89: 'narwhals expression' -> 'Narwhals expression')"
      contains: "Narwhals expression"
  key_links:
    - from: "prose 'narwhals' references"
      to: "capitalized 'Narwhals'"
      via: "manual text edits guided by grep"
      pattern: "Narwhals"
---

<objective>
Consistently capitalize "Narwhals" (as a proper noun / library name) in all prose comments and docstrings across `pandera/`, while leaving code identifiers (variable names, import paths, backticked symbols) lowercase.

Purpose: Addresses DOCS-02 from the v1.2 PR review. The reviewer noted inconsistent capitalization — the library's official name is "Narwhals" (capital N), and scattered lowercase "narwhals" in prose looks sloppy and is inconsistent with module-level docstrings that already use "Narwhals".

Output: Multiple `.py` files updated with prose "narwhals" → "Narwhals" replacements. Zero behavior changes.

Depends on plan 02-01 because both plans edit `pandera/api/checks.py` — 02-01 appends the DOCS-01 caveat, then 02-02 can safely re-capitalize the "narwhals expression" phrase without merge conflicts.
</objective>

<execution_context>
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-documentation-polish/02-CONTEXT.md
@.planning/phases/02-documentation-polish/02-01-SUMMARY.md

@pandera/api/checks.py
@pandera/backends/polars/register.py
@pandera/backends/ibis/register.py
@pandera/backends/narwhals/checks.py
@pandera/backends/narwhals/base.py
@pandera/backends/narwhals/container.py
@pandera/backends/narwhals/components.py
@pandera/engines/narwhals_engine.py

<interfaces>
## Capitalization Rule (from D-04)

**Capitalize** "narwhals" → "Narwhals" when it is used as a proper noun in prose referring to the library:
- "narwhals is installed" → "Narwhals is installed"
- "narwhals backend" → "Narwhals backend"
- "narwhals wrapper" → "Narwhals wrapper"
- "narwhals raises NotImplementedError" → "Narwhals raises NotImplementedError"
- "narwhals expression" → "Narwhals expression"
- "narwhals ColumnBackend" → "Narwhals ColumnBackend"
- "narwhals DataFrameSchemaBackend" → "Narwhals DataFrameSchemaBackend"
- "narwhals frame" → "Narwhals frame"
- "narwhals types" → "Narwhals types"
- "pure narwhals" → "pure Narwhals"
- "narwhals ops" → "Narwhals ops"

**Keep lowercase** (do NOT touch):
- Variable names: `narwhals_data`, `narwhals_engine`
- Import paths: `narwhals.stable.v1`, `import narwhals as nw`, `narwhals.stable.v1 as nw`
- Backticked code identifiers: `` `nw.col` ``, `` `NarwhalsData` ``, `` `narwhals.stable.v1` ``
- Module/package names in import lines: `pandera.backends.narwhals`, `pandera.api.narwhals`
- File path references: `narwhals/checks.py`, `pandera/backends/narwhals/`

## Exact occurrences to fix (from grep of pandera/ earlier)

### pandera/api/checks.py (line 89) — edit AFTER plan 02-01 completes
- Line 89: `receives a narwhals expression` → `receives a Narwhals expression`

### pandera/backends/polars/register.py (lines 15-17, 28-33 area)
- Line 15: `Auto-detects narwhals: if narwhals is installed, registers narwhals backends`
  → `Auto-detects Narwhals: if Narwhals is installed, registers Narwhals backends`
- Line 16: `(NarwhalsCheckBackend, narwhals ColumnBackend, narwhals DataFrameSchemaBackend)`
  → `(NarwhalsCheckBackend, Narwhals ColumnBackend, Narwhals DataFrameSchemaBackend)`
- Line 17: `and emits a UserWarning. If narwhals is not installed, registers the native`
  → `and emits a UserWarning. If Narwhals is not installed, registers the native`
- Do NOT touch `import narwhals.stable.v1 as nw` on line 28 (import).
- Do NOT touch `from pandera.backends.narwhals import builtin_checks` (module path).

### pandera/backends/ibis/register.py (lines 15-17)
- Identical edits to polars/register.py (lines 15-17 have the same prose):
  - Line 15: `Auto-detects narwhals: if narwhals is installed, registers narwhals backends`
    → `Auto-detects Narwhals: if Narwhals is installed, registers Narwhals backends`
  - Line 16: `(NarwhalsCheckBackend, narwhals ColumnBackend, narwhals DataFrameSchemaBackend)`
    → `(NarwhalsCheckBackend, Narwhals ColumnBackend, Narwhals DataFrameSchemaBackend)`
  - Line 17: `and emits a UserWarning. If narwhals is not installed, registers the native`
    → `and emits a UserWarning. If Narwhals is not installed, registers the native`
- Do NOT touch `import narwhals.stable.v1 as nw` on line 31 or `from pandera.backends.narwhals import ...` lines.

### pandera/backends/narwhals/checks.py
- Line 54: `(narwhals raises NotImplementedError during .select()).`
  → `(Narwhals raises NotImplementedError during .select()).`
- Line 82: `"""Normalize native outputs from native=True checks to narwhals types.`
  → `"""Normalize native outputs from native=True checks to Narwhals types.`
- Line 237-239: `narwhals_data = NarwhalsData(...)` — variable name, DO NOT touch.

### pandera/backends/narwhals/base.py
- Line 83: `failure_cases and check_output stay as narwhals wrappers in the returned`
  → `failure_cases and check_output stay as Narwhals wrappers in the returned`
- Line 124: `failure_cases = fc  # narwhals wrapper — NOT collected here`
  → `failure_cases = fc  # Narwhals wrapper — NOT collected here`
- Line 142: `failure_cases=failure_cases,             # narwhals wrapper — NOT _to_native() here`
  → `failure_cases=failure_cases,             # Narwhals wrapper — NOT _to_native() here`
- Line 192: `# Wrap any native frame (pl.DataFrame, pl.LazyFrame, ibis.Table) back to narwhals`
  → `# Wrap any native frame (pl.DataFrame, pl.LazyFrame, ibis.Table) back to Narwhals`
- Line 203: `# Use narwhals ops only — no Arrow roundtrip, no polars import in this path.`
  → `# Use Narwhals ops only — no Arrow roundtrip, no polars import in this path.`
- Line 358: `"""Remove invalid rows — pure narwhals, no backend delegation.`
  → `"""Remove invalid rows — pure Narwhals, no backend delegation.`
- Line 426: `# Preserve input type: native in -> native out, narwhals in -> narwhals out`
  → `# Preserve input type: native in -> native out, Narwhals in -> Narwhals out`

### pandera/backends/narwhals/container.py
- Line 161: `# Unwrap narwhals failure_cases to native at the SchemaError boundary.`
  → `# Unwrap Narwhals failure_cases to native at the SchemaError boundary.`
- Line 162: `# CoreCheckResult carries narwhals wrappers; SchemaError.failure_cases`
  → `# CoreCheckResult carries Narwhals wrappers; SchemaError.failure_cases`
- Line 263: `# Narwhals backend returns a narwhals frame, not pl.LazyFrame.`
  → `# Narwhals backend returns a Narwhals frame, not pl.LazyFrame.`

### pandera/backends/narwhals/components.py
- Line 144: `# failure_cases and check_output stay lazy — narwhals wrappers, not native.`
  → `# failure_cases and check_output stay lazy — Narwhals wrappers, not native.`
- Line 245: `# construction — pandera.polars/pandera.ibis should produce narwhals`
  → `# construction — pandera.polars/pandera.ibis should produce Narwhals`
- Line 332: `# Convert narwhals failure_cases to native for SchemaError public API.`
  → `# Convert Narwhals failure_cases to native for SchemaError public API.`
- Line 333: `# CoreCheckResult carries narwhals wrappers; SchemaError.failure_cases`
  → `# CoreCheckResult carries Narwhals wrappers; SchemaError.failure_cases`
- Line 335: `# so callers can use the result without narwhals knowledge.`
  → `# so callers can use the result without Narwhals knowledge.`

### pandera/engines/narwhals_engine.py
- Line 32: `Accepts a :class:`~pandera.api.narwhals.types.NarwhalsData` or a`
  → DO NOT touch (`:class:` reference to a dotted path is a code identifier, not prose).
- Line 61: `# For nw.LazyFrame (polars): head(1).collect() stays in narwhals.`
  → `# For nw.LazyFrame (polars): head(1).collect() stays in Narwhals.`
- Line 97: `"""Convert input into a narwhals-compatible`
  → `"""Convert input into a Narwhals-compatible`
- Do NOT touch `import narwhals.stable.v1 as nw` on line 6.
- Do NOT touch `from pandera.api.narwhals.types import NarwhalsData` on line 9.
- Do NOT touch `from pandera.api.narwhals.utils import ...` on line 10.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Capitalize "Narwhals" prose in register.py and api/checks.py files</name>
  <files>
    pandera/backends/polars/register.py,
    pandera/backends/ibis/register.py,
    pandera/api/checks.py
  </files>
  <read_first>
    - pandera/backends/polars/register.py (entire file — small, ~40 lines)
    - pandera/backends/ibis/register.py (entire file — small, ~45 lines)
    - pandera/api/checks.py (lines 80-100 — scope to the :param native: block)
    - .planning/phases/02-documentation-polish/02-CONTEXT.md (D-04 capitalization rule)
  </read_first>
  <action>
    Apply the exact prose replacements listed in the `<interfaces>` block above for these three files. Summary of edits:

    **pandera/backends/polars/register.py** (docstring lines 15-17):
    - `Auto-detects narwhals: if narwhals is installed, registers narwhals backends` → `Auto-detects Narwhals: if Narwhals is installed, registers Narwhals backends`
    - `(NarwhalsCheckBackend, narwhals ColumnBackend, narwhals DataFrameSchemaBackend)` → `(NarwhalsCheckBackend, Narwhals ColumnBackend, Narwhals DataFrameSchemaBackend)`
    - `and emits a UserWarning. If narwhals is not installed, registers the native` → `and emits a UserWarning. If Narwhals is not installed, registers the native`

    **pandera/backends/ibis/register.py** (docstring lines 15-17): Same three edits as polars/register.py.

    **pandera/api/checks.py** (line 89, inside the :param native: docstring):
    - `receives a narwhals expression` → `receives a Narwhals expression`

    Do NOT touch any `import narwhals...` lines. Do NOT touch any `from pandera.backends.narwhals...` or `from pandera.api.narwhals...` lines. Do NOT touch backticked code identifiers. Do NOT touch the DOCS-01 caveat sentence added by plan 02-01.

    After editing, run `python -c "import pandera"` to confirm all three files still parse and import.
  </action>
  <verify>
    <automated>python -c "import ast; [ast.parse(open(p).read()) for p in ['pandera/backends/polars/register.py','pandera/backends/ibis/register.py','pandera/api/checks.py']]; import pandera; print('parse+import OK')" && grep -c "Auto-detects Narwhals: if Narwhals is installed, registers Narwhals backends" pandera/backends/polars/register.py && grep -c "Auto-detects Narwhals: if Narwhals is installed, registers Narwhals backends" pandera/backends/ibis/register.py && grep -c "receives a Narwhals expression" pandera/api/checks.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Auto-detects Narwhals: if Narwhals is installed, registers Narwhals backends" pandera/backends/polars/register.py` outputs `1`
    - `grep -c "Auto-detects Narwhals: if Narwhals is installed, registers Narwhals backends" pandera/backends/ibis/register.py` outputs `1`
    - `grep -c "(NarwhalsCheckBackend, Narwhals ColumnBackend, Narwhals DataFrameSchemaBackend)" pandera/backends/polars/register.py` outputs `1`
    - `grep -c "(NarwhalsCheckBackend, Narwhals ColumnBackend, Narwhals DataFrameSchemaBackend)" pandera/backends/ibis/register.py` outputs `1`
    - `grep -c "If Narwhals is not installed, registers the native" pandera/backends/polars/register.py` outputs `1`
    - `grep -c "If Narwhals is not installed, registers the native" pandera/backends/ibis/register.py` outputs `1`
    - `grep -c "receives a Narwhals expression" pandera/api/checks.py` outputs `1`
    - `grep -c "Auto-detects narwhals" pandera/backends/polars/register.py` outputs `0` (lowercase variant gone)
    - `grep -c "Auto-detects narwhals" pandera/backends/ibis/register.py` outputs `0`
    - `grep -c "receives a narwhals expression" pandera/api/checks.py` outputs `0`
    - `grep -c "import narwhals.stable.v1 as nw" pandera/backends/polars/register.py` outputs `1` (import untouched)
    - `grep -c "import narwhals.stable.v1 as nw" pandera/backends/ibis/register.py` outputs `1` (import untouched)
    - `grep -c "only applies when using the Narwhals backend" pandera/api/checks.py` outputs `1` (DOCS-01 caveat from plan 02-01 preserved)
    - `python -c "import pandera; import pandera.backends.polars.register; import pandera.backends.ibis.register"` exits 0
  </acceptance_criteria>
  <done>
    All three files updated per the exact edits above, DOCS-01 caveat preserved, imports still work, lowercase prose variants (`Auto-detects narwhals`, `receives a narwhals expression`) eliminated.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Capitalize "Narwhals" prose in pandera/backends/narwhals/ and engines/narwhals_engine.py</name>
  <files>
    pandera/backends/narwhals/checks.py,
    pandera/backends/narwhals/base.py,
    pandera/backends/narwhals/container.py,
    pandera/backends/narwhals/components.py,
    pandera/engines/narwhals_engine.py
  </files>
  <read_first>
    - pandera/backends/narwhals/checks.py (lines 40-100 for the target comments and docstrings)
    - pandera/backends/narwhals/base.py (lines 80-210 and 350-430 for target comments/docstrings)
    - pandera/backends/narwhals/container.py (lines 150-280 for target comments)
    - pandera/backends/narwhals/components.py (lines 140-340 for target comments)
    - pandera/engines/narwhals_engine.py (lines 25-100 for target comment and docstring)
    - .planning/phases/02-documentation-polish/02-CONTEXT.md (D-04 capitalization rule)
  </read_first>
  <action>
    Apply the exact prose replacements listed in the `<interfaces>` block for these five files. Summary (all are "narwhals" → "Narwhals" in comments/docstrings only):

    **pandera/backends/narwhals/checks.py:**
    - Line 54: `(narwhals raises NotImplementedError during .select()).` → `(Narwhals raises NotImplementedError during .select()).`
    - Line 82: `"""Normalize native outputs from native=True checks to narwhals types.` → `"""Normalize native outputs from native=True checks to Narwhals types.`
    - Line 237 (`narwhals_data = NarwhalsData(...)`): DO NOT TOUCH — variable name.

    **pandera/backends/narwhals/base.py:**
    - Line 83: `stay as narwhals wrappers` → `stay as Narwhals wrappers`
    - Line 124: `# narwhals wrapper — NOT collected here` → `# Narwhals wrapper — NOT collected here`
    - Line 142: `# narwhals wrapper — NOT _to_native() here` → `# Narwhals wrapper — NOT _to_native() here`
    - Line 192: `back to narwhals` → `back to Narwhals`
    - Line 203: `# Use narwhals ops only` → `# Use Narwhals ops only`
    - Line 358: `pure narwhals, no backend delegation` → `pure Narwhals, no backend delegation`
    - Line 426: `native in -> native out, narwhals in -> narwhals out` → `native in -> native out, Narwhals in -> Narwhals out`

    **pandera/backends/narwhals/container.py:**
    - Line 161: `# Unwrap narwhals failure_cases to native` → `# Unwrap Narwhals failure_cases to native`
    - Line 162: `# CoreCheckResult carries narwhals wrappers;` → `# CoreCheckResult carries Narwhals wrappers;`
    - Line 263: `# Narwhals backend returns a narwhals frame, not pl.LazyFrame.` → `# Narwhals backend returns a Narwhals frame, not pl.LazyFrame.`

    **pandera/backends/narwhals/components.py:**
    - Line 144: `lazy — narwhals wrappers, not native.` → `lazy — Narwhals wrappers, not native.`
    - Line 245: `pandera.ibis should produce narwhals` → `pandera.ibis should produce Narwhals`
    - Line 332: `# Convert narwhals failure_cases to native` → `# Convert Narwhals failure_cases to native`
    - Line 333: `# CoreCheckResult carries narwhals wrappers;` → `# CoreCheckResult carries Narwhals wrappers;`
    - Line 335: `# so callers can use the result without narwhals knowledge.` → `# so callers can use the result without Narwhals knowledge.`

    **pandera/engines/narwhals_engine.py:**
    - Line 32: `Accepts a :class:`~pandera.api.narwhals.types.NarwhalsData` or a` — DO NOT TOUCH (dotted code identifier, not prose).
    - Line 61: `# For nw.LazyFrame (polars): head(1).collect() stays in narwhals.` → `# For nw.LazyFrame (polars): head(1).collect() stays in Narwhals.`
    - Line 97: `"""Convert input into a narwhals-compatible` → `"""Convert input into a Narwhals-compatible`

    DO NOT touch any `import narwhals.stable.v1 as nw` line. DO NOT touch `from pandera.api.narwhals...` or `from pandera.backends.narwhals...` import lines. DO NOT rename any variable or function. DO NOT touch backticked code identifiers such as `` `nw.col` ``, `` `NarwhalsData` ``, `` `narwhals.stable.v1` ``, or `narwhals_data`.

    After editing, run `python -c "import pandera.backends.narwhals.checks, pandera.backends.narwhals.base, pandera.backends.narwhals.container, pandera.backends.narwhals.components, pandera.engines.narwhals_engine"` to confirm all files parse and import.
  </action>
  <verify>
    <automated>python -c "import ast; [ast.parse(open(p).read()) for p in ['pandera/backends/narwhals/checks.py','pandera/backends/narwhals/base.py','pandera/backends/narwhals/container.py','pandera/backends/narwhals/components.py','pandera/engines/narwhals_engine.py']]; import pandera.backends.narwhals.checks, pandera.backends.narwhals.base, pandera.backends.narwhals.container, pandera.backends.narwhals.components, pandera.engines.narwhals_engine; print('parse+import OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Narwhals raises NotImplementedError during .select()" pandera/backends/narwhals/checks.py` outputs `1`
    - `grep -c "checks to Narwhals types" pandera/backends/narwhals/checks.py` outputs `1`
    - `grep -c "stay as Narwhals wrappers" pandera/backends/narwhals/base.py` outputs `1`
    - `grep -c "# Narwhals wrapper — NOT collected here" pandera/backends/narwhals/base.py` outputs `1`
    - `grep -c "# Narwhals wrapper — NOT _to_native() here" pandera/backends/narwhals/base.py` outputs `1`
    - `grep -c "back to Narwhals" pandera/backends/narwhals/base.py` outputs `1`
    - `grep -c "# Use Narwhals ops only" pandera/backends/narwhals/base.py` outputs `1`
    - `grep -c "pure Narwhals, no backend delegation" pandera/backends/narwhals/base.py` outputs `1`
    - `grep -c "Narwhals in -> Narwhals out" pandera/backends/narwhals/base.py` outputs `1`
    - `grep -c "Unwrap Narwhals failure_cases" pandera/backends/narwhals/container.py` outputs `1`
    - `grep -c "CoreCheckResult carries Narwhals wrappers" pandera/backends/narwhals/container.py` outputs `1`
    - `grep -c "returns a Narwhals frame, not pl.LazyFrame" pandera/backends/narwhals/container.py` outputs `1`
    - `grep -c "Narwhals wrappers, not native" pandera/backends/narwhals/components.py` outputs `1`
    - `grep -c "should produce Narwhals" pandera/backends/narwhals/components.py` outputs `1`
    - `grep -c "Convert Narwhals failure_cases to native" pandera/backends/narwhals/components.py` outputs `1`
    - `grep -c "CoreCheckResult carries Narwhals wrappers" pandera/backends/narwhals/components.py` outputs `1`
    - `grep -c "without Narwhals knowledge" pandera/backends/narwhals/components.py` outputs `1`
    - `grep -c "stays in Narwhals" pandera/engines/narwhals_engine.py` outputs `1`
    - `grep -c "Convert input into a Narwhals-compatible" pandera/engines/narwhals_engine.py` outputs `1`

    **Lowercase variants eliminated:**
    - `grep -c "narwhals raises NotImplementedError" pandera/backends/narwhals/checks.py` outputs `0`
    - `grep -c "stay as narwhals wrappers" pandera/backends/narwhals/base.py` outputs `0`
    - `grep -c "# narwhals wrapper" pandera/backends/narwhals/base.py` outputs `0`
    - `grep -c "Unwrap narwhals failure_cases" pandera/backends/narwhals/container.py` outputs `0`
    - `grep -c "stays in narwhals\." pandera/engines/narwhals_engine.py` outputs `0`

    **Code identifiers preserved:**
    - `grep -c "import narwhals.stable.v1 as nw" pandera/backends/narwhals/base.py` outputs `1`
    - `grep -c "import narwhals.stable.v1 as nw" pandera/backends/narwhals/checks.py` outputs `1`
    - `grep -c "import narwhals.stable.v1 as nw" pandera/backends/narwhals/container.py` outputs `1`
    - `grep -c "import narwhals.stable.v1 as nw" pandera/backends/narwhals/components.py` outputs `1`
    - `grep -c "import narwhals.stable.v1 as nw" pandera/engines/narwhals_engine.py` outputs `1`
    - `grep -c "narwhals_data = NarwhalsData" pandera/backends/narwhals/checks.py` outputs `1` (variable name preserved)
    - `grep -c "pandera.api.narwhals.types.NarwhalsData" pandera/engines/narwhals_engine.py` outputs `1` (dotted code path preserved)

    **Files parse and import:**
    - `python -c "import pandera.backends.narwhals.checks, pandera.backends.narwhals.base, pandera.backends.narwhals.container, pandera.backends.narwhals.components, pandera.engines.narwhals_engine"` exits 0
  </acceptance_criteria>
  <done>
    All five files updated per the exact edits above, all `grep` lowercase-variant checks return 0, all `grep` capitalized-variant checks return 1, all imports still work, and no code identifiers were touched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Sweep pandera/ for any remaining lowercase prose "narwhals" and verify DOCS-02 complete</name>
  <files>
    pandera/
  </files>
  <read_first>
    - .planning/phases/02-documentation-polish/02-CONTEXT.md (D-03: fix ALL .py files in pandera/; D-04 capitalization rule)
    - Output of `grep -rn "\bnarwhals\b" pandera/ --include="*.py"` (broad sweep)
  </read_first>
  <action>
    Run a broad sweep to catch any prose "narwhals" occurrences missed by tasks 1 and 2.

    1. Run:
       ```
       grep -rn "\bnarwhals\b" pandera/ --include="*.py"
       ```

    2. For every match, classify it:
       - **Prose (capitalize):** Comments and docstrings where "narwhals" is used as the library name — e.g., "narwhals backend", "narwhals frame", "narwhals expression", "narwhals wrapper", "narwhals raises", "narwhals knowledge", "narwhals ops", "narwhals types", "stays in narwhals", "pure narwhals", "back to narwhals", "produce narwhals".
       - **Code identifier (leave alone):** import statements, variable names (`narwhals_data`, `narwhals_engine`), backticked inline code (`` `narwhals.stable.v1` ``, `` `nw.col` ``), dotted Python paths in `:class:` references, module paths (`pandera.backends.narwhals.*`, `pandera.api.narwhals.*`), file paths, from-imports.

    3. Edit each prose occurrence to capitalize "Narwhals". If task 1 or task 2 already covered it, verify and move on. If it is a new occurrence, apply the same rule.

    4. Do NOT introduce any code changes. Do NOT rename variables, functions, classes, or imports. Do NOT touch `.md`, `.rst`, or non-`.py` files.

    5. After edits, re-run the grep and verify every remaining `\bnarwhals\b` match is inside one of the protected categories (import, variable name, backticked code, dotted path, file path).

    6. Run `python -c "import pandera; import pandera.backends.narwhals.checks; import pandera.backends.narwhals.base; import pandera.backends.narwhals.container; import pandera.backends.narwhals.components; import pandera.backends.polars.register; import pandera.backends.ibis.register; import pandera.engines.narwhals_engine; import pandera.api.checks"` — must exit 0.
  </action>
  <verify>
    <automated>python -c "import pandera; import pandera.backends.narwhals.checks; import pandera.backends.narwhals.base; import pandera.backends.narwhals.container; import pandera.backends.narwhals.components; import pandera.backends.polars.register; import pandera.backends.ibis.register; import pandera.engines.narwhals_engine; import pandera.api.checks; print('all imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "\bnarwhals backend" pandera/ --include="*.py"` outputs nothing (no lowercase prose "narwhals backend" remains). Command: `grep -rn "\bnarwhals backend" pandera/ --include="*.py" | wc -l` outputs `0`.
    - `grep -rn "\bnarwhals frame" pandera/ --include="*.py" | wc -l` outputs `0`
    - `grep -rn "\bnarwhals wrapper" pandera/ --include="*.py" | wc -l` outputs `0`
    - `grep -rn "\bnarwhals expression" pandera/ --include="*.py" | wc -l` outputs `0`
    - `grep -rn "\bnarwhals ops" pandera/ --include="*.py" | wc -l` outputs `0`
    - `grep -rn "\bnarwhals types" pandera/ --include="*.py" | wc -l` outputs `0`
    - `grep -rn "\bnarwhals raises" pandera/ --include="*.py" | wc -l` outputs `0`
    - `grep -rn "\bnarwhals knowledge" pandera/ --include="*.py" | wc -l` outputs `0`
    - `grep -rn "pure narwhals" pandera/ --include="*.py" | wc -l` outputs `0`
    - `grep -rn "stays in narwhals\." pandera/ --include="*.py" | wc -l` outputs `0`
    - `grep -rn "back to narwhals" pandera/ --include="*.py" | wc -l` outputs `0`
    - `grep -rn "produce narwhals" pandera/ --include="*.py" | wc -l` outputs `0`
    - `grep -rn "Auto-detects narwhals" pandera/ --include="*.py" | wc -l` outputs `0`
    - `grep -rn "If narwhals is installed" pandera/ --include="*.py" | wc -l` outputs `0`
    - `grep -rn "If narwhals is not installed" pandera/ --include="*.py" | wc -l` outputs `0`

    **Code identifiers still present (sanity checks):**
    - `grep -rn "import narwhals.stable.v1 as nw" pandera/ --include="*.py" | wc -l` outputs a number `>= 5` (imports untouched)
    - `grep -rn "narwhals_data" pandera/backends/narwhals/checks.py | wc -l` outputs a number `>= 1` (variable preserved)
    - `grep -rn "pandera.api.narwhals" pandera/ --include="*.py" | wc -l` outputs a number `>= 1` (module path preserved)

    **Import smoke test:**
    - `python -c "import pandera; import pandera.backends.narwhals.checks; import pandera.backends.narwhals.base; import pandera.backends.narwhals.container; import pandera.backends.narwhals.components; import pandera.backends.polars.register; import pandera.backends.ibis.register; import pandera.engines.narwhals_engine; import pandera.api.checks"` exits 0
  </acceptance_criteria>
  <done>
    A broad grep sweep for lowercase prose "narwhals" in `pandera/**/*.py` returns zero matches for all prose phrases listed in acceptance criteria. All code identifiers are preserved. All listed modules import cleanly.
  </done>
</task>

</tasks>

<verification>
Phase-level checks for this plan:

1. `grep -rn "\bnarwhals\b" pandera/ --include="*.py"` — manually verify every remaining match is a code identifier (import, variable, backticked symbol, dotted path) and not prose.
2. `python -c "import pandera"` exits 0.
3. `pixi run pytest tests/backends/narwhals -x -q` passes (docstring changes should not affect tests; run as smoke check). If pixi not available, fall back to `pytest tests/backends/narwhals -x -q`.
4. `git diff --stat` shows only `.py` files under `pandera/` modified — no test files, no `.md` files, no config files.
5. Combined with plan 02-01, `grep -c "only applies when using the Narwhals backend" pandera/api/checks.py` outputs `1` (DOCS-01 preserved).
</verification>

<success_criteria>
- DOCS-02 requirement satisfied: all prose "narwhals" in `.py` files under `pandera/` is capitalized as "Narwhals"
- Code identifiers untouched (imports, variables, backticks, dotted paths)
- All edited modules parse and import cleanly
- Existing Narwhals backend tests still pass
- DOCS-01 caveat from plan 02-01 still present in `pandera/api/checks.py`
</success_criteria>

<output>
After completion, create `.planning/phases/02-documentation-polish/02-02-SUMMARY.md` listing:
- Files modified with line-count deltas
- Prose phrases capitalized (with before/after)
- Confirmation that no code identifiers were renamed
- Confirmation that the DOCS-01 caveat from plan 02-01 is preserved
</output>
