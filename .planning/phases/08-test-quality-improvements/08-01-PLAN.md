---
phase: 08-test-quality-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/pyspark/conftest.py
  - tests/pyspark/test_pyspark_config.py
  - tests/pyspark/test_pyspark_error.py
autonomous: true
requirements: [TQ-01]
must_haves:
  truths:
    - "tests/pyspark/conftest.py defines a module-level _cmp_errors function (not staticmethod, not fixture)"
    - "TestPanderaConfig._cmp_errors in test_pyspark_config.py delegates to the conftest module-level function via a one-line call"
    - "test_pyspark_error.py contains no `if CONFIG.use_narwhals_backend else` ternaries anywhere"
    - "test_pyspark_error.py imports _cmp_errors from tests.pyspark.conftest (auto-injected, but call site uses bare name)"
    - "test_pyspark_check_eq, test_pyspark_schema_data_checks, and test_pyspark_fields all use _cmp_errors(...) on DATA assertions only"
    - "SCHEMA assertions in test_pyspark_schema_data_checks and test_pyspark_fields continue using `assert dict(...) == expected[...]` (direct equality)"
    - "pytest tests/pyspark/test_pyspark_error.py tests/pyspark/test_pyspark_config.py passes under both PANDERA_USE_NARWHALS_BACKEND=True and PANDERA_USE_NARWHALS_BACKEND=False"
  artifacts:
    - path: "tests/pyspark/conftest.py"
      provides: "Module-level _cmp_errors helper (auto-importable in tests/pyspark/)"
      contains: "def _cmp_errors(actual, expected):"
    - path: "tests/pyspark/test_pyspark_config.py"
      provides: "TestPanderaConfig._cmp_errors delegation to conftest helper"
      contains: "_cmp_errors(actual, expected)"
    - path: "tests/pyspark/test_pyspark_error.py"
      provides: "Three DATA-error tests refactored to use _cmp_errors; no CONFIG ternaries"
      contains: "_cmp_errors("
  key_links:
    - from: "tests/pyspark/test_pyspark_error.py"
      to: "tests/pyspark/conftest.py::_cmp_errors"
      via: "pytest conftest auto-injection (no explicit import statement)"
      pattern: "_cmp_errors\\("
    - from: "tests/pyspark/test_pyspark_config.py::TestPanderaConfig._cmp_errors"
      to: "tests/pyspark/conftest.py::_cmp_errors"
      via: "delegation call"
      pattern: "_cmp_errors\\(actual, expected\\)"
---

<objective>
Implement TQ-01: eliminate the 6 inline `if CONFIG.use_narwhals_backend else` ternaries in `tests/pyspark/test_pyspark_error.py` DATA assertions by extracting the `_cmp_errors` helper from `TestPanderaConfig` in `test_pyspark_config.py` into a module-level function in `tests/pyspark/conftest.py`, then rewiring both test files to use it.

Purpose: Single source of truth for the structural error-dict comparison pattern. Removes backend-conditional test logic, prevents drift between two implementations, and produces cleaner test assertions that compare structural fields (check, column, schema) without coupling to error-message text format.

Output: One conftest function, one delegation refactor, six ternary removals across three test functions, and (conditionally) removal of the now-unused `CONFIG` import in test_pyspark_error.py.
</objective>

<execution_context>
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-test-quality-improvements/08-CONTEXT.md
@.planning/phases/08-test-quality-improvements/08-RESEARCH.md
@.planning/phases/08-test-quality-improvements/08-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract _cmp_errors to conftest and convert TestPanderaConfig method to delegation</name>
  <files>tests/pyspark/conftest.py, tests/pyspark/test_pyspark_config.py</files>
  <read_first>
    - tests/pyspark/conftest.py — current 191-line conftest; observe imports (datetime, os, pyspark, pytest, packaging.version, pyspark.sql.SparkSession, pandera.config.PanderaConfig) and final fixture `config_params` at line 188-191; new function appends after this with no new imports.
    - tests/pyspark/test_pyspark_config.py — current implementation of `TestPanderaConfig._cmp_errors` at lines 33-47 (the verbatim body to move); also the import block lines 1-21 to confirm no conflicting symbol named `_cmp_errors`.
    - tests/pyspark/test_pyspark_error.py — verify no top-level `_cmp_errors` exists yet so the conftest version is reachable without ambiguity.
    - .planning/phases/08-test-quality-improvements/08-CONTEXT.md — D-01 locked decision specifying module-level placement and no underscore prefix on the inner `drop_error` function.
    - .planning/phases/08-test-quality-improvements/08-PATTERNS.md — verbatim function body and delegation snippet under TQ-01 sections.
  </read_first>
  <behavior>
    - tests/pyspark/conftest.py defines `def _cmp_errors(actual, expected):` at module scope (not inside any class, not decorated as a fixture)
    - The inner helper inside `_cmp_errors` is named `drop_error` (NO underscore prefix per feedback_naming_conventions)
    - The function body is identical to the current `TestPanderaConfig._cmp_errors` body (lines 34-47 of test_pyspark_config.py): asserts `set(actual) == set(expected)`, drops the `error` key, compares remaining fields per key, asserts all error strings are non-empty
    - `TestPanderaConfig._cmp_errors` retains the `@staticmethod` decorator and signature `(actual, expected)`, but its body becomes a single call `_cmp_errors(actual, expected)` plus a one-line docstring
    - pytest tests/pyspark/test_pyspark_config.py still exits 0 (no behavioral change for any TestPanderaConfig test)
    - The conftest function is auto-injected into tests/pyspark/ namespace via pytest's conftest mechanism — no explicit import statement is required in test files
  </behavior>
  <action>
    Per D-01, extract `_cmp_errors` into `tests/pyspark/conftest.py` as a module-level function and convert `TestPanderaConfig._cmp_errors` to a delegation call.

    Step A — Append to `tests/pyspark/conftest.py` after the existing `config_params` fixture (after line 191):

    Add a module-level function `_cmp_errors(actual, expected)`. Body is the verbatim copy of the body currently inside `TestPanderaConfig._cmp_errors` at `test_pyspark_config.py` lines 34-47. Concretely the function must:
      - Define an inner helper `drop_error(entries)` (no underscore prefix) that returns a list comprehension dropping the `"error"` key from each dict entry.
      - Assert `set(actual) == set(expected)`.
      - For each key in expected, assert `drop_error(actual[key]) == drop_error(expected[key])`.
      - For each key in expected, assert `all(e["error"] for e in actual[key])`.

    Include a docstring matching the current `_cmp_errors` docstring (3 lines: "Compare pandera error dicts ignoring the exact error message text.\n\nError message format varies by backend (narwhals vs native PySpark),\nso only structural fields (check, column, schema) are compared."). No new imports are required for this function.

    Step B — Edit `tests/pyspark/test_pyspark_config.py` to replace the body of `TestPanderaConfig._cmp_errors` (lines 33-47) with a single delegation call:
      - Keep the `@staticmethod` decorator and signature `def _cmp_errors(actual, expected):`.
      - Replace the docstring with a one-line `"""Delegates to module-level _cmp_errors in conftest."""`.
      - Replace the function body with a single line: `_cmp_errors(actual, expected)`.
      - No imports change in this file — the conftest-level `_cmp_errors` is auto-injected by pytest into the test namespace.

    Do NOT modify any other test in `test_pyspark_config.py`. Do NOT add `from tests.pyspark.conftest import _cmp_errors` to either file — the auto-injection makes it unnecessary and adding it would create an inconsistent style relative to the existing tests that use shared conftest helpers.
  </action>
  <verify>
    <automated>pytest tests/pyspark/test_pyspark_config.py -x -q 2>&amp;1 | tail -20 &amp;&amp; pytest tests/pyspark/test_pyspark_config.py -x -q -p no:cacheprovider 2>&amp;1 | tail -5</automated>
    <grep>grep -nE '^def _cmp_errors\(actual, expected\)' tests/pyspark/conftest.py &amp;&amp; grep -nE '^    def drop_error\(entries\):' tests/pyspark/conftest.py &amp;&amp; grep -nE '^        _cmp_errors\(actual, expected\)$' tests/pyspark/test_pyspark_config.py</grep>
  </verify>
  <acceptance_criteria>
    - `grep -c '^def _cmp_errors(actual, expected):' tests/pyspark/conftest.py` returns exactly 1
    - `grep -c '_drop_error' tests/pyspark/conftest.py` returns 0 (no underscore-prefixed inner function)
    - `grep -A1 'def drop_error(entries):' tests/pyspark/conftest.py | grep -c '\{k: v for k, v in e.items() if k != "error"\}'` returns at least 1
    - In `tests/pyspark/test_pyspark_config.py`, the body of `TestPanderaConfig._cmp_errors` is exactly one executable line: `_cmp_errors(actual, expected)` (verifiable via `grep -nE '^        _cmp_errors\(actual, expected\)$' tests/pyspark/test_pyspark_config.py`)
    - `pytest tests/pyspark/test_pyspark_config.py -x -q` exits 0
    - `PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/test_pyspark_config.py -x -q` exits 0
  </acceptance_criteria>
  <done>Conftest contains module-level `_cmp_errors`; `TestPanderaConfig._cmp_errors` delegates to it via a single call; all existing `test_pyspark_config.py` tests continue to pass under both backend modes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace 6 CONFIG ternaries in test_pyspark_error.py with _cmp_errors on DATA assertions only</name>
  <files>tests/pyspark/test_pyspark_error.py</files>
  <read_first>
    - tests/pyspark/test_pyspark_error.py — current 312-line file; the 6 ternary sites are confirmed at lines 60-64, 70-74 (test_pyspark_check_eq), 150-154, 160-164 (test_pyspark_schema_data_checks), 232-236, 242-246 (test_pyspark_fields); SCHEMA assertions to leave unchanged are at lines 103-113 (test_pyspark_check_nullable, no ternary), 169-186 (test_pyspark_schema_data_checks SCHEMA block), 251-269 (test_pyspark_fields SCHEMA block).
    - tests/pyspark/conftest.py — confirm Task 1 added module-level `_cmp_errors` (required dependency).
    - tests/pyspark/test_pyspark_config.py — reference for the canonical `_cmp_errors(actual, expected)` call pattern.
    - .planning/phases/08-test-quality-improvements/08-RESEARCH.md — Common Pitfalls #1 (do not touch SCHEMA assertions) and #2 (verify CONFIG import removal).
    - .planning/phases/08-test-quality-improvements/08-PATTERNS.md — TQ-01 site-by-site BEFORE/AFTER snippets for the three test functions.
  </read_first>
  <behavior>
    - `grep -c 'if CONFIG.use_narwhals_backend' tests/pyspark/test_pyspark_error.py` returns 0 (all 6 ternaries removed)
    - `test_pyspark_check_eq` final assertion becomes `_cmp_errors(dict(df_out.pandera.errors["DATA"]), expected)` where `expected["DATAFRAME_CHECK"]` entries no longer contain the `"error"` key
    - `test_pyspark_schema_data_checks` DATA assertion becomes `_cmp_errors(dict(output_data.pandera.errors["DATA"]), expected["DATA"])`; the SCHEMA assertion `assert dict(output_data.pandera.errors["SCHEMA"]) == expected["SCHEMA"]` remains unchanged; entries inside `expected["DATA"]["DATAFRAME_CHECK"]` drop the `"error"` key; entries inside `expected["SCHEMA"]["WRONG_DATATYPE"]` keep their `"error"` keys (the error string is backend-invariant for SCHEMA failures)
    - `test_pyspark_fields` DATA assertion becomes `_cmp_errors(data_errors, expected["DATA"])`; the SCHEMA assertion `assert schema_errors == expected["SCHEMA"]` remains unchanged; `expected["DATA"]["DATAFRAME_CHECK"]` entries drop their `"error"` keys; `expected["SCHEMA"]["WRONG_DATATYPE"]` keeps its `"error"` key
    - If no other use of `CONFIG` remains after the edits, the `from pandera.config import CONFIG` import on line 9 is removed
    - pytest tests/pyspark/test_pyspark_error.py exits 0 under BOTH `PANDERA_USE_NARWHALS_BACKEND=True` and unset/False
  </behavior>
  <action>
    Per D-01, replace each of the 6 DATA-error ternary sites with the `_cmp_errors` helper and remove the now-redundant `"error"` keys from those `expected` dicts. Leave every SCHEMA assertion exactly as-is.

    Concretely:

    Site 1 — `test_pyspark_check_eq` (around lines 55-79): the `expected` dict has `"DATAFRAME_CHECK"` with two entries containing `"error": (... if CONFIG.use_narwhals_backend else ...)`. Drop the `"error"` key from both entries, then change the final assertion `assert dict(df_out.pandera.errors["DATA"]) == expected` to `_cmp_errors(dict(df_out.pandera.errors["DATA"]), expected)`.

    Sites 2-3 — `test_pyspark_schema_data_checks` (around lines 144-186): inside `expected["DATA"]["DATAFRAME_CHECK"]`, two entries have CONFIG ternaries — drop the `"error"` key from both. Change `assert dict(output_data.pandera.errors["DATA"]) == expected["DATA"]` to `_cmp_errors(dict(output_data.pandera.errors["DATA"]), expected["DATA"])`. LEAVE the next line `assert dict(output_data.pandera.errors["SCHEMA"]) == expected["SCHEMA"]` and the entire `expected["SCHEMA"]` sub-dict (including its `"error"` keys) unchanged.

    Sites 4-6 — `test_pyspark_fields` (around lines 226-269): inside `expected["DATA"]["DATAFRAME_CHECK"]`, two entries have CONFIG ternaries — drop the `"error"` key from both. Change `assert data_errors == expected["DATA"]` to `_cmp_errors(data_errors, expected["DATA"])`. LEAVE `assert schema_errors == expected["SCHEMA"]` and the entire `expected["SCHEMA"]` sub-dict (including its `"error"` keys) unchanged.

    After all 6 ternaries are removed, run `grep -n 'CONFIG' tests/pyspark/test_pyspark_error.py`. If the only remaining match is the import on line 9 (`from pandera.config import CONFIG`), remove that import line. If any other CONFIG reference exists, leave the import in place. Do NOT add a new import for `_cmp_errors` — conftest auto-injection makes it available by bare name.

    Do NOT modify `test_pyspark_check_nullable` (around lines 82-113) — its SCHEMA `"SERIES_CONTAINS_NULLS"` block has no CONFIG ternary and is already backend-invariant. Do NOT modify any other test function in the file.
  </action>
  <verify>
    <automated>pytest tests/pyspark/test_pyspark_error.py -x -q 2>&amp;1 | tail -10 &amp;&amp; PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/test_pyspark_error.py -x -q 2>&amp;1 | tail -10</automated>
    <grep>grep -c 'if CONFIG.use_narwhals_backend' tests/pyspark/test_pyspark_error.py &amp;&amp; grep -c '_cmp_errors' tests/pyspark/test_pyspark_error.py</grep>
  </verify>
  <acceptance_criteria>
    - `grep -c 'if CONFIG.use_narwhals_backend' tests/pyspark/test_pyspark_error.py` returns exactly 0
    - `grep -c '_cmp_errors(' tests/pyspark/test_pyspark_error.py` returns exactly 3 (one per refactored test function)
    - `grep -nE '^assert dict\(.*\.pandera\.errors\[.SCHEMA.\]\) == expected\[.SCHEMA.\]$' tests/pyspark/test_pyspark_error.py` returns at least 1 line (test_pyspark_schema_data_checks SCHEMA assertion preserved)
    - `grep -nE '^    assert schema_errors == expected\[.SCHEMA.\]$' tests/pyspark/test_pyspark_error.py` returns 1 line (test_pyspark_fields SCHEMA assertion preserved)
    - `grep -c '"error":' tests/pyspark/test_pyspark_error.py` matches the count of SCHEMA-error entries kept (test_pyspark_check_nullable's 1 + test_pyspark_schema_data_checks SCHEMA's 1 + test_pyspark_fields SCHEMA's 1 = 3); use `grep -nE '"error":' tests/pyspark/test_pyspark_error.py` to manually confirm all three remaining matches are in SCHEMA blocks, not DATA blocks
    - If `grep -c 'CONFIG' tests/pyspark/test_pyspark_error.py` returns 0, then `grep -c 'from pandera.config import CONFIG' tests/pyspark/test_pyspark_error.py` MUST also return 0 (import removed)
    - `pytest tests/pyspark/test_pyspark_error.py -x -q` exits 0
    - `PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/test_pyspark_error.py -x -q` exits 0
    - `python -c "import ast; ast.parse(open('tests/pyspark/test_pyspark_error.py').read())"` exits 0 (file is syntactically valid)
  </acceptance_criteria>
  <done>All 6 CONFIG ternaries are removed; 3 DATA assertions use `_cmp_errors`; all SCHEMA assertions are unchanged; CONFIG import removed if unused; both backend modes pass the test file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code only | No new trust boundaries — this is internal test-suite refactoring, no production input/output paths change, no new dependencies, no auth/network/storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-T | Tampering | tests/pyspark/conftest.py auto-loaded by all PySpark tests | accept | Conftest helper is a pure function with no I/O, no global state mutation, no network access. Risk of malicious tampering is equivalent to any test-suite code change and is governed by repository CODEOWNERS/review (same trust model as existing conftest fixtures). |
| T-08-01-I | Information Disclosure | _cmp_errors error message comparison | accept | Function compares structural fields and drops error message text — does not log, persist, or transmit the dropped text. The dropped error strings already appear in test assertion output on failure (same as today). No additional exposure surface. |
</threat_model>

<verification>
- Both refactored test files (`test_pyspark_config.py`, `test_pyspark_error.py`) pass under PANDERA_USE_NARWHALS_BACKEND=True and under default (False)
- conftest.py module-level `_cmp_errors` is syntactically valid Python and is reachable by bare name in both test files (no NameError)
- No CONFIG ternaries remain in `test_pyspark_error.py`; CONFIG import removed if and only if no other CONFIG reference remains in the file
- SCHEMA assertions are byte-identical to their previous form (a `git diff` should show changes only to: imports, DATA-block `"error"` keys, and DATA-block assertion calls — never to SCHEMA blocks)
</verification>

<success_criteria>
- TQ-01 closed: `tests/pyspark/test_pyspark_error.py` has zero `if CONFIG.use_narwhals_backend else` ternaries; the 6 DATA-error assertions use the shared `_cmp_errors` helper from `tests/pyspark/conftest.py`; `TestPanderaConfig._cmp_errors` delegates to the same conftest helper (single implementation, no drift)
- Existing test coverage preserved: `pytest tests/pyspark/test_pyspark_error.py tests/pyspark/test_pyspark_config.py -x` exits 0 under both backend modes
- No production-code changes in this plan (all edits are inside `tests/pyspark/`)
</success_criteria>

<output>
Create `.planning/phases/08-test-quality-improvements/08-01-SUMMARY.md` when done
</output>
