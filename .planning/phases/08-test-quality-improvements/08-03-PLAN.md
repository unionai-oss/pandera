---
phase: 08-test-quality-improvements
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/narwhals/test_arch03_schema_driven_dispatch.py
  - pandera/backends/pyspark/register.py
autonomous: true
requirements: [TQ-03, TQ-04]
must_haves:
  truths:
    - "tests/narwhals/test_arch03_schema_driven_dispatch.py contains zero inspect.getsource source-inspection tests for ColumnBackend.check_dtype"
    - "tests/narwhals/test_arch03_schema_driven_dispatch.py keeps the existing behavioral test test_check_dtype_narwhals_schema_takes_narwhals_engine_path"
    - "tests/narwhals/test_arch03_schema_driven_dispatch.py defines a spark fixture (module-scoped) and a HAS_PYSPARK guard with pyspark_only skipif marker"
    - "tests/narwhals/test_arch03_schema_driven_dispatch.py adds at least 2 new behavioral tests: test_check_dtype_pyspark_schema_pass and test_check_dtype_pyspark_schema_fail; both call ColumnBackend().check_dtype() directly with a PySpark frame and pyspark_engine-wrapped schema dtype"
    - "The new PySpark behavioral tests do NOT carry @_xfail (implementation is complete)"
    - "pandera/backends/pyspark/register.py has a comment explaining that nw.DataFrame registration is intentionally omitted (PySpark frames are always nw.LazyFrame under narwhals; consistent with ibis precedent) OR adds Check.register_backend(nw.DataFrame, NarwhalsCheckBackend) to mirror polars (D-04 chooses comment-only)"
  artifacts:
    - path: "tests/narwhals/test_arch03_schema_driven_dispatch.py"
      provides: "Behavioral test suite for ARCH-03 schema-driven dispatch (narwhals dtype path + PySpark dtype path)"
      contains: "def test_check_dtype_pyspark_schema_pass"
    - path: "tests/narwhals/test_arch03_schema_driven_dispatch.py"
      provides: "PySpark-only fail-path behavioral test"
      contains: "def test_check_dtype_pyspark_schema_fail"
    - path: "pandera/backends/pyspark/register.py"
      provides: "Comment documenting intentional omission of nw.DataFrame registration"
      contains: "nw.DataFrame"
  key_links:
    - from: "tests/narwhals/test_arch03_schema_driven_dispatch.py::test_check_dtype_pyspark_schema_pass"
      to: "pandera.backends.narwhals.components.ColumnBackend.check_dtype"
      via: "direct method call with PySpark frame + pyspark_engine.Engine.dtype(T.LongType()) schema"
      pattern: "ColumnBackend\\(\\)\\.check_dtype\\(frame, schema\\)"
    - from: "tests/narwhals/test_arch03_schema_driven_dispatch.py"
      to: "tests/narwhals/test_e2e.py (analog patterns)"
      via: "PySpark guard, pyspark_only marker, and spark fixture copied inline"
      pattern: "pytest\\.mark\\.skipif\\(not HAS_PYSPARK"
---

<objective>
Implement TQ-03 and TQ-04 together since they touch separate files but share the same broad theme (test-quality and registration cleanup for the narwhals PySpark integration).

TQ-03: replace 4 brittle source-inspection tests in `tests/narwhals/test_arch03_schema_driven_dispatch.py` (which call `inspect.getsource(ColumnBackend.check_dtype)` and string-match variable names like `is_pyspark` / `uses_pyspark_dtype`) with behavioral equivalents. Keep the existing 5th test (`test_check_dtype_narwhals_schema_takes_narwhals_engine_path`) which is already behavioral. Add 2 PySpark-gated behavioral tests that exercise the schema-driven dispatch by passing a `pyspark_engine`-wrapped dtype to `ColumnBackend().check_dtype()` with a real PySpark frame.

TQ-04: per D-04, no code change is required to the `nw.DataFrame` registration omission — it is intentional and consistent with ibis. Add a one-line comment in `pandera/backends/pyspark/register.py` documenting the rationale so future readers do not "fix" the perceived gap. This closes the SC4 success criterion as "documented; consistent with ibis."

Purpose: Source-inspection tests are brittle and test intermediate implementation state, not the contract. Behavioral tests verify the schema-driven dispatch invariant directly: a PySpark-typed schema dispatches through the PySpark-dtype path regardless of frame implementation; a narwhals-typed schema dispatches through the narwhals-engine path. The comment in `register.py` prevents drift confusion.

Output: One test-file rewrite (delete 4, keep 1, add 2 plus fixtures/guards) and one comment-only edit in `register.py`.
</objective>

<execution_context>
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-test-quality-improvements/08-CONTEXT.md
@.planning/phases/08-test-quality-improvements/08-RESEARCH.md
@.planning/phases/08-test-quality-improvements/08-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace 4 source-inspection tests with 2 PySpark-gated behavioral tests in test_arch03</name>
  <files>tests/narwhals/test_arch03_schema_driven_dispatch.py</files>
  <read_first>
    - tests/narwhals/test_arch03_schema_driven_dispatch.py — full current 136-line file. The 4 tests to delete are at lines 26-99 (test_check_dtype_has_no_is_pyspark_variable, test_check_dtype_uses_pyspark_dtype_variable, test_check_dtype_has_no_frame_implementation_probe_for_pyspark, test_check_dtype_uses_pyspark_engine_isinstance_probe). The test to KEEP is at lines 102-136 (test_check_dtype_narwhals_schema_takes_narwhals_engine_path). The current `import inspect` on line 15 must be removed once the 4 source-inspection tests are gone.
    - tests/narwhals/test_e2e.py lines 58-69 and lines 706-742 — source of the HAS_PYSPARK guard, pyspark_only marker, _spark_env_vars autouse fixture, and module-scoped spark fixture. Copy these inline into the test file (tests/narwhals/conftest.py does NOT define a spark fixture).
    - tests/narwhals/conftest.py — confirm `_ensure_narwhals_backends_registered` autouse fixture re-registers PySpark backends when pyspark is importable (lines 44-52). This means `ColumnBackend` will be the registered narwhals backend for PySpark frames in this directory.
    - pandera/backends/narwhals/components.py lines 255-345 — `ColumnBackend.check_dtype` implementation, especially the `uses_pyspark_dtype` schema-driven probe at lines 274-279 and the PySpark-dtype string comparison at lines 289-313. This is the contract the new behavioral tests verify.
    - pandera/engines/pyspark_engine.py — confirm `pyspark_engine.Engine.dtype(T.LongType())` produces a `pyspark_engine.DataType` instance whose `str()` matches the column dtype string `str(LongType())`; if `pyspark_engine.Engine.dtype()` is not the right wrapping API (verify against tests/pyspark/test_pyspark_engine.py line 19 usage), find the equivalent and use it.
    - tests/pyspark/conftest.py lines 17-40 — pattern for the spark fixture (session-scoped there; the narwhals copy uses module scope per test_e2e.py precedent).
    - .planning/phases/08-test-quality-improvements/08-CONTEXT.md — D-03 specifies: delete 4, keep 5th, add 2 PySpark-gated; placement is planner's discretion (chosen: same file `test_arch03_schema_driven_dispatch.py` per RESEARCH recommendation, preserves topical cohesion with the ARCH-03 contract).
    - .planning/phases/08-test-quality-improvements/08-RESEARCH.md — Common Pitfall #4 (do NOT mark new tests @_xfail); Open Question #2 (spark fixture availability — verified absent, must define inline); Code Examples section for the verbatim test bodies.
    - .planning/phases/08-test-quality-improvements/08-PATTERNS.md — full TQ-03 section with exact snippet for the new tests, fixture, and guards.
  </read_first>
  <behavior>
    - The 4 source-inspection tests at current lines 26-99 are deleted (`test_check_dtype_has_no_is_pyspark_variable`, `test_check_dtype_uses_pyspark_dtype_variable`, `test_check_dtype_has_no_frame_implementation_probe_for_pyspark`, `test_check_dtype_uses_pyspark_engine_isinstance_probe`)
    - `import inspect` on current line 15 is removed (no remaining uses after the 4 deletions)
    - The existing 5th test `test_check_dtype_narwhals_schema_takes_narwhals_engine_path` at lines 102-136 is preserved byte-for-byte (no changes to its body or imports)
    - A `HAS_PYSPARK` guard block and `pyspark_only` marker are added near the top of the module (after `import pytest`, before the kept test)
    - A `_spark_env_vars` autouse function-scoped fixture is added (sets SPARK_LOCAL_IP and PYARROW_IGNORE_TIMEZONE for each test, yields, restores)
    - A `spark` module-scoped fixture is added (creates a SparkSession with the Java 17+ Hadoop workaround for PySpark 4.0+, yields, calls .stop())
    - Two new behavioral tests are added:
      - `test_check_dtype_pyspark_schema_pass(spark)` — creates a PySpark DataFrame with a LongType column, wraps as narwhals frame, builds a SimpleNamespace schema with `dtype=pyspark_engine.Engine.dtype(T.LongType())`, calls `ColumnBackend().check_dtype(frame, schema)`, asserts `len(results) == 1` and `results[0].passed is True`
      - `test_check_dtype_pyspark_schema_fail(spark)` — same but `dtype=pyspark_engine.Engine.dtype(T.StringType())` against a LongType column, asserts `results[0].passed is False` and `results[0].reason_code == SchemaErrorReason.WRONG_DATATYPE`
    - BOTH new tests carry `@pyspark_only`; NEITHER carries `@_xfail`
    - Running `pytest tests/narwhals/test_arch03_schema_driven_dispatch.py -x -q` exits 0 in an environment with pyspark installed (skips the 2 new tests if pyspark is not installed; the kept narwhals test always runs)
  </behavior>
  <action>
    Per D-03 and RESEARCH placement recommendation, rewrite `tests/narwhals/test_arch03_schema_driven_dispatch.py` so it contains ONLY behavioral tests for the ARCH-03 schema-driven dispatch contract.

    Final file structure (top to bottom):

    1. Module docstring — replace the current docstring (lines 1-13) with a shorter one explaining the file now contains behavioral tests for ARCH-03 (`ColumnBackend.check_dtype` dispatches on `schema.dtype` type, not on `check_obj.implementation`), covering both the narwhals-engine dtype path (polars frame, narwhals_engine.Int64 schema) and the PySpark-engine dtype path (PySpark frame, pyspark_engine-wrapped T.LongType schema). Mention that previous source-inspection tests (which grepped `inspect.getsource(ColumnBackend.check_dtype)` for variable names) were removed in Phase 08 because they tested intermediate implementation state, not the behavioral contract.

    2. Imports — keep `polars as pl`, `pytest`. REMOVE `import inspect`. Add `import os` (needed by `_spark_env_vars`).

    3. PySpark guard block — add immediately after imports:
       - try import `pyspark.sql`, `from pyspark.sql import SparkSession`; on success set `HAS_PYSPARK = True`, on `ImportError` set `HAS_PYSPARK = False`
       - Define `pyspark_only = pytest.mark.skipif(not HAS_PYSPARK, reason="pyspark not installed")`

    4. Fixtures — copy two fixtures inline from tests/narwhals/test_e2e.py:
       - `_spark_env_vars` (autouse=True, scope="function"): if `not HAS_PYSPARK` yield and return; else save prev values of SPARK_LOCAL_IP and PYARROW_IGNORE_TIMEZONE, set them, yield, restore on exit
       - `spark` (scope="module"): `pytest.importorskip("pyspark")`, builder = SparkSession.builder.config("spark.sql.ansi.enabled", False); if PYSPARK_VERSION >= 4.0.0 add spark.hadoop.fs.defaultFS=file:/// and spark.sql.warehouse.dir=file:///tmp/spark-warehouse; yield session, then session.stop()

    5. Preserve the existing test `test_check_dtype_narwhals_schema_takes_narwhals_engine_path` (current lines 102-136) byte-for-byte. This test calls `ColumnBackend().check_dtype()` with a polars LazyFrame and a `narwhals_engine.Int64()` schema and asserts `results[0].passed is True`.

    6. Add the two new PySpark-gated behavioral tests (placement: after the kept test):

       `test_check_dtype_pyspark_schema_pass(spark)` — decorated with `@pyspark_only`. Body:
         - Local imports: `pyspark.sql.types as T`, `SimpleNamespace`, `narwhals.stable.v1 as nw`, `from pandera.backends.narwhals.components import ColumnBackend`, `from pandera.engines import pyspark_engine`
         - `df = spark.createDataFrame([(1,), (2,)], schema=["col"])` — produces a LongType column
         - `frame = nw.from_native(df, eager_or_interchange_only=False)`
         - `schema = SimpleNamespace(selector="col", name="col", nullable=True, unique=False, dtype=pyspark_engine.Engine.dtype(T.LongType()), checks=[])`
         - `results = ColumnBackend().check_dtype(frame, schema)`
         - `assert len(results) == 1` and `assert results[0].passed is True`

       `test_check_dtype_pyspark_schema_fail(spark)` — decorated with `@pyspark_only`. Body:
         - Same imports plus `from pandera.errors import SchemaErrorReason`
         - Same `df` and `frame` as the pass test
         - `schema = SimpleNamespace(selector="col", name="col", nullable=True, unique=False, dtype=pyspark_engine.Engine.dtype(T.StringType()), checks=[])` — wrong type vs the LongType column
         - `results = ColumnBackend().check_dtype(frame, schema)`
         - `assert len(results) == 1`, `assert results[0].passed is False`, `assert results[0].reason_code == SchemaErrorReason.WRONG_DATATYPE`

    Naming/style notes:
    - Inner helpers inside tests should not use underscore prefix (per feedback_naming_conventions); the new tests do not introduce inner helpers so this is not an active concern, but if `_spark_env_vars` (which is itself a fixture, not a local inner) is renamed, keep it `_spark_env_vars` because that is the established autouse-fixture name from test_e2e.py.
    - Do NOT add `@_xfail` or any xfail marker to the 2 new tests — components.py IS fully implemented (per ARCH-03 closure in Phase 04); the tests must pass immediately when pyspark is available.
    - Do NOT add the new tests to `tests/narwhals/test_components.py` — that file's existing `check_dtype` tests are all `@_xfail` (the marker is gated on `ColumnBackend is None` historical state); mixing passing PySpark tests with xfail polars tests there muddies the file's purpose. Keeping them in `test_arch03_schema_driven_dispatch.py` aligns with the ARCH-03 contract focus.

    Reference the schema-driven dispatch contract (ARCH-03) in the docstring or comments only where it adds clarity; do NOT replicate the source-inspection assertions in any new form.
  </action>
  <verify>
    <automated>pytest tests/narwhals/test_arch03_schema_driven_dispatch.py -x -q 2>&amp;1 | tail -10</automated>
    <grep>grep -c 'inspect.getsource' tests/narwhals/test_arch03_schema_driven_dispatch.py &amp;&amp; grep -c '^import inspect' tests/narwhals/test_arch03_schema_driven_dispatch.py &amp;&amp; grep -c '^def test_check_dtype_pyspark_schema_pass' tests/narwhals/test_arch03_schema_driven_dispatch.py &amp;&amp; grep -c '^def test_check_dtype_pyspark_schema_fail' tests/narwhals/test_arch03_schema_driven_dispatch.py &amp;&amp; grep -c '^def test_check_dtype_narwhals_schema_takes_narwhals_engine_path' tests/narwhals/test_arch03_schema_driven_dispatch.py</grep>
  </verify>
  <acceptance_criteria>
    - `grep -c 'inspect.getsource' tests/narwhals/test_arch03_schema_driven_dispatch.py` returns exactly 0
    - `grep -cE '^import inspect$' tests/narwhals/test_arch03_schema_driven_dispatch.py` returns exactly 0
    - `grep -cE '^def test_check_dtype_has_no_is_pyspark_variable' tests/narwhals/test_arch03_schema_driven_dispatch.py` returns 0
    - `grep -cE '^def test_check_dtype_uses_pyspark_dtype_variable' tests/narwhals/test_arch03_schema_driven_dispatch.py` returns 0
    - `grep -cE '^def test_check_dtype_has_no_frame_implementation_probe_for_pyspark' tests/narwhals/test_arch03_schema_driven_dispatch.py` returns 0
    - `grep -cE '^def test_check_dtype_uses_pyspark_engine_isinstance_probe' tests/narwhals/test_arch03_schema_driven_dispatch.py` returns 0
    - `grep -cE '^def test_check_dtype_narwhals_schema_takes_narwhals_engine_path' tests/narwhals/test_arch03_schema_driven_dispatch.py` returns 1
    - `grep -cE '^def test_check_dtype_pyspark_schema_pass' tests/narwhals/test_arch03_schema_driven_dispatch.py` returns 1
    - `grep -cE '^def test_check_dtype_pyspark_schema_fail' tests/narwhals/test_arch03_schema_driven_dispatch.py` returns 1
    - `grep -c '@pyspark_only' tests/narwhals/test_arch03_schema_driven_dispatch.py` returns at least 2
    - `grep -c '@_xfail' tests/narwhals/test_arch03_schema_driven_dispatch.py` returns 0 (none of the new or kept tests are xfailed)
    - `grep -c 'HAS_PYSPARK' tests/narwhals/test_arch03_schema_driven_dispatch.py` returns at least 2 (definition + use in skipif)
    - `grep -cE '^@pytest\.fixture\(scope="module"\)' tests/narwhals/test_arch03_schema_driven_dispatch.py` returns at least 1 (spark fixture)
    - `grep -c 'pyspark_engine.Engine.dtype' tests/narwhals/test_arch03_schema_driven_dispatch.py` returns at least 2 (pass and fail tests)
    - `python -c "import ast; ast.parse(open('tests/narwhals/test_arch03_schema_driven_dispatch.py').read())"` exits 0
    - `pytest tests/narwhals/test_arch03_schema_driven_dispatch.py -x -q` exits 0 (all tests pass or are properly skipped if pyspark is unavailable)
    - When pyspark is installed, the 2 new tests are collected and pass: `pytest tests/narwhals/test_arch03_schema_driven_dispatch.py::test_check_dtype_pyspark_schema_pass tests/narwhals/test_arch03_schema_driven_dispatch.py::test_check_dtype_pyspark_schema_fail -x -q` exits 0
  </acceptance_criteria>
  <done>4 source-inspection tests deleted; `import inspect` removed; the kept narwhals-engine test still passes; 2 new PySpark-gated behavioral tests pass without xfail; spark fixture and pyspark_only marker are wired locally.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Document intentional nw.DataFrame omission in pandera/backends/pyspark/register.py</name>
  <files>pandera/backends/pyspark/register.py</files>
  <read_first>
    - pandera/backends/pyspark/register.py — full file (~67 lines); identify the narwhals registration block around lines 55-67 where `Check.register_backend(pyspark_sql.DataFrame, NarwhalsCheckBackend)` and `Check.register_backend(nw.LazyFrame, NarwhalsCheckBackend)` are called.
    - pandera/backends/polars/register.py — reference: line 47-48 where polars DOES register `Check.register_backend(nw.DataFrame, NarwhalsCheckBackend)` (this is the asymmetry the comment explains).
    - pandera/backends/ibis/register.py — reference: ibis follows the same pattern as PySpark (LazyFrame only, no DataFrame). This is the precedent the comment cites.
    - .planning/phases/08-test-quality-improvements/08-CONTEXT.md — D-04 specifies: no code change; just add a comment explaining the omission is intentional and consistent with ibis.
    - .planning/phases/08-test-quality-improvements/08-RESEARCH.md — TQ-04 section confirms ibis precedent and the rationale (PySpark frames are always SQL-lazy `nw.LazyFrame`, never `nw.DataFrame`).
  </read_first>
  <behavior>
    - `pandera/backends/pyspark/register.py` contains a comment in or immediately adjacent to the narwhals registration block (the block that calls `Check.register_backend(nw.LazyFrame, NarwhalsCheckBackend)`) explaining that `nw.DataFrame` is intentionally NOT registered for PySpark because PySpark frames are always SQL-lazy under narwhals (exposed as `nw.LazyFrame`, never `nw.DataFrame`), and that this matches the ibis registration pattern
    - No registration call is added or removed (no behavioral change)
    - The comment references "ibis" by name as the precedent
    - Existing tests pass without modification
  </behavior>
  <action>
    Per D-04, add a comment-only edit to `pandera/backends/pyspark/register.py` documenting why `nw.DataFrame` is intentionally not registered.

    Locate the narwhals registration block — the contiguous block that registers `pyspark_sql.DataFrame` and `nw.LazyFrame` (and any `pyspark_connect.DataFrame`) with `Check.register_backend` and `NarwhalsCheckBackend`. Immediately before `Check.register_backend(nw.LazyFrame, NarwhalsCheckBackend)`, insert a comment block (2-3 short lines max) that says, in substance:

      - PySpark SQL DataFrames are always SQL-lazy under narwhals — they are exposed as `nw.LazyFrame`, never `nw.DataFrame`. Therefore `Check.register_backend(nw.DataFrame, NarwhalsCheckBackend)` is intentionally omitted (registering it would add unreachable code).
      - This matches the ibis registration in `pandera/backends/ibis/register.py`, which also registers only `nw.LazyFrame` for the same reason. The polars registration in `pandera/backends/polars/register.py` DOES register `nw.DataFrame` because polars frames can be eager.

    Use `#` line comments (not a docstring, not a noqa). Keep the comment terse — 2 to 4 lines total. Cite "ibis" by name and the contrast with polars by name.

    Do NOT add `Check.register_backend(nw.DataFrame, NarwhalsCheckBackend)`. Do NOT modify any other line in the file. Do NOT touch the conditional gate (e.g. `if config.use_narwhals_backend`) that wraps the narwhals branch.

    After the edit, verify the file is still syntactically valid and that the narwhals branch still registers exactly `pyspark_sql.DataFrame` (and `pyspark_connect.DataFrame` if present in the existing logic) plus `nw.LazyFrame` — and nothing else.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('pandera/backends/pyspark/register.py').read())" &amp;&amp; PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/test_pyspark_narwhals_register.py -x -q 2>&amp;1 | tail -10</automated>
    <grep>grep -cE 'nw\.DataFrame' pandera/backends/pyspark/register.py &amp;&amp; grep -cE '^[[:space:]]*#.*ibis' pandera/backends/pyspark/register.py</grep>
  </verify>
  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('pandera/backends/pyspark/register.py').read())"` exits 0
    - `grep -cE 'Check\.register_backend\(nw\.DataFrame' pandera/backends/pyspark/register.py` returns exactly 0 (no registration call added)
    - `grep -cE 'nw\.DataFrame' pandera/backends/pyspark/register.py` returns at least 1 (the symbol appears in the new comment)
    - `grep -cE '^[[:space:]]*#.*ibis' pandera/backends/pyspark/register.py` returns at least 1 (comment references the ibis precedent)
    - `grep -cE '^[[:space:]]*#' pandera/backends/pyspark/register.py` returns strictly more than the count before the edit (use `git diff --stat pandera/backends/pyspark/register.py` to confirm only comment additions, no executable line changes — `git diff -G '^[^#]' pandera/backends/pyspark/register.py` should show no non-comment changes)
    - `PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/test_pyspark_narwhals_register.py -x -q` exits 0 (registration behavior is unchanged; existing registration test still passes)
  </acceptance_criteria>
  <done>Comment block added before the narwhals registration in register.py explaining the intentional nw.DataFrame omission and citing the ibis precedent; no executable code changed; existing registration tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code + documentation comment | TQ-03 changes test files only (no production code). TQ-04 is a comment-only change in `register.py` (no executable change). Neither alters any trust boundary, input validation surface, dependency, or runtime behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-T | Tampering | tests/narwhals/test_arch03_schema_driven_dispatch.py — fixture creates a SparkSession | accept | The `spark` fixture uses `SparkSession.builder.getOrCreate()` with local config (`spark.sql.ansi.enabled=false`, file:/// for PySpark 4.0+) — same pattern as the existing tests/narwhals/test_e2e.py. No network exposure, no external service. Test-only, gated by `pyspark_only` skipif. |
| T-08-03-R | Repudiation | Documentation comment in register.py could be edited away later | accept | Comment is intent documentation only; correctness of registration is enforced by tests (`test_pyspark_narwhals_register.py`). If a future change removes the comment AND adds an erroneous `nw.DataFrame` registration, existing registration tests would still pass (unreachable code is silent), but the ibis-PySpark consistency invariant would still be verifiable by inspection. Acceptable risk for a comment-only mitigation. |
| T-08-03-I | Information Disclosure | _spark_env_vars sets SPARK_LOCAL_IP=127.0.0.1, PYARROW_IGNORE_TIMEZONE=1 | accept | Identical to existing test_e2e.py pattern. Loopback-only Spark binding; no external exposure. Env vars restored on test exit. |
</threat_model>

<verification>
- `tests/narwhals/test_arch03_schema_driven_dispatch.py` no longer contains `inspect.getsource` calls, no longer imports `inspect`, and runs the 2 new behavioral tests + the 1 kept narwhals test successfully under PANDERA_USE_NARWHALS_BACKEND=True with pyspark installed
- `pandera/backends/pyspark/register.py` contains a comment block referencing `nw.DataFrame` and `ibis` (the rationale for the asymmetric registration), and `git diff -G '^[^#]'` against the prior commit shows no executable code changes in the file
- Existing PySpark narwhals registration test (`tests/pyspark/test_pyspark_narwhals_register.py::test_pyspark_narwhals_activated_when_opted_in`) still passes
</verification>

<success_criteria>
- TQ-03 closed: 4 source-inspection tests deleted; `import inspect` removed; existing 5th behavioral test kept verbatim; 2 new PySpark-gated behavioral tests (`test_check_dtype_pyspark_schema_pass`, `test_check_dtype_pyspark_schema_fail`) verify schema-driven dispatch for the PySpark-engine dtype path; new tests do NOT carry @_xfail
- TQ-04 closed: `pandera/backends/pyspark/register.py` documents the intentional `nw.DataFrame` omission with a comment referencing the ibis precedent; no executable behavior change; existing registration tests still pass
- All work is within `tests/narwhals/test_arch03_schema_driven_dispatch.py` and `pandera/backends/pyspark/register.py` — no other files modified
</success_criteria>

<output>
Create `.planning/phases/08-test-quality-improvements/08-03-SUMMARY.md` when done
</output>
