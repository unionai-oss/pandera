---
phase: 02-test-coverage-and-ci
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - tests/pyspark/test_pyspark_config.py
  - tests/pyspark/test_pyspark_check.py
  - tests/pyspark/test_pyspark_container.py
  - tests/pyspark/test_pyspark_decorators.py
  - tests/pyspark/test_pyspark_error.py
  - tests/pyspark/test_pyspark_accessor.py
  - tests/pyspark/test_pyspark_dtypes.py
  - tests/pyspark/test_pyspark_model.py
  - pandera/backends/narwhals/builtin_checks.py
  - pandera/backends/narwhals/base.py
  - pandera/backends/narwhals/checks.py
  - pandera/backends/narwhals/container.py
  - pandera/backends/narwhals/column.py
autonomous: false
requirements: [TEST-01, TEST-03]
user_setup:
  - service: pyspark-local
    why: "Running tests/pyspark/ requires Java 17 and pyspark installed locally; the nox session wraps both, but a local Java install is required for nox to bootstrap"
    env_vars:
      - name: JAVA_HOME
        source: "Local Java 17 install (zulu 17 recommended to match CI)"
must_haves:
  truths:
    - "Running `PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/ -k 'spark and not spark_connect'` produces no unexpected failures: every failing test is either an xfail (with reason) or fixed in narwhals backend source"
    - "A triage report (TRIAGE.md committed under the phase directory) lists every test in tests/pyspark/ along with its outcome under narwhals: pass, xfail (with reason), or fix (with commit reference)"
    - "Every test that fails because of dtype string format divergence, error message format divergence, or any other discovered narwhals-specific limitation either has a new xfail marker (if it is an SQL-lazy limitation) or a backend fix (if it is a true bug per TEST-03)"
    - "No test marked xfail in this phase uses os.getenv(...) — all conditions are CONFIG.use_narwhals_backend (D-03)"
    - "Every newly-added xfail marker uses strict=True (D-04)"
    - "TEST-03 bug fixes (if any) preserve native pyspark behavior: running the same tests with PANDERA_USE_NARWHALS_BACKEND unset still passes"
  artifacts:
    - path: ".planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md"
      provides: "Per-test triage record (file, test id, outcome, disposition, reason or fix link)"
      min_lines: 30
    - path: "tests/pyspark/"
      provides: "All test files with xfail markers covering every expected SQL-lazy limitation discovered during triage"
      contains: "condition=CONFIG.use_narwhals_backend"
  key_links:
    - from: "Triage run output"
      to: ".planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md"
      via: "executor captures pytest output and categorizes each failure"
      pattern: "## "
    - from: "Each xfail marker added during triage"
      to: "pandera.config.CONFIG"
      via: "condition=CONFIG.use_narwhals_backend"
      pattern: "condition=CONFIG\\.use_narwhals_backend"
    - from: "Any TEST-03 backend fix"
      to: "tests/pyspark/<file>"
      via: "verified by re-running the previously-failing test without xfail"
      pattern: "PASSED"
---

<objective>
Execute the triage run identified by RESEARCH.md, categorize every failure in `tests/pyspark/` under `PANDERA_USE_NARWHALS_BACKEND=True`, and close the loop:

1. **Expected SQL-lazy limitations not already covered by Plan 01** → add new xfail markers using the same canonical pattern.
2. **True narwhals backend bugs (TEST-03)** → diagnose and fix in `pandera/backends/narwhals/`. The deferred "synthetic column construction refactor" (CONTEXT.md) is in scope ONLY if it surfaces as a blocking failure here.
3. **Pre-existing failures unrelated to the narwhals backend** (e.g., `spark_connect` fixture errors from no running server) → document in TRIAGE.md and exclude from the run via `-k "spark and not spark_connect"`; do not add xfail markers.

After all xfails are applied and all true bugs are fixed, the human verification checkpoint confirms the full `tests/pyspark/` suite is clean under the narwhals backend (per ROADMAP success criterion 1).

Purpose: Close TEST-01 (no unexpected failures) and TEST-03 (true bugs fixed). RESEARCH.md identified ASSUMED-risk files (`test_pyspark_error.py`, `test_pyspark_accessor.py`, `test_pyspark_dtypes.py`, `test_pyspark_model.py`) where dtype string and error message divergence is theoretical until the actual run produces output — those assumptions resolve here.

Output:
- Updated test files with any additional xfail markers (uniform canonical pattern)
- Any source fixes in `pandera/backends/narwhals/` for true bugs (with passing tests)
- `02-03-TRIAGE.md` committed under the phase directory recording every test's outcome
</objective>

<execution_context>
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-test-coverage-and-ci/02-CONTEXT.md
@.planning/phases/02-test-coverage-and-ci/02-RESEARCH.md
@.planning/phases/02-test-coverage-and-ci/02-PATTERNS.md
@.planning/phases/02-test-coverage-and-ci/02-01-SUMMARY.md
@.planning/phases/02-test-coverage-and-ci/02-02-SUMMARY.md

<interfaces>
<!-- Canonical xfail pattern (must match Plan 01 exactly for any new markers added here) -->

```python
from pandera.config import CONFIG

@pytest.mark.xfail(
    condition=CONFIG.use_narwhals_backend,
    reason="<specific reason discovered during triage>",
    strict=True,
)
```

<!-- Narwhals backend surface (locations TEST-03 fixes would land) -->

Narwhals backend modules:
- pandera/backends/narwhals/base.py — subsample() raises NotImplementedError for sample=/tail= on SQL-lazy
- pandera/backends/narwhals/checks.py — element_wise raises NotImplementedError on SQL-lazy
- pandera/backends/narwhals/builtin_checks.py — registered builtin checks (missing unique_values_eq per RESEARCH.md)
- pandera/backends/narwhals/container.py — DataFrameSchemaBackend.validate(); synthetic column construction at lines 318-323
- pandera/backends/narwhals/column.py — ColumnBackend.check_dtype() using narwhals_engine

<!-- Common pyspark dtype error message format (likely divergence point per RESEARCH.md Pitfall 3) -->

Native pyspark error format example:
"expected column 'col' to have type array<string>, got array<string>"

Narwhals backend likely produces a different dtype string via str(nw_dtype). Tests in test_pyspark_error.py and test_pyspark_accessor.py assert these strings exactly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run triage and produce TRIAGE.md classifying every test outcome</name>
  <files>.planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md</files>
  <read_first>
    - .planning/phases/02-test-coverage-and-ci/02-RESEARCH.md (sections "Triage Map: Expected Failures by File", "Common Pitfalls", and "Assumptions Log" — every assumption A1-A7 must be resolved here)
    - .planning/phases/02-test-coverage-and-ci/02-PATTERNS.md (section "Triage-Dependent Files" lists the four files whose outcomes are not yet known)
    - tests/pyspark/conftest.py (understand the spark vs spark_connect fixtures — spark_connect needs a live sc://localhost server and is expected to error in CI without one)
  </read_first>
  <action>
Run the PySpark test suite under the narwhals backend and produce a triage report.

**Step 1 — Run the triage command:**
```bash
PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/ \
  --ignore=tests/pyspark/test_schemas_on_pyspark_pandas.py \
  -k "spark and not spark_connect" \
  -v --no-header -q --tb=short 2>&1 | tee /tmp/pyspark-narwhals-triage.log
```

The `--ignore` flag excludes `test_schemas_on_pyspark_pandas.py` which uses `pandera.pandas` and is unaffected by the narwhals flag (verified in RESEARCH.md). The `-k "spark and not spark_connect"` filter excludes the pre-existing Spark Connect fixture errors (no live server). Use `-v` for full test IDs.

If pyspark isn't installed locally, run via nox first:
```bash
nox -v -db uv --non-interactive --session "tests_narwhals_backend-3.11(extra='pyspark')" 2>&1 | tee /tmp/pyspark-narwhals-triage.log
```

**Step 2 — Categorize each failure into one of four buckets:**

A. **`xfail already applied by Plan 01`** — test already marked. Expected outcomes: `XFAIL` (pass) or `XPASS` (treat as backend fix — see Step 3).

B. **New expected SQL-lazy limitation** — test failed because of a documented narwhals limitation that Plan 01 missed (e.g., dtype string format divergence in test_pyspark_error.py per RESEARCH.md Pitfall 3 / A4). Needs a new xfail marker — handled in Task 2.

C. **True narwhals backend bug (TEST-03)** — test failed because of a narwhals backend defect, not an SQL-lazy limitation. Needs a fix in `pandera/backends/narwhals/` — handled in Task 3.

D. **Pre-existing failure unrelated to narwhals** — e.g., `spark_connect` fixture error (already filtered out by `-k`), unrelated flaky test. Document, do not modify.

**Step 3 — Handle XPASS results.** A test that XPASSes (was xfail-marked but passed) means narwhals fixed the limitation or the marker was wrong. Action: remove the xfail marker; the test should pass without it. Document each XPASS in TRIAGE.md.

**Step 4 — Write `.planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md`** with this exact structure (use the Write tool, do not use heredoc):

```markdown
# Phase 2 Triage Report

**Date:** {today}
**Command:** {exact command used}
**Outcome summary:** {N} passed, {N} xfailed (passing), {N} failed-categorized-B, {N} failed-categorized-C, {N} skipped/errored-D

## Test Results

| File | Test ID | Outcome | Category | Disposition | Reason or Fix Reference |
|---|---|---|---|---|---|
| test_pyspark_config.py | TestPanderaConfig::test_disable_validation | XFAIL | A | already xfail (Plan 01) | "Narwhals backend sets use_narwhals_backend=True..." |
| ... | ... | ... | ... | ... | ... |

## Category B — New xfail markers to add (handled in Task 2)

For each row in Category B:
- File: <path>
- Test: <test id>
- Failure summary: <one line of the assertion or exception>
- Proposed reason: <text for reason= argument>

## Category C — Backend bugs to fix (handled in Task 3)

For each row in Category C:
- Test: <test id>
- Failure summary: <one line>
- Likely source: <narwhals module / function suspected>
- Fix approach: <brief>

## Category D — Pre-existing failures (no action)

- spark_connect fixture errors: {N} tests, filtered by -k "not spark_connect"
- Others: <list any>

## Resolved Assumptions (from RESEARCH.md)

- A1 (test_cache_dataframe_requirements passes): {confirmed | refuted}
- A2 (test_cache_dataframe_settings fails): {confirmed | refuted}
- A3 (test_pyspark_dtypes.py mostly passes): {confirmed | refuted | details}
- A4 (test_pyspark_error.py mostly passes — dtype string risk): {confirmed | refuted | details}
- A5 (spark_connect failures are pre-existing): {confirmed | refuted}
- A6 (pyspark pulls pandas transitively): {confirmed | refuted}
- A7 (TestCustomCheck tests fail because of API mismatch): {confirmed | refuted}
```

Fill in every row. If pytest output is huge, pipe through `grep -E "^(PASSED|FAILED|XFAIL|XPASS|ERROR)"` to extract verdicts.

**Step 5 — Commit TRIAGE.md** with message `docs(phase-2): record triage outcomes for tests/pyspark under narwhals backend`.

If the triage run identifies zero Category B and zero Category C entries (i.e., Plan 01 covered everything and there are no bugs), Tasks 2 and 3 below become no-ops — TRIAGE.md still records the run.
  </action>
  <verify>
    <automated>cd /Users/deepyaman/github/unionai-oss/pandera && test -f .planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md && grep -c "^| " .planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md && grep -E "^- A[1-7] \(" .planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md` exists
    - `grep -c "^## Test Results" .planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md` returns exactly 1
    - `grep -c "^## Category B" .planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md` returns exactly 1
    - `grep -c "^## Category C" .planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md` returns exactly 1
    - `grep -c "^## Resolved Assumptions" .planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md` returns exactly 1
    - `grep -cE "^- A[1-7] \(" .planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md` returns exactly 7 (every assumption A1-A7 has an explicit resolution line)
    - `grep -c "^| " .planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md` returns at least 30 (one row per test in tests/pyspark/ minus the ignored file; the suite has ~40+ collected tests when spark_connect is filtered)
    - The triage command output is preserved at `/tmp/pyspark-narwhals-triage.log` (or attached/referenced from TRIAGE.md if /tmp is volatile)
  </acceptance_criteria>
  <done>TRIAGE.md exists with one row per test, four named categories filled out, all 7 RESEARCH.md assumptions explicitly resolved, and the triage log preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply additional xfail markers for Category B failures discovered during triage</name>
  <files>tests/pyspark/test_pyspark_error.py, tests/pyspark/test_pyspark_accessor.py, tests/pyspark/test_pyspark_dtypes.py, tests/pyspark/test_pyspark_model.py, tests/pyspark/test_pyspark_decorators.py, tests/pyspark/test_pyspark_config.py, tests/pyspark/test_pyspark_check.py, tests/pyspark/test_pyspark_container.py</files>
  <read_first>
    - .planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md (the "Category B" section drives this task — one xfail to add per entry. If Category B is empty, this task is a no-op.)
    - tests/ibis/test_ibis_check.py (lines 43-47 — canonical xfail pattern, identical to what Plan 01 used)
    - For each file in `files`: read the file before modifying to find the existing imports and the line numbers of the tests called out in TRIAGE.md Category B
  </read_first>
  <action>
For each entry in the "Category B" section of `02-03-TRIAGE.md`:

1. **Ensure `CONFIG` is imported.** Open the file. If the file already imports CONFIG (Plan 01 may have added it), skip. Otherwise add `CONFIG` to the existing `from pandera.config import ...` line (do not introduce a new import line if one exists).

2. **Add the xfail decorator** immediately above the test function/method (4-space indent for methods inside a class, 0-space indent for module-level functions). When stacked with `@pytest.mark.parametrize`, the xfail MUST be the outermost decorator:

```python
@pytest.mark.xfail(
    condition=CONFIG.use_narwhals_backend,
    reason="<reason from TRIAGE.md Category B>",
    strict=True,
)
```

The `reason` string must come from the "Proposed reason" field of the TRIAGE.md Category B entry — it must be specific (not "narwhals limitation") and reference the concrete divergence (e.g., "narwhals dtype string 'Array(String)' differs from pyspark 'array<string>'", or "narwhals DataFrameSchemaBackend.validate() does not emit pyspark cache log messages").

Per D-03: condition is `CONFIG.use_narwhals_backend`, never `os.getenv(...)`. Per D-04: `strict=True` is mandatory.

3. **Re-run the affected tests** after marker placement to confirm each one now XFAILs cleanly:
```bash
PANDERA_USE_NARWHALS_BACKEND=True pytest <file>::<test_id> -v
```
Each newly-marked test must show as `XFAIL` (not `FAILED` or `XPASS`).

**If TRIAGE.md Category B is empty, this task is a no-op** — record that fact in the task summary and skip to verification (which still passes because no new markers are required).

Do NOT modify any test body. Do NOT remove markers added in Plan 01. Do NOT add xfails outside `tests/pyspark/`.
  </action>
  <verify>
    <automated>cd /Users/deepyaman/github/unionai-oss/pandera && python -c "
import re, ast, pathlib
# Every xfail in tests/pyspark/ uses canonical form
files = list(pathlib.Path('tests/pyspark').glob('test_*.py'))
violations = []
for f in files:
    src = f.read_text()
    try:
        ast.parse(src)
    except SyntaxError as e:
        violations.append((f.name, f'syntax: {e}'))
        continue
    # Find xfail blocks
    for m in re.finditer(r'@pytest\.mark\.xfail\(([^)]*?)\)', src, re.DOTALL):
        body = m.group(1)
        if 'CONFIG.use_narwhals_backend' in body or 'use_narwhals_backend' in body:
            if 'strict=True' not in body:
                violations.append((f.name, 'xfail missing strict=True'))
            if 'os.getenv' in body:
                violations.append((f.name, 'xfail uses os.getenv'))
if violations:
    for v in violations: print(v)
    raise SystemExit(1)
print('All pyspark xfail markers use canonical form')
"</automated>
  </verify>
  <acceptance_criteria>
    - Every xfail marker in `tests/pyspark/` that references `use_narwhals_backend` uses `condition=CONFIG.use_narwhals_backend` and `strict=True` (verified by the inline Python script above)
    - No xfail marker in `tests/pyspark/` uses `os.getenv` (D-03)
    - For each Category B test in TRIAGE.md, the test ID appears in a `grep -A 5 "def <test_name>"` output that shows `@pytest.mark.xfail` immediately above the def (within 6 lines)
    - All eight test files parse as valid Python: `python -c "import ast; [ast.parse(open(f).read()) for f in ['tests/pyspark/test_pyspark_error.py','tests/pyspark/test_pyspark_accessor.py','tests/pyspark/test_pyspark_dtypes.py','tests/pyspark/test_pyspark_model.py','tests/pyspark/test_pyspark_decorators.py','tests/pyspark/test_pyspark_config.py','tests/pyspark/test_pyspark_check.py','tests/pyspark/test_pyspark_container.py']]"` exits 0
    - If Category B is non-empty, re-running the targeted tests under `PANDERA_USE_NARWHALS_BACKEND=True` produces `XFAIL` (not `FAILED`) for every newly-marked test
  </acceptance_criteria>
  <done>All Category B failures from TRIAGE.md have canonical xfail markers; every xfail in tests/pyspark/ uses CONFIG.use_narwhals_backend + strict=True; files parse cleanly; re-run shows XFAIL for each newly-marked test.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix Category C true bugs in pandera/backends/narwhals/</name>
  <files>pandera/backends/narwhals/builtin_checks.py, pandera/backends/narwhals/base.py, pandera/backends/narwhals/checks.py, pandera/backends/narwhals/container.py, pandera/backends/narwhals/column.py</files>
  <read_first>
    - .planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md (the "Category C" section drives this task — one fix per entry. If Category C is empty, this task is a no-op.)
    - For each Category C entry, read the suspected source file (listed in the "Likely source" field of the TRIAGE.md entry) before modifying
    - .planning/phases/02-test-coverage-and-ci/02-CONTEXT.md (the "Deferred Ideas" section: synthetic column construction refactor at pandera/backends/narwhals/container.py:318-323 — IN SCOPE only if it surfaces as a Category C blocking failure)
  </read_first>
  <action>
For each entry in the "Category C — Backend bugs to fix" section of `02-03-TRIAGE.md`:

1. **Reproduce the failure locally** by running the single failing test under the narwhals backend:
```bash
PANDERA_USE_NARWHALS_BACKEND=True pytest <file>::<test_id> -v --tb=long
```

2. **Diagnose the root cause** by reading the narwhals backend module identified in the "Likely source" field. Cross-reference with the trace to pinpoint the exact function.

3. **Implement the fix in `pandera/backends/narwhals/`.** The fix must:
   - Be the minimal change that makes the test pass
   - NOT change behavior for the existing polars/ibis narwhals coverage (verify by re-running `nox -s "tests_narwhals_backend-3.11(extra='polars')"` and `(extra='ibis')` after fix — those sessions must remain green)
   - NOT break native pyspark behavior (verify by re-running `pytest <file>::<test_id>` WITHOUT `PANDERA_USE_NARWHALS_BACKEND=True` — must still pass)
   - Follow existing code patterns in the file (style, naming, imports)

4. **Re-run the previously-failing test** without xfail to confirm it now PASSES:
```bash
PANDERA_USE_NARWHALS_BACKEND=True pytest <file>::<test_id> -v
```
Expected: `PASSED`.

5. **Record the fix in TRIAGE.md.** Update the Category C row's "Disposition" to include the commit SHA or "fixed in this plan" with a brief description of the change.

**Special case — `unique_values_eq`:** RESEARCH.md identifies this as either xfail (Plan 01 covered it) OR a TEST-03 fix (implement in `pandera/backends/narwhals/builtin_checks.py`). The Plan 01 xfail is the default disposition. Only implement `unique_values_eq` in narwhals if TRIAGE.md Category C explicitly elevates it from xfail to fix (which would require removing the Plan 01 xfail from `test_pyspark_check.py::TestUniqueValuesEqCheck` and the `tests/common/test_builtin_checks.py:1379-1383` xfail).

**Special case — synthetic column construction refactor (CONTEXT.md Deferred):** Only touch `pandera/backends/narwhals/container.py:318-323` if a Category C entry specifically attributes the failure to this code path. Otherwise this remains deferred.

**If TRIAGE.md Category C is empty, this task is a no-op** — record that fact in the task summary and skip to verification. No backend file should be modified.

Do NOT modify backend modules unless required by a Category C entry. Do NOT modify test files in this task (Task 2 handles test markers; xfails for fixed bugs were either removed because the test now passes, or never added).
  </action>
  <verify>
    <automated>cd /Users/deepyaman/github/unionai-oss/pandera && python -c "
import ast, pathlib
# Backend modules parse cleanly
modules = [
  'pandera/backends/narwhals/builtin_checks.py',
  'pandera/backends/narwhals/base.py',
  'pandera/backends/narwhals/checks.py',
  'pandera/backends/narwhals/container.py',
  'pandera/backends/narwhals/column.py',
]
for m in modules:
    ast.parse(pathlib.Path(m).read_text())
print('All narwhals backend modules parse OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - Every narwhals backend module modified in this task parses as valid Python (AST check above)
    - For every Category C entry in TRIAGE.md, the referenced test passes under `PANDERA_USE_NARWHALS_BACKEND=True pytest <test_id>` after the fix (output contains `PASSED` not `FAILED` or `XFAIL`)
    - Running the existing polars narwhals session does not regress: `nox -s "tests_narwhals_backend-3.11(extra='polars')"` exits 0 (manual check OK at human-verify checkpoint)
    - Running the existing ibis narwhals session does not regress: `nox -s "tests_narwhals_backend-3.11(extra='ibis')"` exits 0 (manual check OK at human-verify checkpoint)
    - TRIAGE.md Category C section has every entry annotated with a fix description or commit reference
    - If Category C was empty: no narwhals backend module is modified (`git diff --name-only pandera/backends/narwhals/` is empty)
  </acceptance_criteria>
  <done>Every Category C bug has a minimal fix in pandera/backends/narwhals/; the previously-failing tests now PASS; polars+ibis narwhals sessions still green; TRIAGE.md updated with fix references. (No-op if Category C empty.)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — full tests/pyspark/ suite clean under narwhals</name>
  <what-built>
    - Plan 01 xfail markers (11 tests across 4 files)
    - Plan 02 nox session + CI matrix extension
    - Plan 03 Task 1 triage report (02-03-TRIAGE.md)
    - Plan 03 Task 2 additional xfail markers for Category B (if any)
    - Plan 03 Task 3 backend fixes for Category C (if any)
  </what-built>
  <how-to-verify>
    Run these commands and confirm each one finishes with no unexpected failures:

    1. Local fast smoke (filter out spark_connect since no live server):
       ```bash
       PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/ \
         --ignore=tests/pyspark/test_schemas_on_pyspark_pandas.py \
         -k "spark and not spark_connect" \
         -v --tb=short
       ```
       Expected verdict line: every test is either `PASSED` or `XFAIL`. No `FAILED`, no `XPASS`.
       (Errors on `spark_connect` fixtures are filtered out by `-k`.)

    2. Nox session under pyspark extra (full CI parity check):
       ```bash
       nox -s "tests_narwhals_backend-3.11(extra='pyspark')"
       ```
       Expected exit code: 0.

    3. Confirm polars/ibis narwhals sessions did not regress:
       ```bash
       nox -s "tests_narwhals_backend-3.11(extra='polars')"
       nox -s "tests_narwhals_backend-3.11(extra='ibis')"
       ```
       Expected: both exit 0.

    4. Visually skim `.planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md` — confirm:
       - Every test in `tests/pyspark/` (minus the ignored file) has a row
       - Every Category B entry has a corresponding xfail marker in the codebase (`grep -n` the test name to confirm)
       - Every Category C entry has a fix description or commit reference

    5. Confirm uniformity of xfail markers across `tests/pyspark/`:
       ```bash
       grep -rE 'condition=os\.getenv' tests/pyspark/
       ```
       Expected: no output (D-03 compliance — every xfail uses `CONFIG.use_narwhals_backend`).

       ```bash
       python -c "
       import re, pathlib
       for f in pathlib.Path('tests/pyspark').glob('test_*.py'):
           src = f.read_text()
           for m in re.finditer(r'@pytest\.mark\.xfail\([^)]*?use_narwhals_backend[^)]*?\)', src, re.DOTALL):
               assert 'strict=True' in m.group(0), (f.name, m.group(0))
       print('All narwhals xfails use strict=True')
       "
       ```
       Expected: prints `All narwhals xfails use strict=True`.
  </how-to-verify>
  <resume-signal>Type "approved" if all five checks pass, or describe the failing check.</resume-signal>
</task>

</tasks>

<verification>
Final post-checkpoint validation:

```bash
# 1. TRIAGE.md is complete and committed
test -f .planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md
git log --oneline -- .planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md | head -1

# 2. No regressions in polars/ibis narwhals coverage
# (Run in checkpoint Task 4; recorded here as evidence)

# 3. Every Category C entry in TRIAGE.md has a fix; every Category B has an xfail
# (Validated visually at checkpoint Task 4 step 4)
```
</verification>

<success_criteria>
- `02-03-TRIAGE.md` documents every test in `tests/pyspark/` (excluding the ignored pyspark-pandas file) with an outcome
- Every "Category B" failure has a new canonical xfail marker (4-line decorator, `CONFIG.use_narwhals_backend`, `strict=True`, specific reason)
- Every "Category C" failure has a minimal fix in `pandera/backends/narwhals/` that makes the test pass without an xfail
- Running `PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/ -k "spark and not spark_connect"` produces only `PASSED` and `XFAIL` outcomes — no `FAILED`, no `XPASS`
- `nox -s "tests_narwhals_backend-3.11(extra='pyspark')"` succeeds
- `nox -s "tests_narwhals_backend-3.11(extra='polars')"` and `(extra='ibis')` still succeed (no regressions)
- All 7 RESEARCH.md assumptions A1-A7 are resolved (confirmed/refuted) in TRIAGE.md
- No xfail in `tests/pyspark/` uses `os.getenv` (D-03)
- Every xfail in `tests/pyspark/` referencing `use_narwhals_backend` uses `strict=True` (D-04)
- Phase ROADMAP success criteria 1, 2, 3 all met (criterion 4 covered by Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/02-test-coverage-and-ci/02-03-SUMMARY.md`
</output>
