---
phase: 02-test-coverage-and-ci
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - pandera/backends/narwhals/container.py
  - pandera/backends/narwhals/base.py
  - pandera/backends/narwhals/components.py
  - .planning/ROADMAP.md
autonomous: false
requirements: [TEST-01, TEST-03]
gap_closure: true
closes_gaps:
  - "VERIFICATION SC1: 300 unexpected failures under PANDERA_USE_NARWHALS_BACKEND=True"
  - "VERIFICATION SC3: 4 Category C root-cause backend bugs identified but deferred (zero fixed)"
  - "VERIFICATION SC2c: row-index in failure_cases applicability to PySpark (human decision)"
user_setup:
  - service: pyspark-local
    why: "Running tests/pyspark/ requires Java 17 and PySpark installed locally so the fix can be reproduced and verified per-test"
    env_vars:
      - name: JAVA_HOME
        source: "Local Java 17 install (zulu 17 recommended to match CI). The user previously used /Users/deepyaman/Library/Caches/rattler/cache/pkgs/openjdk-17.0.17-h99a4030_0/lib/jvm — adjust to your local path."
must_haves:
  truths:
    - "Running `PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/ -k 'spark and not spark_connect' --ignore=tests/pyspark/test_schemas_on_pyspark_pandas.py` produces 0 unexpected FAILED outcomes — every test is either PASSED or XFAIL"
    - "When narwhals DataFrameSchemaBackend.validate() is called with a PySpark DataFrame and lazy=True, the function returns the native PySpark DataFrame with errors recorded on check_obj.pandera.errors instead of raising SchemaErrors"
    - "When _concat_failure_cases() is invoked with a list whose first item is a native PySpark DataFrame, it returns a concatenated PySpark DataFrame produced by .union(), not a polars-only pl.concat() crash"
    - "When ColumnBackend.check_dtype() reports a WRONG_DATATYPE mismatch on a PySpark frame, the error message reports the PySpark-native dtype string (e.g., 'IntegerType()', 'LongType()') instead of the narwhals dtype string (e.g., 'Int32', 'Int64')"
    - "DataFrameSchemaBackend.validate() does not eagerly materialize PySpark DataFrames during the check loop; tests in tests/pyspark/test_pyspark_dtypes.py that pass verifySchema=False at frame creation do not trigger STRUCT_ARRAY_LENGTH_MISMATCH during validation"
    - "Running `PANDERA_USE_NARWHALS_BACKEND=True nox -s \"tests_narwhals_backend-3.11(extra='polars')\"` and the ibis equivalent still exit 0 (no regression on existing narwhals coverage)"
    - "Native PySpark behavior is unchanged: running `pytest tests/pyspark/ -k 'spark and not spark_connect'` without PANDERA_USE_NARWHALS_BACKEND=True still passes for every previously-passing test"
    - "ROADMAP Phase 2 SC2c (row-index in failure_cases) is either covered by a new pyspark xfail-marked test OR the SC2c clause is explicitly scoped out of pyspark via a documented ROADMAP edit, with the decision recorded in TRIAGE.md and 02-04-SUMMARY.md"
  artifacts:
    - path: "pandera/backends/narwhals/container.py"
      provides: "PySpark-aware DataFrameSchemaBackend.validate() that sets check_obj.pandera.errors for PySpark frames"
      contains: "_is_sql_lazy"
    - path: "pandera/backends/narwhals/base.py"
      provides: "PySpark-aware _concat_failure_cases() that dispatches to .union() for PySpark frames and lazy-only run_check() path for PySpark"
      contains: "_concat_failure_cases"
    - path: "pandera/backends/narwhals/components.py"
      provides: "ColumnBackend.check_dtype() that emits PySpark-native dtype strings on mismatch for PySpark frames"
      contains: "check_dtype"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 2 SC2c clarification (either kept with new xfail target or scoped out of pyspark)"
      contains: "row-index in"
  key_links:
    - from: "pandera/backends/narwhals/container.py::DataFrameSchemaBackend.validate"
      to: "check_obj.pandera (PanderaDataFrameAccessor)"
      via: "check_obj.pandera.errors = error_dicts; return check_obj — PySpark branch only"
      pattern: "\\.pandera\\.errors\\s*="
    - from: "pandera/backends/narwhals/base.py::_concat_failure_cases"
      to: "PySpark DataFrame.union()"
      via: "type(first).__module__.startswith('pyspark') branch"
      pattern: "\\.union\\("
    - from: "pandera/backends/narwhals/components.py::ColumnBackend.check_dtype"
      to: "native PySpark dataType string via nw.to_native(...).schema"
      via: "PySpark branch reads native dtype repr for the mismatch message"
      pattern: "schema\\[column\\]"
    - from: "tests/pyspark/test_pyspark_check.py + test_pyspark_dtypes.py + test_pyspark_error.py + test_pyspark_container.py + test_pyspark_model.py + test_pyspark_accessor.py"
      to: "df_out.pandera.errors"
      via: "validation completes without raising SchemaErrors"
      pattern: "PASSED|XFAIL"
---

<objective>
Close the four narwhals-backend backend bugs documented in `.planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md` under Category C, and resolve the SC2c "row-index in failure_cases" applicability question recorded in `02-VERIFICATION.md`. After this plan ships, ROADMAP Phase 2 SC1 and SC3 must be satisfied: running `tests/pyspark/` under `PANDERA_USE_NARWHALS_BACKEND=True` produces only PASSED and XFAIL outcomes.

Purpose: Plan 02-03 emitted a `PHASE SPLIT RECOMMENDED` signal because all 4 Category C entries shared the same architectural root cause (narwhals was designed for polars/ibis whose API contracts differ from native pyspark) and exceeded the per-run cap. This plan is that follow-on — TEST-01 and TEST-03 are blocked until it completes.

Output:
- `pandera/backends/narwhals/container.py` — `DataFrameSchemaBackend.validate()` branches on SQL-lazy implementation to either raise `SchemaErrors` (polars/ibis path, unchanged) or set `check_obj.pandera.errors` and return the native PySpark DataFrame (PySpark branch).
- `pandera/backends/narwhals/base.py` — `_concat_failure_cases()` detects PySpark DataFrames in the input list and uses `.union()`. `run_check()` (and `_materialize` call sites that affect the check loop for PySpark) no longer eagerly collect PySpark frames during validation.
- `pandera/backends/narwhals/components.py` — `ColumnBackend.check_dtype()` reports the native PySpark dtype string in the WRONG_DATATYPE message when the input frame is PySpark.
- `.planning/ROADMAP.md` — Phase 2 SC2c clause is either kept (with a new xfail-marked pyspark test created elsewhere) or scoped out of pyspark with a documented rationale.
- `.planning/phases/02-test-coverage-and-ci/02-04-SUMMARY.md` — records every fix, the SC2c decision, and the regression-check results.
</objective>

<execution_context>
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-test-coverage-and-ci/02-CONTEXT.md
@.planning/phases/02-test-coverage-and-ci/02-RESEARCH.md
@.planning/phases/02-test-coverage-and-ci/02-PATTERNS.md
@.planning/phases/02-test-coverage-and-ci/02-VERIFICATION.md
@.planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md
@.planning/phases/02-test-coverage-and-ci/02-03-SUMMARY.md

<interfaces>
<!-- Native PySpark backend reference — the polars/ibis narwhals container raises SchemaErrors, -->
<!-- but native pyspark sets .pandera.errors. The PySpark branch in narwhals MUST mirror this. -->

From `pandera/backends/pyspark/container.py` lines 146-151 (native PySpark contract):
- Native PySpark `DataFrameSchemaBackend.validate()` ends with:
  - `error_dicts = error_handler.summarize(schema_name=schema.name)` when `error_handler.collected_errors`
  - `check_obj.pandera.errors = error_dicts`
  - `return check_obj`
- It NEVER raises `SchemaErrors` for PySpark inputs — error_dicts on the accessor IS the contract.

From `pandera/backends/narwhals/container.py` lines 219-233 (current narwhals contract):
- Narwhals `DataFrameSchemaBackend.validate()` ends with `raise SchemaErrors(...)` for all backends.
- This is correct for polars/ibis but wrong for PySpark.

From `pandera/api/narwhals/utils.py` lines 32-40:
- `_is_sql_lazy(frame)` returns True for `nw.Implementation.PYSPARK` / `PYSPARK_CONNECT` / `IBIS` / `DUCKDB` / `SQLFRAME`.
- Use a tighter PySpark-only check (not _is_sql_lazy) for the validate() branch, because ibis should keep raising SchemaErrors.

PySpark Implementation predicate (recommended new helper or inline check):
- `frame.implementation in (nw.Implementation.PYSPARK, nw.Implementation.PYSPARK_CONNECT)` on a `nw.LazyFrame`/`nw.DataFrame`
- OR detect on the native frame: `type(native).__module__.startswith("pyspark")` when the LazyFrame is not available

PanderaDataFrameAccessor (target of `check_obj.pandera.errors = ...`):
- Registered in `pandera/accessors/pyspark_sql_accessor.py` via `register_dataframe_accessor("pandera")(PanderaDataFrameAccessor)` at line 154
- Native pyspark DataFrame gains `.pandera.errors` attribute via this accessor — already wired by Phase 1

<!-- Failure-case concat dispatch — the existing pattern dispatches on hasattr(first, "union"). -->
<!-- PySpark DataFrame DOES have a .union() method, but it expects a PySpark DataFrame argument. -->
<!-- The current code path is: -->

From `pandera/backends/narwhals/base.py` lines 41-53 (current _concat_failure_cases):
- Branches on `hasattr(first, "union")` → ibis path
- Else: `pl.concat(items)` → crashes when items contain PySpark DataFrames

Problem: PySpark DataFrames also have `.union()`, but the first item may be a polars DataFrame (built by `_build_scalar_failure_case`) while another item is a PySpark DataFrame (built by `_build_lazy_failure_case`). The hasattr branch is brittle. Fix: type-dispatch on whether ANY item in `items` is a PySpark DataFrame, and if so, normalize ALL items to PySpark and use `.union()`. (Alternative: build pyspark-native failure_case frames in `_build_lazy_failure_case` for pyspark inputs, then the `.union()` dispatch via hasattr works correctly.)

<!-- Dtype string mismatch — current code emits str(nw_dtype) which is the narwhals representation -->

From `pandera/backends/narwhals/components.py` lines 240-273 (current check_dtype):
- Loop produces `f"got {nw_dtype}"` and `failure_cases=str(nw_dtype)` — both use the narwhals dtype string.
- For PySpark, the native dtype string lives at `nw.to_native(check_obj).schema[column].dataType` (PySpark `T.StructField.dataType.simpleString()` or `repr()`).

Native PySpark dtype representation (from `pandera/backends/pyspark/column.py` line 195-196):
- `Engine.dtype(check_obj.schema[schema.name].dataType)` returns a pandera Engine dtype whose `str()` is the PySpark-native form (e.g., 'IntegerType()').
- For the narwhals path: equivalent native access is `nw.to_native(check_obj).schema[column].dataType` after stripping LazyFrame wrap — for a `pyspark.sql.DataFrame`, `.schema[column]` returns a `StructField` whose `.dataType` repr is the form tests expect.

<!-- Materialization on PySpark — _materialize() collects via .execute() for SQL-lazy or .collect() for LazyFrame -->

From `pandera/api/narwhals/utils.py` lines 43-57 (current _materialize):
- nw.LazyFrame → .collect()
- nw.DataFrame + _is_sql_lazy → nw.to_native(frame).execute() — this is the path PySpark currently takes.

`pyspark.sql.DataFrame` does NOT have `.execute()`. The current code likely fails fast for PySpark, OR narwhals has a different conversion path that triggers `spark.createDataFrame(...).collect()` indirectly during the check loop in `base.py` line 124 (`passed_lf = check_result.check_passed; passed = bool(_materialize(passed_lf)[CHECK_OUTPUT_KEY][0])`). This collection forces schema verification — with `verifySchema=False` in test fixtures the actual data doesn't match the declared schema, which Spark only discovers on materialization.

Fix approach: For PySpark frames, in `run_check`, avoid `_materialize(passed_lf)` for the pass/fail scalar — instead extract the scalar through PySpark-native APIs (`.collect()[0][CHECK_OUTPUT_KEY]` is still a collect, but it's a bounded 1-row collect on the AGGREGATED check result, not the full input frame). Critically, the AGGREGATED scalar is already a 1-row frame; the issue is only when the dtype-mismatched ROW data is included. Confirm by reading checks.py to see what `check_result.check_passed` actually looks like for PySpark — it should be a single-row aggregation. The test failures indicate the source frame itself is what gets materialized somewhere. Trace from `DataFrameSchemaBackend.validate` → component validate → check_dtype, and identify the unnecessary collect.

<!-- ROADMAP SC2c row-index decision -->

From `.planning/ROADMAP.md` Phase 2 SC2 line 80:
- "Element-wise checks, `sample=`/`tail=` params, and row-index in `failure_cases` are each covered by at least one `xfail`-marked test documenting the SQL-lazy limitation"

PySpark distributed-system reality: PySpark DataFrames do not have integer row indices (no `with_row_index` equivalent that produces a stable ordering across executors). The polars/ibis xfail target for row-index doesn't translate to pyspark.

Two valid resolutions (this plan picks ONE at the checkpoint):
- (a) **Scope out of pyspark.** Edit ROADMAP SC2 to read: "Element-wise checks and `sample=`/`tail=` params are each covered by at least one `xfail`-marked test documenting the SQL-lazy limitation. Row-index in `failure_cases` is inapplicable to PySpark (no row-index semantics in distributed Spark DataFrames)." Update TRIAGE.md and 02-04-SUMMARY.md to record this decision.
- (b) **Add xfail.** Identify or write a pyspark test that asserts the presence of an integer row index in failure_cases, then xfail-mark it with the canonical pattern: `condition=CONFIG.use_narwhals_backend, reason="Row-index in failure_cases is not available for PySpark — distributed DataFrames have no native row index", strict=True`.

Default recommendation in the checkpoint: (a). The user makes the final call at the human-verify gate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix DataFrameSchemaBackend.validate() to set check_obj.pandera.errors for PySpark frames (Category C Entry 1 — affects ~290 tests)</name>
  <files>pandera/backends/narwhals/container.py</files>
  <read_first>
    - pandera/backends/narwhals/container.py (full file — touching lines 84-233; understand the existing polars/ibis flow before branching)
    - pandera/backends/pyspark/container.py lines 100-152 (native PySpark contract: how check_obj.pandera.errors is built and returned — this is the behavior to mirror in the PySpark branch)
    - pandera/api/narwhals/utils.py lines 10-40 (_is_sql_lazy, _SQL_LAZY_IMPLEMENTATIONS — use Implementation enum, not attribute probing)
    - pandera/api/narwhals/error_handler.py (ErrorHandler.summarize signature — what schema_name kw it takes and what shape it returns)
    - .planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md Category C Entry 1 (lines 143-165) for the exact failure trace
    - tests/pyspark/test_pyspark_error.py and test_pyspark_check.py (sample failing tests — read how `df_out.pandera.errors` is consumed: the assertions check dict keys like "DATA"/"SCHEMA"/"DATAFRAME_CHECK" structure)
  </read_first>
  <action>
Branch `DataFrameSchemaBackend.validate()` in `pandera/backends/narwhals/container.py` to mirror the native PySpark contract when (and only when) the input is a PySpark DataFrame. For polars/ibis the existing `raise SchemaErrors(...)` path stays exactly as is.

Concrete target state:

1. **Detect PySpark inputs.** After `check_lf = _to_lazy_nw(check_obj)` (line 100), compute `is_pyspark = check_lf.implementation in (nw.Implementation.PYSPARK, nw.Implementation.PYSPARK_CONNECT)`. This must use `nw.Implementation`, not a `_SQL_LAZY_IMPLEMENTATIONS` membership check, because ibis must remain on the `raise SchemaErrors` path.

2. **Replace the final error-emission block.** The existing block at lines 219-231 currently has two arms (`drop_invalid_rows` then `raise SchemaErrors`). Replace it with three arms:
   - `drop_invalid_rows` arm — UNCHANGED.
   - `is_pyspark and error_handler.collected_errors` arm — build `error_dicts` by calling `error_handler.summarize(schema_name=schema.name)`, defaultdict-flatten the result via the existing `defaultdict_to_dict` helper pattern from `base.py::failure_cases_metadata` (lines 246-249) — extract that helper to a module-level function in `base.py` and import it here, OR inline the recursion (prefer the extraction so both call sites use the same code). Then set `check_obj.pandera.errors = error_dicts` on the ORIGINAL native `check_obj` (not on `check_lf` — the accessor lives on the native frame), and `return check_obj`.
   - Polars/Ibis arm — UNCHANGED `raise SchemaErrors(schema=schema, schema_errors=error_handler.schema_errors, data=_to_frame_kind_nw(check_lf, return_type))`.

3. **No-error PySpark path.** When `is_pyspark` is True and `error_handler.collected_errors` is empty, set `check_obj.pandera.errors = {}` on the native `check_obj` before returning. The native PySpark backend does this unconditionally at line 150 of `pandera/backends/pyspark/container.py`. Mirror that.

4. **Preserve the `failure_cases` SchemaError unwrapping logic** at lines 186-217 — that block builds SchemaError instances and collects them in `error_handler`. It runs for all backends (PySpark needs the errors collected just as polars/ibis do). Do NOT skip it for PySpark; the PySpark branch reads from `error_handler` after this block runs.

5. **Preserve native return type.** Native PySpark `check_obj` is a `pyspark.sql.DataFrame`. The PySpark branch must return THIS object (not the narwhals-wrapped or collected version). The native object already has the `.pandera` accessor from Phase 1's `register_pyspark_backends()`.

Do NOT modify the polars/ibis happy-path return at line 233 (`return _to_frame_kind_nw(check_lf, return_type)`) — it's only the error-emission branch that splits.

Do NOT introduce new top-level imports beyond what's already in the file plus `narwhals.stable.v1 as nw` (already imported) and the existing `_is_sql_lazy` helper. If `defaultdict_to_dict` is extracted to `base.py`, import it via `from pandera.backends.narwhals.base import defaultdict_to_dict`.

Reference D-07: this PySpark branch applies to vanilla `pyspark.sql.DataFrame` (Implementation.PYSPARK). The `PYSPARK_CONNECT` case is included for symmetry but is not exercised in CI (no Spark Connect server).

After the change, run the targeted failing test from TRIAGE.md to confirm the SchemaErrors→pandera.errors flip:
```
JAVA_HOME=<your-jdk-17-path> PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/test_pyspark_check.py::TestEqualToCheck::test_equal_to_check -x -v
```
Expected: PASSED (not FAILED with pandera.errors.SchemaErrors).
  </action>
  <verify>
    <automated>cd /Users/deepyaman/github/unionai-oss/pandera && python -c "import ast; ast.parse(open('pandera/backends/narwhals/container.py').read())" && grep -n "check_obj.pandera.errors" pandera/backends/narwhals/container.py</automated>
  </verify>
  <acceptance_criteria>
    - `pandera/backends/narwhals/container.py` parses as valid Python: `python -c "import ast; ast.parse(open('pandera/backends/narwhals/container.py').read())"` exits 0
    - The string `check_obj.pandera.errors` appears at least once in `pandera/backends/narwhals/container.py` (verified by `grep -c "check_obj.pandera.errors" pandera/backends/narwhals/container.py` ≥ 1)
    - The string `Implementation.PYSPARK` appears at least once in `pandera/backends/narwhals/container.py` (verified by `grep -c "Implementation.PYSPARK" pandera/backends/narwhals/container.py` ≥ 1)
    - The existing `raise SchemaErrors(` call is still present in container.py for the polars/ibis branch (verified by `grep -c "raise SchemaErrors(" pandera/backends/narwhals/container.py` ≥ 1)
    - Running `PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/test_pyspark_check.py::TestEqualToCheck::test_equal_to_check -x -v --tb=short` (with JAVA_HOME set) produces at least one PASSED outcome (not all FAILED with `pandera.errors.SchemaErrors`)
    - No regression for polars: running `pytest tests/polars/test_polars_container.py -x` exits 0
    - No regression for ibis: running `pytest tests/ibis/test_ibis_container.py -x` exits 0
  </acceptance_criteria>
  <done>DataFrameSchemaBackend.validate() in container.py branches on Implementation.PYSPARK / PYSPARK_CONNECT to set check_obj.pandera.errors and return the native frame; polars/ibis still raise SchemaErrors. Sampling test from TestEqualToCheck shows PASSED. No polars/ibis regression.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix _concat_failure_cases() PySpark dispatch and avoid PySpark materialization in run_check() (Category C Entries 2 & 4)</name>
  <files>pandera/backends/narwhals/base.py</files>
  <read_first>
    - pandera/backends/narwhals/base.py (full file — touching `_concat_failure_cases` at lines 41-53 and `run_check` at lines 113-183)
    - .planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md Category C Entry 2 (lines 167-178) and Entry 4 (lines 217-227) for failure traces — Entry 2 is `_df` AttributeError from `pl.concat(items)`, Entry 4 is `STRUCT_ARRAY_LENGTH_MISMATCH` from `_materialize(passed_lf)` on PySpark frames
    - pandera/api/narwhals/utils.py lines 43-57 (`_materialize` — does NOT handle pyspark.sql.DataFrame, which lacks `.execute()`)
    - tests/pyspark/test_pyspark_dtypes.py lines 25-67 (BaseClass.validate_datatype — passes verifySchema=False with mismatched data; Spark only discovers the schema mismatch on materialization, which the narwhals backend triggers but the native pyspark backend never does)
    - tests/pyspark/conftest.py lines 85-89 (the `spark_df` helper sets verifySchema=False — clarifies why dtype tests must NOT materialize during validation)
  </read_first>
  <action>
Make two related changes in `pandera/backends/narwhals/base.py`. Both fix problems that arise only for PySpark inputs; polars/ibis paths must be unaffected.

**Change A — `_concat_failure_cases()` PySpark dispatch (Category C Entry 2):**

Concrete target state for `_concat_failure_cases(items)` at lines 41-53:

1. If `items` is empty: return `pl.DataFrame()` (UNCHANGED — polars path).
2. If ANY item in `items` is a native PySpark DataFrame (detected via `type(item).__module__.startswith("pyspark")` on either the first item or any item — prefer checking ALL items to handle the mixed scalar/lazy case): take the PySpark dispatch path.
   - PySpark dispatch: normalize all items to PySpark DataFrames. Polars-native `pl.DataFrame` items (built by `_build_scalar_failure_case`) must be converted to PySpark DataFrames via the active `SparkSession` (`pyspark.sql.SparkSession.getActiveSession()`) and `spark.createDataFrame(...)`. Then `functools.reduce(lambda a, b: a.union(b), pyspark_items)`.
   - If `SparkSession.getActiveSession()` returns None: skip the polars-to-pyspark conversion for scalar items by building scalar items in pyspark form from the SchemaError directly (refactor `_build_scalar_failure_case` to optionally accept a `target_backend="pyspark"` hint that returns a single-row PySpark DataFrame). Alternative simpler fix: when the failure-case collection contains a mix of PySpark and polars, the polars items represent metadata-only failures (no data rows) that can be safely dropped from the concat — pick the PySpark-only subset and union those. Document the chosen path in the docstring.
3. Else (ibis frames): UNCHANGED `functools.reduce(lambda a, b: a.union(b), items)`.
4. Else: UNCHANGED `pl.concat(items)`.

Add a docstring section explaining the PySpark branch and why mixed-backend item lists arise.

Place the PySpark detection branch BEFORE the existing `hasattr(first, "union")` ibis check, because PySpark DataFrames also satisfy `hasattr(first, "union")` and the current code would mis-dispatch them to the ibis path with a polars argument.

**Change B — Avoid PySpark materialization in `run_check()` (Category C Entry 4):**

Concrete target state for `run_check()` at lines 113-183:

1. The line `passed = bool(_materialize(passed_lf)[CHECK_OUTPUT_KEY][0])` at line 124 must NOT eagerly materialize a PySpark frame through narwhals' `_materialize()` (which doesn't know how to handle pyspark.sql.DataFrame and either crashes or triggers Spark's verifySchema=False mismatch discovery).
2. Branch on `_is_sql_lazy(passed_lf) and passed_lf.implementation in (nw.Implementation.PYSPARK, nw.Implementation.PYSPARK_CONNECT)`. For PySpark frames:
   - Convert to native: `native_passed = nw.to_native(passed_lf)`
   - Collect with PySpark's bounded path: `row = native_passed.first()` returns a Spark Row with the aggregated boolean. (`.first()` on an aggregated single-row PySpark DataFrame is bounded — it does NOT pull the full original input frame; the aggregation already projected to a single row before this point.)
   - Extract: `passed = bool(row[CHECK_OUTPUT_KEY])`
3. For polars/ibis (the default branch): UNCHANGED `passed = bool(_materialize(passed_lf)[CHECK_OUTPUT_KEY][0])`.
4. The downstream `failure_cases` logic at lines 129-165 stays the same — it operates on `nw.LazyFrame`/`nw.DataFrame` wrappers and does not eagerly materialize the source data (per existing comments at lines 119-120).

If the failing test `tests/pyspark/test_pyspark_dtypes.py::TestAllNumericTypes::test_pyspark_all_int_types` STILL fails after the run_check fix with STRUCT_ARRAY_LENGTH_MISMATCH, the materialization is happening elsewhere. Likely candidates:
- `pandera/backends/narwhals/container.py::check_column_values_are_unique` line 584: `nw.to_native(_materialize(dup_rows))` — operates on a bounded `dup_rows` (only duplicate keys), should be safe; verify.
- `pandera/backends/narwhals/components.py::check_unique` line 195: `nw.to_native(_materialize(dup_values))` — same pattern; verify.
- `pandera/backends/narwhals/container.py::check_column_presence` line 542: `_to_native(check_obj.head())` — for a column-not-in-dataframe error message; this calls `.head()` which IS bounded but `_to_native` then renders the frame in an f-string → may trigger collection. Audit and gate behind `if not _is_sql_lazy(check_obj):` for the head display, or replace with a column-name list to avoid materialization for PySpark frames.

For any additional materialization point discovered: add the same PySpark guard pattern (`_is_sql_lazy and Implementation.PYSPARK*`) and use a PySpark-safe equivalent (no `_materialize`, no full collect on the source frame).

After both changes, run the targeted tests:
```
PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/test_pyspark_error.py::test_pyspark_check_eq -x -v
PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/test_pyspark_dtypes.py::TestAllNumericTypes::test_pyspark_all_int_types -x -v
```
Expected: both PASSED (no `_df` AttributeError, no `STRUCT_ARRAY_LENGTH_MISMATCH`).
  </action>
  <verify>
    <automated>cd /Users/deepyaman/github/unionai-oss/pandera && python -c "import ast; ast.parse(open('pandera/backends/narwhals/base.py').read())" && grep -c "pyspark" pandera/backends/narwhals/base.py</automated>
  </verify>
  <acceptance_criteria>
    - `pandera/backends/narwhals/base.py` parses as valid Python: `python -c "import ast; ast.parse(open('pandera/backends/narwhals/base.py').read())"` exits 0
    - The substring `pyspark` (case-insensitive) appears at least once in `_concat_failure_cases` and at least once in `run_check`, verified by `grep -ic "pyspark" pandera/backends/narwhals/base.py` ≥ 2
    - Running `PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/test_pyspark_error.py::test_pyspark_check_eq -x -v --tb=short` produces PASSED (with JAVA_HOME set). No `PySparkAttributeError` and no `_df` AttributeError in the traceback.
    - Running `PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/test_pyspark_dtypes.py::TestAllNumericTypes::test_pyspark_all_int_types -x -v --tb=short` produces PASSED (with JAVA_HOME set). No `STRUCT_ARRAY_LENGTH_MISMATCH` IllegalArgumentException in the traceback.
    - Polars regression check: `pytest tests/polars/test_polars_components.py tests/polars/test_polars_container.py -x` exits 0
    - Ibis regression check: `pytest tests/ibis/test_ibis_components.py tests/ibis/test_ibis_container.py -x` exits 0
    - The hasattr ibis path is preserved: `grep -c 'hasattr(first, "union")' pandera/backends/narwhals/base.py` ≥ 1
  </acceptance_criteria>
  <done>_concat_failure_cases() detects PySpark items and dispatches before the hasattr-ibis branch; run_check() uses PySpark-native .first() for the scalar pass/fail on PySpark frames and avoids _materialize() for PySpark inputs. Targeted tests from Category C Entries 2 & 4 PASS. Polars/ibis tests unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix dtype string format mismatch in ColumnBackend.check_dtype() (Category C Entry 3)</name>
  <files>pandera/backends/narwhals/components.py</files>
  <read_first>
    - pandera/backends/narwhals/components.py lines 219-275 (`check_dtype` — currently emits `f"got {nw_dtype}"` and `failure_cases=str(nw_dtype)`)
    - pandera/backends/pyspark/column.py lines 184-209 (native pyspark `check_dtype` — uses `Engine.dtype(check_obj.schema[schema.name].dataType)` for the dtype string; this is the form tests like `test_pyspark_dataframeschema` and `test_dataframe_schema_unique` assert against, e.g. `"expected column 'age' to have type IntegerType(), got IntegerType()"`)
    - .planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md Category C Entry 3 (lines 180-215) for the failure trace — narwhals reports "got Int64" / "got Int32" but tests expect "got LongType()" / "got IntegerType()"
    - tests/pyspark/test_pyspark_container.py (the `test_pyspark_dataframeschema` test compares error_dicts strings against pyspark-native dtype repr; read the assertion to confirm exact expected format)
    - tests/pyspark/test_pyspark_model.py (`test_dataframe_schema_unique` — same dtype assertion pattern)
    - pandera/api/narwhals/utils.py (`_is_sql_lazy`, `_SQL_LAZY_IMPLEMENTATIONS`, `nw.Implementation.PYSPARK`)
  </read_first>
  <action>
Modify `ColumnBackend.check_dtype()` in `pandera/backends/narwhals/components.py` (currently at lines 219-275) so that the WRONG_DATATYPE error message and `failure_cases` use the PySpark-native dtype string when the input frame is a PySpark DataFrame.

Concrete target state:

1. **Detect PySpark inputs** at the top of the per-column loop (currently `for column, nw_dtype in zip(schema_obj.names(), schema_obj.dtypes()):` at line 242). Before the loop, compute `is_pyspark = isinstance(check_obj, (nw.DataFrame, nw.LazyFrame)) and check_obj.implementation in (nw.Implementation.PYSPARK, nw.Implementation.PYSPARK_CONNECT)`.

2. **Compute the display dtype** inside the loop. Today: `nw_dtype` (a narwhals dtype) is used directly in the f-string and as `failure_cases`. For PySpark, replace with the native dtype repr:
   - Access the native PySpark schema: `native_schema = nw.to_native(check_obj).schema` — for `pyspark.sql.DataFrame`, `.schema` is a `T.StructType`, and `.schema[column].dataType` returns a `T.DataType` subclass (e.g., `IntegerType()`, `LongType()`, `StringType()`).
   - The display string is `str(native_schema[column].dataType)` for non-parameterized types (renders as `IntegerType()`) — match what the native pyspark backend emits via `Engine.dtype(...).__str__()`. Confirm by running native pyspark side-by-side and comparing the string.
   - Use this display dtype as both the message `got {display_dtype}` and the `failure_cases=str(display_dtype)`.
3. **Polars/ibis path UNCHANGED.** When `is_pyspark` is False, keep `f"got {nw_dtype}"` and `failure_cases=str(nw_dtype) if not passed else None`.

4. **`check` field UNCHANGED.** The `check=f"dtype('{schema.dtype}')"` field on `CoreCheckResult` uses `schema.dtype` (the expected dtype), not the actual dtype — already in the correct native form for tests because pyspark schemas are constructed with pyspark types. Do not modify.

5. **Type comparison logic UNCHANGED.** Lines 243-259 (the `try/except TypeError` around `Engine.dtype()` and `schema_nw_dtype.check(...)`) are about WHETHER the dtypes match. The fix only affects the DISPLAY string in the failure message. Do not touch the comparison logic — only the f-string and the `failure_cases=` value.

After the change, verify the targeted failing test:
```
PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/test_pyspark_container.py::test_pyspark_dataframeschema -x -v --tb=long
```
Expected: PASSED. The error message in `df_out.pandera.errors["SCHEMA"]["WRONG_DATATYPE"]` for a mismatched column reports a string like `"expected column 'age' to have type IntegerType(), got LongType()"` — not `"got Int64"`.

Note: Some Category C Entry 3 failures may also depend on Task 1's `check_obj.pandera.errors` fix (they currently raise SchemaErrors instead of accessor errors). After Task 1 + Task 3 are both done, re-verify Entry 3 tests.
  </action>
  <verify>
    <automated>cd /Users/deepyaman/github/unionai-oss/pandera && python -c "import ast; ast.parse(open('pandera/backends/narwhals/components.py').read())" && grep -c "Implementation.PYSPARK" pandera/backends/narwhals/components.py</automated>
  </verify>
  <acceptance_criteria>
    - `pandera/backends/narwhals/components.py` parses as valid Python
    - `grep -c "Implementation.PYSPARK" pandera/backends/narwhals/components.py` ≥ 1
    - The existing polars/ibis path is preserved: `grep -c "got {nw_dtype}" pandera/backends/narwhals/components.py` ≥ 1 (the unchanged f-string for the non-PySpark branch)
    - Running `PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/test_pyspark_container.py::test_pyspark_dataframeschema -x -v --tb=short` (after Tasks 1 + 2 land) produces PASSED, and the error message in any captured failure case for a mismatched dtype contains a PySpark-native dtype string ("IntegerType()", "LongType()", etc.) rather than a narwhals dtype string ("Int32", "Int64")
    - Running `PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/test_pyspark_model.py::test_dataframe_schema_unique -x -v --tb=short` produces PASSED for all parametrizations
    - Polars regression check: `pytest tests/polars/test_polars_components.py -x -k dtype` exits 0
    - Ibis regression check: `pytest tests/ibis/test_ibis_components.py -x -k dtype` exits 0
  </acceptance_criteria>
  <done>ColumnBackend.check_dtype() emits PySpark-native dtype strings (IntegerType(), LongType(), etc.) in WRONG_DATATYPE messages and failure_cases for PySpark frames; polars/ibis unchanged. Targeted tests from Entry 3 PASS.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: Decide SC2c — row-index in failure_cases applicability to PySpark</name>
  <decision>How should ROADMAP Phase 2 SC2 treat "row-index in failure_cases" for PySpark? PySpark DataFrames have no native integer row index (distributed, partitioned). The SC2 clause came from polars/ibis context.</decision>
  <context>
    From `02-VERIFICATION.md` "Human Verification Required" (lines 183-189) and the gap entry under SC2c: PySpark tests have no assertions on integer row indices in failure_cases. Two valid resolutions exist; the developer picks one. The chosen path is recorded in `.planning/ROADMAP.md`, `.planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md`, and `02-04-SUMMARY.md`.
  </context>
  <options>
    <option id="scope-out">
      <name>Scope out of PySpark (recommended)</name>
      <pros>Honest reflection of the distributed-system reality. No fake test that asserts something that cannot be true under any future narwhals version. Aligns ROADMAP SC2 with what PySpark can actually do.</pros>
      <cons>Edits ROADMAP wording, which is a roadmap-level change rather than a test-level change. Requires updating TRIAGE.md to note the decision.</cons>
    </option>
    <option id="add-xfail">
      <name>Add an xfail-marked PySpark test asserting absence of row-index</name>
      <pros>Keeps SC2 wording unchanged. Provides an explicit test artifact that documents the limitation in code rather than only in ROADMAP.</pros>
      <cons>The test would be `xfail(strict=True)` from day one — it would never XPASS because PySpark cannot grow row indices. The strict gate (D-04) would never fire. The test exists only to satisfy a roadmap clause and adds noise to the suite.</cons>
    </option>
  </options>
  <resume-signal>Select: scope-out OR add-xfail OR a description of an alternative approach. The selected option determines the followup action in Task 5.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Apply the SC2c decision from Task 4 (ROADMAP edit OR new xfail test)</name>
  <files>.planning/ROADMAP.md, .planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md, tests/pyspark/test_pyspark_error.py</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 2 SC2 currently at line 81)
    - .planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md (add a "## SC2c Decision" section at the end)
    - Resume signal from Task 4 (which option was selected)
    - tests/pyspark/test_pyspark_error.py (canonical location for a new pyspark error-shape test if Task 4 selected `add-xfail` — confirm imports include `pytest`, `pandera.config.CONFIG`)
  </read_first>
  <action>
Apply the decision recorded in Task 4. Exactly ONE of the following two branches runs:

**Branch A — Task 4 returned `scope-out`:**

1. Edit `.planning/ROADMAP.md` Phase 2 SC2 (currently `"Element-wise checks, `sample=`/`tail=` params, and row-index in `failure_cases` are each covered by at least one `xfail`-marked test documenting the SQL-lazy limitation"` at line 81).
   - New wording: `"Element-wise checks and `sample=`/`tail=` params are each covered by at least one `xfail`-marked test documenting the SQL-lazy limitation. Row-index in `failure_cases` is inapplicable to PySpark — PySpark DataFrames are distributed and partitioned with no native integer row index; this clause applies only to polars/ibis backends."`
   - Do not change any other SC lines.
2. Append to `.planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md` a new section `## SC2c Decision` with:
   - Decision: `scope-out`
   - Date: today (2026-05-11)
   - Rationale: brief paragraph echoing the PySpark distributed-system reasoning
   - Reference: link to 02-VERIFICATION.md gap entry
3. Do NOT modify any test file. Do NOT add an xfail.

**Branch B — Task 4 returned `add-xfail`:**

1. Add a new test to `tests/pyspark/test_pyspark_error.py` named `test_failure_cases_have_no_row_index`:
   - Asserts that for any validation failure produced by a pyspark schema under the narwhals backend, the failure cases (as exposed via `df_out.pandera.errors`) do NOT contain integer row indices.
   - Marked with the canonical pattern:
     ```
     @pytest.mark.xfail(
         condition=CONFIG.use_narwhals_backend,
         reason="Row-index in failure_cases is not available for PySpark — distributed DataFrames have no native row index",
         strict=True,
     )
     ```
   - Import `CONFIG` from `pandera.config` if not already imported in the file.
   - Test body: build a small `DataFrameSchema` that will fail, call `.validate(df, lazy=True)`, inspect `df_out.pandera.errors` for any nested dict containing the string key `"index"`. If found, fail the test (this would be the XPASS case — narwhals unexpectedly added row indices). Under the current narwhals backend this assertion holds (no index key present), so the xfail will fire as expected XFAIL.
2. Append to `.planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md` a new section `## SC2c Decision` with:
   - Decision: `add-xfail`
   - Date: today (2026-05-11)
   - Test added: `tests/pyspark/test_pyspark_error.py::test_failure_cases_have_no_row_index`
3. Do NOT edit `.planning/ROADMAP.md`.

**Branch C — Task 4 returned a different option:** record the option text verbatim in TRIAGE.md `## SC2c Decision`, raise a `BLOCKED — alternative SC2c resolution requires planning revisit` flag in the task summary, and stop Task 5 work. Do not edit ROADMAP or tests until the alternative is planned out.

Whichever branch runs, the final state must include exactly one new `## SC2c Decision` section in TRIAGE.md.
  </action>
  <verify>
    <automated>cd /Users/deepyaman/github/unionai-oss/pandera && grep -c "^## SC2c Decision" .planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md` contains exactly one `## SC2c Decision` heading: `grep -c "^## SC2c Decision" .planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md` returns 1
    - The decision section records one of: `scope-out`, `add-xfail`, or `BLOCKED — alternative SC2c resolution requires planning revisit`
    - If Branch A: `.planning/ROADMAP.md` line for Phase 2 SC2 no longer contains the substring "row-index in `failure_cases`" as a required xfail clause (verified by `grep "row-index in.*failure_cases.*xfail" .planning/ROADMAP.md` returning zero matches for the Phase 2 SC2 line); the rationale text "inapplicable to PySpark" appears in the file
    - If Branch B: `tests/pyspark/test_pyspark_error.py` contains a `def test_failure_cases_have_no_row_index` function with a `@pytest.mark.xfail` decorator using `condition=CONFIG.use_narwhals_backend` and `strict=True`; running `pytest tests/pyspark/test_pyspark_error.py::test_failure_cases_have_no_row_index -v` exits 0 (XFAIL under narwhals OR PASSED without narwhals)
    - If Branch C: TRIAGE.md `## SC2c Decision` section text includes the literal string `BLOCKED` and no ROADMAP or test edit is made
  </acceptance_criteria>
  <done>SC2c decision recorded in TRIAGE.md; ROADMAP and/or tests/pyspark/ updated according to the selected option; no edits made for non-selected branches.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Human verification — full tests/pyspark/ suite clean and no regressions</name>
  <what-built>
    - Task 1: PySpark-aware DataFrameSchemaBackend.validate() in pandera/backends/narwhals/container.py
    - Task 2: PySpark dispatch in _concat_failure_cases() and PySpark-safe scalar extraction in run_check() in pandera/backends/narwhals/base.py
    - Task 3: PySpark-native dtype strings in ColumnBackend.check_dtype() in pandera/backends/narwhals/components.py
    - Task 5: SC2c decision applied (ROADMAP edit or new xfail test, per Task 4 selection)
  </what-built>
  <how-to-verify>
    Run each command below and confirm the stated expected outcome. JAVA_HOME must be set (use the path from the user's local Java 17 install — see `user_setup` in this plan's frontmatter).

    1. **Targeted Category C tests pass (one representative test per root-cause entry):**
       ```bash
       PANDERA_USE_NARWHALS_BACKEND=True pytest \
         tests/pyspark/test_pyspark_check.py::TestEqualToCheck::test_equal_to_check \
         tests/pyspark/test_pyspark_error.py::test_pyspark_check_eq \
         tests/pyspark/test_pyspark_dtypes.py::TestAllNumericTypes::test_pyspark_all_int_types \
         tests/pyspark/test_pyspark_container.py::test_pyspark_dataframeschema \
         -v --tb=short
       ```
       Expected: every test PASSED. No FAILED, no `pandera.errors.SchemaErrors`, no `_df` AttributeError, no `STRUCT_ARRAY_LENGTH_MISMATCH`, no `"got Int64"` / `"got Int32"` strings in any error message.

    2. **Full pyspark suite clean under narwhals (the SC1 goal):**
       ```bash
       PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/ \
         --ignore=tests/pyspark/test_schemas_on_pyspark_pandas.py \
         -k "spark and not spark_connect" \
         -v --tb=short
       ```
       Expected outcome summary: only PASSED and XFAIL outcomes. No FAILED. No XPASS. The previous Plan 02-03 run produced 300 FAILED; this run must produce 0 FAILED.

    3. **Native PySpark (no narwhals flag) still passes — no regression:**
       ```bash
       pytest tests/pyspark/ \
         --ignore=tests/pyspark/test_schemas_on_pyspark_pandas.py \
         -k "spark and not spark_connect" \
         -v --tb=short
       ```
       Expected: every previously-passing test still PASSED. (Native pyspark behavior must be unchanged by this plan.)

    4. **Polars and ibis narwhals coverage did not regress:**
       ```bash
       nox -s "tests_narwhals_backend-3.11(extra='polars')"
       nox -s "tests_narwhals_backend-3.11(extra='ibis')"
       ```
       Expected: both exit 0.

    5. **TRIAGE.md Category C entries are annotated as fixed:**
       ```bash
       grep -A 2 "^### Category C Entry" .planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md
       ```
       Manually confirm each of Entries 1, 2, 3, 4 has been updated from `[DEFERRED — exceeded per-run cap]` to a fix description with a commit reference or `fixed in plan 02-04`.

    6. **SC2c decision is recorded:**
       ```bash
       grep -A 5 "^## SC2c Decision" .planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md
       ```
       Expected: one decision section showing either `scope-out` (with ROADMAP edit) or `add-xfail` (with test file reference).

    7. **No code uses os.getenv for the narwhals condition (D-03):**
       ```bash
       grep -rE "condition=os\.getenv" tests/pyspark/ pandera/backends/narwhals/
       ```
       Expected: no output.
  </how-to-verify>
  <resume-signal>Type "approved" if all seven checks pass, or describe the failing check and stop.</resume-signal>
</task>

</tasks>

<verification>
Final post-checkpoint validation (also runs during Task 6 verification):

```bash
# 1. All four narwhals backend modules parse cleanly
python -c "import ast; [ast.parse(open(f).read()) for f in ['pandera/backends/narwhals/container.py','pandera/backends/narwhals/base.py','pandera/backends/narwhals/components.py']]"

# 2. PySpark-native error contract present
grep -c "check_obj.pandera.errors" pandera/backends/narwhals/container.py  # >= 1

# 3. PySpark dispatch present in failure-case concat
grep -c "pyspark" pandera/backends/narwhals/base.py  # >= 2 (one for concat, one for run_check)

# 4. PySpark-native dtype string present in components.py
grep -c "Implementation.PYSPARK" pandera/backends/narwhals/components.py  # >= 1

# 5. SC2c decision recorded
grep -c "^## SC2c Decision" .planning/phases/02-test-coverage-and-ci/02-03-TRIAGE.md  # == 1

# 6. D-03 / D-04 compliance unchanged
grep -rE "condition=os\.getenv" tests/pyspark/  # no output
```
</verification>

<success_criteria>
- All four Category C root-cause bugs in `02-03-TRIAGE.md` are fixed in `pandera/backends/narwhals/`. The TRIAGE.md Category C section is updated to show each entry as `fixed in plan 02-04` with a commit reference.
- Running `PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/ -k "spark and not spark_connect" --ignore=tests/pyspark/test_schemas_on_pyspark_pandas.py` produces only PASSED and XFAIL outcomes (zero FAILED). This satisfies ROADMAP Phase 2 SC1.
- Every Category C bug fix preserves polars/ibis narwhals coverage (`tests_narwhals_backend-3.11(extra='polars')` and `(extra='ibis')` still pass) AND preserves native PySpark behavior (`pytest tests/pyspark/` without `PANDERA_USE_NARWHALS_BACKEND=True` is unchanged). This satisfies SC3.
- The SC2c row-index question is resolved: either ROADMAP Phase 2 SC2 is edited to scope row-index out of pyspark with a documented rationale, OR a new xfail-marked test is added. The decision is recorded in TRIAGE.md `## SC2c Decision`.
- No xfail in `tests/pyspark/` uses `os.getenv` (D-03 unchanged).
- Every xfail in `tests/pyspark/` referencing `use_narwhals_backend` uses `strict=True` (D-04 unchanged).
- `02-04-SUMMARY.md` is created under the phase directory.
- After this plan ships, re-running the verifier on Phase 2 should change SC1 and SC3 status from FAILED to VERIFIED.
</success_criteria>

<output>
After completion, create `.planning/phases/02-test-coverage-and-ci/02-04-SUMMARY.md`.

The summary MUST include:
- Per-task outcome (file diffs, test verdicts before/after)
- The SC2c decision text (scope-out or add-xfail)
- Confirmation that polars+ibis narwhals sessions did not regress
- Confirmation that native pyspark behavior did not regress
- Any unexpected materialization point discovered during Task 2 (Entry 4 may require fixes beyond the two anticipated sites — record them all)
- The final pytest summary line for the SC1 verification command (count of passed / xfailed / failed)
</output>
