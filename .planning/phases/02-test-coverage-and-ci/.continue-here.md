---
context: phase
phase: 02-test-coverage-and-ci
task: 6
total_tasks: 6
status: in_progress
last_updated: 2026-05-12T03:44:03.137Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Do NOT add `if is_pyspark:` backend-specific branches in narwhals backend code — user explicitly rejected this pattern. Accept limitations or fix tests/fixtures instead.
- [ ] CONSTRAINT: `verifySchema=False` removal from conftest was too broad — some fixtures (float data for DecimalType) genuinely need it. Do NOT remove it globally; address specific test cases individually.
- [ ] CONSTRAINT: Dtype alias mismatch — `str(schema.dtype)` for `Column("int")` is `"int"` but `str(IntegerType())` is `"IntegerType()"`. String equality comparison in check_dtype() fails for aliases.
- [ ] CONSTRAINT: Error message format differs between narwhals and native PySpark backend — narwhals produces `"Check '<Check ...>' failed."`, native produces `"<Schema Column(...)> failed validation ..."`. Tests asserting exact messages need xfail.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Backend-specific if-else in narwhals | Adding `if is_pyspark:` detection in narwhals backend to work around narwhals abstraction gaps | blocking | Accept limitation (xfail tests) or fix upstream; do NOT add backend detection branches |
| Global verifySchema removal | Removing `verifySchema=False` from conftest globally breaks fixtures with genuine type mismatches (float→Decimal, etc.) | blocking | Only remove from specific fixtures where data provably matches declared schema |
| Wrong Bug B diagnosis | Original handoff said Bug B was _concat_failure_cases dropping scalar polars items. Actual root cause for test_pyspark_check_eq is error categorization (SCHEMA_COMPONENT_CHECK vs DATAFRAME_CHECK) | advisory | Read current container.py and base.py before assuming the original handoff analysis was correct |

<current_state>
Plan 02-04, mid-Task 6 (human verification). Tasks 1-5 committed (c6f3bb22, 3ea211f6). Three new uncommitted fixes: (1) base.py _concat_failure_cases fallback for empty pyspark_items, (2) container.py run_schema_component_checks uses err.reason_code instead of SCHEMA_COMPONENT_CHECK, (3) components.py check_dtype() PySpark branch with string comparison (still failing on type aliases). Full suite still has 11 targeted failures.
</current_state>

<completed_work>

- Task 1: DataFrameSchemaBackend.validate() PySpark contract (commit c6f3bb22) ✓
- Task 2: _concat_failure_cases PySpark dispatch + run_check() PySpark scalar (commit c6f3bb22) ✓
- Task 3: check_dtype() PySpark comparison — UNCOMMITTED, partially correct (passes for exact type names, fails for string aliases)
- Task 4: SC2c checkpoint decision — scope-out (commit 3ea211f6) ✓
- Task 5: ROADMAP and TRIAGE.md updated (commit 3ea211f6) ✓
- Task 6 PARTIAL: run_schema_component_checks reason_code fix (UNCOMMITTED — container.py)
- Task 6 PARTIAL: _concat_failure_cases fallback (UNCOMMITTED — base.py)
</completed_work>

<remaining_work>

**Fix 1 — dtype alias mismatch (test_pyspark_all_int_types, test_pyspark_dataframeschema):**
In `components.py check_dtype()` PySpark branch, `str(schema.dtype)` for `Column("int")` is `"int"` but `str(pyspark_col_dtype)` is `"IntegerType()"`. Options:
- Use PySpark's `_parse_datatype_string(str(schema.dtype))` to normalize string aliases to DataType, then compare
- Or use `schema.dtype.check(pandera_pyspark_dtype)` if the pandera PySpark type system can handle it
- Or xfail the alias test cases for narwhals backend

**Fix 2 — error message format (test_pyspark_check_eq):**
Test expects `"<Schema Column(...)> failed validation str_startswith('B')"`, narwhals produces `"Check '<Check ...>' failed."`. Options:
- Xfail test_pyspark_check_eq for narwhals backend with reason
- Or change narwhals message format (complex, affects all backends)

**Fix 3 — TypeError vs PysparkSchemaError (TestEqualToCheck::test_failed_unaccepted_datatypes):**
Test expects `TypeError` (native backend raises it for ArrayType/MapType with equal_to), narwhals backend catches AnalysisException and raises PysparkSchemaError instead.
- Xfail ArrayType and MapType cases for narwhals backend
- TestEqualToCheck.pytest_generate_tests needs updating to support marks (see TestUniqueValuesEqCheck for pattern)

**After all fixes:**
- Run full suite: `PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/ -k "spark and not spark_connect" --ignore=tests/pyspark/test_schemas_on_pyspark_pandas.py`
- Commit all uncommitted files
- Write 02-04-SUMMARY.md
</remaining_work>

<decisions_made>

- Do NOT add backend-specific if/is_pyspark branches — accept limitations or xfail tests
- verifySchema=False NOT removed globally — only safe for fixtures with exact type matches
- run_schema_component_checks: use err.reason_code instead of SCHEMA_COMPONENT_CHECK (DATA vs SCHEMA categorization)
- _concat_failure_cases: fall back to pl.concat(items) when pyspark_items is empty (scalar-only failures)
- SC2c: scope out row-index clause for PySpark
- check_dtype() for PySpark: str equality comparison (uncommitted, failing on aliases)
</decisions_made>

<blockers>
- Dtype alias mismatch: "int" != "IntegerType()" — need alias resolution in check_dtype()
- Error message format: narwhals vs native PySpark produce different strings
- TestEqualToCheck xfail: needs pytest_generate_tests to support marks first
</blockers>

## Required Reading (in order)
1. `pandera/backends/narwhals/components.py` — check_dtype() PySpark branch (~line 241-273), check_nullable() (~line 110-164)
2. `pandera/backends/narwhals/base.py` — _concat_failure_cases (lines 41-72), run_check() PySpark branch (lines 143-154)
3. `pandera/backends/narwhals/container.py` — run_schema_component_checks (lines 287-330), PySpark validate() return path (lines 224-248)
4. `tests/pyspark/test_pyspark_check.py` lines 326-338 (TestEqualToCheck.pytest_generate_tests), lines 1958-1973 (TestUniqueValuesEqCheck.pytest_generate_tests with marks support)

## Infrastructure State
- JAVA_HOME: /Users/deepyaman/Library/Caches/rattler/cache/pkgs/openjdk-17.0.17-h99a4030_0/lib/jvm
- Run tests directly: `JAVA_HOME=... PANDERA_USE_NARWHALS_BACKEND=True python -m pytest tests/pyspark/ -k "spark and not spark_connect" --ignore=tests/pyspark/test_schemas_on_pyspark_pandas.py`
- Do NOT use act/nox — stale pandas incompatibility in nox env

<context>
Session hit context limit mid-Task 6. Three logical sub-problems remain: (1) dtype alias normalization for PySpark type strings, (2) xfailing error-message-sensitive tests, (3) xfailing tests expecting TypeError for unsupported types. Each is small (~5-15 lines). The run_schema_component_checks reason_code fix and _concat_failure_cases fallback are uncommitted but correct. The components.py check_dtype() is also uncommitted and partially correct — needs alias resolution added.
</context>

<next_action>
Start with: Fix dtype alias in components.py check_dtype() PySpark branch. Try `nw.to_native(check_obj).dtypes[column_name]` for the PySpark DataType, then compare with `DataType._parse_datatype_string(str(schema.dtype))` from pyspark.sql.types if available. Alternatively, check if `pa.pyspark.DataType(schema.dtype)` can normalize the alias. Run test_pyspark_all_int_types after fix to verify.
</next_action>
