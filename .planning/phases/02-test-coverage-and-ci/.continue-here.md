---
context: phase
phase: 02-test-coverage-and-ci
task: 6
total_tasks: 6
status: in_progress
last_updated: 2026-05-17T19:38:27.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Do NOT add `if is_pyspark:` backend-specific branches in narwhals backend code — user explicitly rejected this pattern. Accept limitations or fix tests/fixtures instead.
- [ ] CONSTRAINT: Error message format differs between narwhals and native PySpark backend — narwhals produces `"Check '<Check ...>' failed."`, native produces `"<Schema Column(...)> failed validation ..."`. Tests asserting exact messages need backend-conditional expected values (NOT xfail — narwhals format is correct behavior).
- [ ] CONSTRAINT: `@register_input_datatypes` type restrictions are PySpark-native-only. The narwhals backend is intentionally more permissive (aligned with Ibis). Do not try to replicate the decorator in narwhals backend code.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Backend-specific if-else in narwhals | Adding `if is_pyspark:` detection in narwhals backend to work around narwhals abstraction gaps | blocking | Accept limitation (xfail tests) or fix upstream; do NOT add backend detection branches |
| xfail for wrong-format messages | Using xfail when narwhals produces a different (but correct) error message format | advisory | Use `CONFIG.use_narwhals_backend` conditional in test to select expected string |

<current_state>
Plan 02-04, Task 6. Tasks 1-5 and 6e-marks done. Remaining: 6c (error message format), 6d (coerce xfail), 6f (model/accessor/regex/decorator failures), 6g (full suite + SUMMARY.md). ~25 failures remain.
</current_state>

<completed_work>

- Task 1: DataFrameSchemaBackend.validate() PySpark contract (commit c6f3bb22) ✓
- Task 2: _concat_failure_cases PySpark dispatch + run_check() PySpark scalar (commit c6f3bb22) ✓
- Task 3: check_dtype() fix — empty DF projection, dtype comparison via narwhals engine (commit 8976a697) ✓
- Task 4: SC2c checkpoint decision — scope-out (commit 3ea211f6) ✓
- Task 5: ROADMAP and TRIAGE.md updated (commit 3ea211f6) ✓
- Task 6e-marks: xfail test_failed_unaccepted_datatypes for 5 classes — module-level `_xfail_narwhals_type_restriction` mark, 48 xfailed clean (commit 8705e58f) ✓
</completed_work>

<remaining_work>

**Task 6c — error message format (test_pyspark_error.py, test_pyspark_container.py, test_pyspark_fields.py):**
Tests assert `"<Schema Column(...)> failed validation X"` strings. Narwhals produces `"Check '<Check ...>' failed."`. Fix: add `CONFIG.use_narwhals_backend` conditional to select which expected string to assert. Pattern already established in test_pyspark_check.py and test_pyspark_config.py.

**Task 6d — coerce xfail (test_pyspark_container.py::test_pyspark_dataframeschema):**
Second assertion fails: `Column(IntegerType(), coerce=True)` expects string→int coercion. Narwhals `ColumnBackend.validate()` has no coerce_dtype step. Xfail with `CONFIG.use_narwhals_backend`.

**Task 6f — investigate remaining failures:**
- `test_pyspark_model.py::test_dataframe_schema_unique_wrong_column`
- `test_pyspark_model.py::test_dataframe_schema_strict`
- `test_pyspark_model.py::test_registered_dataframemodel_checks`
- `test_pyspark_container.py::test_pyspark_regex_column`
- `test_pyspark_decorators.py::TestPanderaDecorators::test_cache_dataframe_settings[False-True-None-None]`
- `test_pyspark_decorators.py::TestPanderaDecorators::test_cache_dataframe_settings[False-False-None-None]`
Each needs individual diagnosis before fix/xfail decision.

**Task 6g:** Run full suite, commit all fixes, write 02-04-SUMMARY.md.
</remaining_work>

<decisions_made>

- Do NOT add backend-specific if/is_pyspark branches — accept limitations or xfail tests
- Error message format: backend-conditional expected values (not xfail) — narwhals format IS correct
- @register_input_datatypes is PySpark-native-only; narwhals backend intentionally more permissive (Ibis has no equivalent). Backlog item 999.1 added to relax native backend eventually.
- _xfail_narwhals_type_restriction: module-level mark before TestGreaterThanEqualToCheck class; shared across 5 classes' get_data_param() — DRY
- SC2c: scope out row-index in failure_cases for PySpark
</decisions_made>

<blockers>
- Error message format: ~5 tests assert exact native PySpark error strings that differ from narwhals format.
</blockers>

## Required Reading (in order)
1. `tests/pyspark/test_pyspark_check.py` lines 692-703 — `_xfail_narwhals_type_restriction` module-level mark definition
2. `tests/pyspark/test_pyspark_config.py` — established pattern for CONFIG.use_narwhals_backend conditional expected values

## Infrastructure State
- JAVA_HOME: `/Users/deepyaman/Library/Caches/rattler/cache/pkgs/openjdk-17.0.17-h99a4030_0/lib/jvm`
- Run tests: `JAVA_HOME=<path> PANDERA_USE_NARWHALS_BACKEND=True python -m pytest tests/pyspark/ --ignore=tests/pyspark/test_schemas_on_pyspark_pandas.py -k 'spark and not spark_connect' --no-header -q`
- Do NOT use act/nox — stale pandas incompatibility in nox env

<context>
6e-marks complete and clean. Next is 6c: find tests asserting native PySpark error message format and add backend-conditional expected strings. Then 6d (coerce xfail) and 6f (diagnose model/decorator failures). 6g closes the plan.
</context>

<next_action>
Start with task 6c: `grep -n "failed validation\|Schema Column" tests/pyspark/test_pyspark_error.py tests/pyspark/test_pyspark_container.py tests/pyspark/test_pyspark_fields.py` to find exact strings needing conditionals, then run those files under narwhals to confirm failures before editing.
</next_action>
