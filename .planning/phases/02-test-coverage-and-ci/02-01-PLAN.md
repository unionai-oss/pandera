---
phase: 02-test-coverage-and-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/pyspark/test_pyspark_config.py
  - tests/pyspark/test_pyspark_check.py
  - tests/pyspark/test_pyspark_container.py
  - tests/pyspark/test_pyspark_decorators.py
autonomous: true
requirements: [TEST-02]
must_haves:
  truths:
    - "All 5 TestPanderaConfig methods in test_pyspark_config.py are xfail-marked when narwhals backend is active"
    - "test_pyspark_sample in test_pyspark_container.py is xfail-marked for the sample= SQL-lazy limitation"
    - "TestCustomCheck.test_extension and test_extension_dataframe_model in test_pyspark_check.py are xfail-marked"
    - "TestUniqueValuesEqCheck.test_unique_values_eq_check and test_failed_unaccepted_datatypes are xfail-marked"
    - "TestPanderaDecorators.test_cache_dataframe_settings in test_pyspark_decorators.py is xfail-marked"
    - "Every xfail marker uses condition=CONFIG.use_narwhals_backend, strict=True, and a specific reason string"
    - "Each modified test file imports CONFIG from pandera.config"
    - "D-05: element-wise checks, sample=/tail= params, and row-index failures are each covered by at least one xfail-marked test"
    - "D-06: test_pyspark_config.py xfails follow test_polars_config.py pattern"
  artifacts:
    - path: "tests/pyspark/test_pyspark_config.py"
      provides: "5 xfail markers on TestPanderaConfig methods"
      contains: "CONFIG, ValidationDepth"
    - path: "tests/pyspark/test_pyspark_check.py"
      provides: "4 xfail markers (2 TestCustomCheck + 2 TestUniqueValuesEqCheck)"
      contains: "from pandera.config import CONFIG"
    - path: "tests/pyspark/test_pyspark_container.py"
      provides: "1 xfail marker on test_pyspark_sample"
      contains: "CONFIG, PanderaConfig, ValidationDepth"
    - path: "tests/pyspark/test_pyspark_decorators.py"
      provides: "1 xfail marker on test_cache_dataframe_settings"
      contains: "from pandera.config import CONFIG, config_context"
  key_links:
    - from: "tests/pyspark/test_pyspark_config.py"
      to: "pandera.config.CONFIG"
      via: "xfail marker condition"
      pattern: "condition=CONFIG\\.use_narwhals_backend"
    - from: "tests/pyspark/test_pyspark_check.py"
      to: "pandera.config.CONFIG"
      via: "xfail marker condition"
      pattern: "condition=CONFIG\\.use_narwhals_backend"
    - from: "tests/pyspark/test_pyspark_container.py"
      to: "pandera.config.CONFIG"
      via: "xfail marker condition"
      pattern: "condition=CONFIG\\.use_narwhals_backend"
    - from: "tests/pyspark/test_pyspark_decorators.py"
      to: "pandera.config.CONFIG"
      via: "xfail marker condition"
      pattern: "condition=CONFIG\\.use_narwhals_backend"
---

<objective>
Apply `@pytest.mark.xfail(condition=CONFIG.use_narwhals_backend, strict=True, reason=...)` markers to all 11 test sites identified by RESEARCH.md as expected SQL-lazy limitations under the Narwhals backend. Each modified file gains the `CONFIG` import alongside its existing `pandera.config` import.

Purpose: Per D-03 / D-04 / D-05, expected narwhals SQL-lazy limitations must be `xfail`-marked using the canonical `CONFIG.use_narwhals_backend` condition with `strict=True`, so that (a) the test run is clean under `PANDERA_USE_NARWHALS_BACKEND=True`, and (b) CI catches if narwhals unexpectedly starts supporting one of these features (the xfail becomes XPASS and the marker must be removed).

Output: Four test files with xfail markers and CONFIG imports applied at the exact lines identified in the patterns map.
</objective>

<execution_context>
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-test-coverage-and-ci/02-CONTEXT.md
@.planning/phases/02-test-coverage-and-ci/02-RESEARCH.md
@.planning/phases/02-test-coverage-and-ci/02-PATTERNS.md

<interfaces>
<!-- Canonical xfail marker pattern (must match exactly) -->

From tests/polars/test_polars_config.py:82-86 — canonical xfail in a config test:
```python
@pytest.mark.xfail(
    condition=CONFIG.use_narwhals_backend,
    reason="<specific reason>",
    strict=True,
)
```

From tests/ibis/test_ibis_check.py:43-47 — canonical xfail in a check test:
```python
@pytest.mark.xfail(
    condition=CONFIG.use_narwhals_backend,
    reason="<specific reason>",
    strict=True,
)
```

Import (must be added to every file that gains xfail markers):
```python
from pandera.config import CONFIG
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Apply xfail markers to test_pyspark_config.py</name>
  <files>tests/pyspark/test_pyspark_config.py</files>
  <read_first>
    - tests/pyspark/test_pyspark_config.py (the file being modified — see current state, including the existing `from pandera.config import ValidationDepth, config_context, get_config_context` import on line 8 and the 5 test method signatures)
    - tests/polars/test_polars_config.py (canonical analog at lines 82-86 — copy this xfail pattern exactly)
    - .planning/phases/02-test-coverage-and-ci/02-PATTERNS.md (see section "tests/pyspark/test_pyspark_config.py" for exact line numbers of each test method)
  </read_first>
  <action>
Modify `tests/pyspark/test_pyspark_config.py` with two changes:

**Change 1 — Update the CONFIG import.** On line 8, replace:
```python
from pandera.config import ValidationDepth, config_context, get_config_context
```
with:
```python
from pandera.config import CONFIG, ValidationDepth, config_context, get_config_context
```

**Change 2 — Add `@pytest.mark.xfail` decorator immediately above each of the 5 test methods in `TestPanderaConfig`.** The decorator must be placed directly above the `def` line (or above any existing `@pytest.mark.parametrize` decorator for `test_cache_dataframe_settings`, making `@pytest.mark.xfail` the OUTERMOST decorator). Use this exact decorator block, replacing nothing else:

```python
    @pytest.mark.xfail(
        condition=CONFIG.use_narwhals_backend,
        reason="Narwhals backend sets use_narwhals_backend=True; config dict assertions hardcode False",
        strict=True,
    )
```

The 5 test methods (in `class TestPanderaConfig`) are:
1. `test_disable_validation` (around line 28)
2. `test_schema_only` (around line 61)
3. `test_data_only` (around line 150)
4. `test_schema_and_data` (around line 232)
5. `test_cache_dataframe_settings` (around line 346 — this has existing `@pytest.mark.parametrize` decorators; place `@pytest.mark.xfail` as the OUTERMOST decorator, above the parametrize decorators)

The indentation of the decorator must match the methods (4 spaces inside the class). Do NOT modify any test body or any other test. Do NOT remove any existing decorators. Per D-03, the condition must be `CONFIG.use_narwhals_backend` — never `os.getenv(...)`. Per D-04, `strict=True` is mandatory.
  </action>
  <verify>
    <automated>cd /Users/deepyaman/github/unionai-oss/pandera && python -c "import ast; tree = ast.parse(open('tests/pyspark/test_pyspark_config.py').read()); print('OK')" && grep -c "condition=CONFIG.use_narwhals_backend" tests/pyspark/test_pyspark_config.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from pandera.config import CONFIG" tests/pyspark/test_pyspark_config.py` returns exactly 1
    - `grep -c "condition=CONFIG.use_narwhals_backend" tests/pyspark/test_pyspark_config.py` returns exactly 5
    - `grep -c "strict=True" tests/pyspark/test_pyspark_config.py` returns at least 5
    - `grep -c "Narwhals backend sets use_narwhals_backend=True" tests/pyspark/test_pyspark_config.py` returns exactly 5
    - `python -c "import ast; ast.parse(open('tests/pyspark/test_pyspark_config.py').read())"` exits 0 (file is syntactically valid Python)
    - `grep -c "os.getenv" tests/pyspark/test_pyspark_config.py` returns 0 (no os.getenv-based xfail conditions introduced — D-03)
  </acceptance_criteria>
  <done>All 5 TestPanderaConfig methods are wrapped with the canonical xfail marker; CONFIG is imported; file parses; no os.getenv used.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply xfail markers to test_pyspark_check.py, test_pyspark_container.py, and test_pyspark_decorators.py</name>
  <files>tests/pyspark/test_pyspark_check.py, tests/pyspark/test_pyspark_container.py, tests/pyspark/test_pyspark_decorators.py</files>
  <read_first>
    - tests/pyspark/test_pyspark_check.py (the file being modified — current imports near line 29, and class/method bodies for TestCustomCheck around lines 1844/1866 and TestUniqueValuesEqCheck around lines 2008/2022)
    - tests/pyspark/test_pyspark_container.py (the file being modified — current `from pandera.config import PanderaConfig, ValidationDepth` import on line 14, and `def test_pyspark_sample` around line 137)
    - tests/pyspark/test_pyspark_decorators.py (the file being modified — current `from pandera.config import config_context` import on line 11, and `def test_cache_dataframe_settings` around line 78)
    - tests/ibis/test_ibis_check.py (canonical analog at lines 43-47 — copy this xfail pattern exactly)
    - .planning/phases/02-test-coverage-and-ci/02-PATTERNS.md (see sections for each of the three files for exact line numbers and reason strings)
  </read_first>
  <action>
Apply xfail markers to three files. Use the exact decorator pattern — `@pytest.mark.xfail` is the OUTERMOST decorator when stacked with `@pytest.mark.parametrize`. Per D-03 use `condition=CONFIG.use_narwhals_backend`. Per D-04 use `strict=True`. Method-level decorators use 4-space indentation (inside class); function-level decorators use 0-space indentation.

**File A: `tests/pyspark/test_pyspark_check.py`** (4 xfail markers, 1 import change)

1. Add the import: insert `from pandera.config import CONFIG` after the existing `from pandera.errors import ...` line (around line 29). Place on its own line.

2. Add `@pytest.mark.xfail` above `TestCustomCheck.test_extension` (around line 1844):
```python
    @pytest.mark.xfail(
        condition=CONFIG.use_narwhals_backend,
        reason="Custom checks using PysparkDataframeColumnObject API are incompatible with narwhals backend (NarwhalsData has different interface)",
        strict=True,
    )
```

3. Add the same `@pytest.mark.xfail` block above `TestCustomCheck.test_extension_dataframe_model` (around line 1866) using the identical reason string.

4. Add `@pytest.mark.xfail` above `TestUniqueValuesEqCheck.test_unique_values_eq_check` (around line 2008) with reason `"unique_values_eq not registered for Narwhals backend"`:
```python
    @pytest.mark.xfail(
        condition=CONFIG.use_narwhals_backend,
        reason="unique_values_eq not registered for Narwhals backend",
        strict=True,
    )
```

5. Add the same xfail block above `TestUniqueValuesEqCheck.test_failed_unaccepted_datatypes` (around line 2022) with the identical reason.

**File B: `tests/pyspark/test_pyspark_container.py`** (1 xfail marker, 1 import update)

1. Update the import on line 14: change
```python
from pandera.config import PanderaConfig, ValidationDepth
```
to
```python
from pandera.config import CONFIG, PanderaConfig, ValidationDepth
```

2. Add `@pytest.mark.xfail` decorator immediately above `def test_pyspark_sample` (around line 137). This is a module-level function, so use 0-space indentation:
```python
@pytest.mark.xfail(
    condition=CONFIG.use_narwhals_backend,
    reason="sample= is not supported in the Narwhals backend; use head= instead",
    strict=True,
)
```

**File C: `tests/pyspark/test_pyspark_decorators.py`** (1 xfail marker, 1 import update)

1. Update the import on line 11: change
```python
from pandera.config import config_context
```
to
```python
from pandera.config import CONFIG, config_context
```

2. Add `@pytest.mark.xfail` above `TestPanderaDecorators.test_cache_dataframe_settings` (around line 78). It is a method, so use 4-space indentation. Place ABOVE any existing `@pytest.mark.parametrize` decorators (xfail must be outermost):
```python
    @pytest.mark.xfail(
        condition=CONFIG.use_narwhals_backend,
        reason="Narwhals backend does not use PySpark caching decorators; cache/unpersist log messages are not emitted",
        strict=True,
    )
```

Do NOT add xfail to `test_cache_dataframe_requirements` (line 25) — RESEARCH.md A1 states this is expected to pass. Triage in Plan 03 will verify.

Do NOT modify any other test or any test body. Do NOT remove any existing decorators.
  </action>
  <verify>
    <automated>cd /Users/deepyaman/github/unionai-oss/pandera && python -c "import ast; [ast.parse(open(f).read()) for f in ['tests/pyspark/test_pyspark_check.py', 'tests/pyspark/test_pyspark_container.py', 'tests/pyspark/test_pyspark_decorators.py']]; print('OK')" && echo "check.py:" && grep -c "condition=CONFIG.use_narwhals_backend" tests/pyspark/test_pyspark_check.py && echo "container.py:" && grep -c "condition=CONFIG.use_narwhals_backend" tests/pyspark/test_pyspark_container.py && echo "decorators.py:" && grep -c "condition=CONFIG.use_narwhals_backend" tests/pyspark/test_pyspark_decorators.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from pandera.config import CONFIG" tests/pyspark/test_pyspark_check.py` returns exactly 1
    - `grep -c "condition=CONFIG.use_narwhals_backend" tests/pyspark/test_pyspark_check.py` returns exactly 4
    - `grep -c "PysparkDataframeColumnObject API are incompatible" tests/pyspark/test_pyspark_check.py` returns exactly 2
    - `grep -c "unique_values_eq not registered for Narwhals backend" tests/pyspark/test_pyspark_check.py` returns exactly 2
    - `grep -c "from pandera.config import CONFIG, PanderaConfig, ValidationDepth" tests/pyspark/test_pyspark_container.py` returns exactly 1
    - `grep -c "condition=CONFIG.use_narwhals_backend" tests/pyspark/test_pyspark_container.py` returns exactly 1
    - `grep -c "sample= is not supported in the Narwhals backend" tests/pyspark/test_pyspark_container.py` returns exactly 1
    - `grep -c "from pandera.config import CONFIG, config_context" tests/pyspark/test_pyspark_decorators.py` returns exactly 1
    - `grep -c "condition=CONFIG.use_narwhals_backend" tests/pyspark/test_pyspark_decorators.py` returns exactly 1
    - `grep -c "cache/unpersist log messages are not emitted" tests/pyspark/test_pyspark_decorators.py` returns exactly 1
    - `python -c "import ast; [ast.parse(open(f).read()) for f in ['tests/pyspark/test_pyspark_check.py', 'tests/pyspark/test_pyspark_container.py', 'tests/pyspark/test_pyspark_decorators.py']]"` exits 0
    - Combined: `grep -c "os.getenv" tests/pyspark/test_pyspark_check.py tests/pyspark/test_pyspark_container.py tests/pyspark/test_pyspark_decorators.py` shows no new os.getenv usage in xfail conditions
  </acceptance_criteria>
  <done>All 6 xfail markers (4 in check.py, 1 in container.py, 1 in decorators.py) are applied; CONFIG is imported in all three files; files parse cleanly.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the xfail markers are syntactically valid and the canonical pattern is used uniformly:

```bash
# Total xfail markers added across all four files (5+4+1+1 = 11)
grep -h "condition=CONFIG.use_narwhals_backend" \
  tests/pyspark/test_pyspark_config.py \
  tests/pyspark/test_pyspark_check.py \
  tests/pyspark/test_pyspark_container.py \
  tests/pyspark/test_pyspark_decorators.py | wc -l
# Expected: 11

# All marked tests use strict=True (no soft xfails)
python -c "
import ast, re
files = ['tests/pyspark/test_pyspark_config.py','tests/pyspark/test_pyspark_check.py','tests/pyspark/test_pyspark_container.py','tests/pyspark/test_pyspark_decorators.py']
for f in files:
    src = open(f).read()
    blocks = re.findall(r'@pytest\.mark\.xfail\([^)]*?CONFIG\.use_narwhals_backend[^)]*?\)', src, re.DOTALL)
    assert all('strict=True' in b for b in blocks), f
print('OK')
"

# All four files import CONFIG
for f in tests/pyspark/test_pyspark_config.py tests/pyspark/test_pyspark_check.py tests/pyspark/test_pyspark_container.py tests/pyspark/test_pyspark_decorators.py; do
  grep -q "from pandera.config import.*CONFIG" "$f" || (echo "MISSING in $f"; exit 1)
done
echo "All imports OK"
```
</verification>

<success_criteria>
- 11 total `@pytest.mark.xfail(condition=CONFIG.use_narwhals_backend, ..., strict=True)` markers across 4 files
- 5 in `test_pyspark_config.py`
- 4 in `test_pyspark_check.py`
- 1 in `test_pyspark_container.py`
- 1 in `test_pyspark_decorators.py`
- Every modified file imports `CONFIG` from `pandera.config`
- Each marker has a specific reason string from the patterns map (no generic placeholders)
- All four files parse as valid Python
- No `os.getenv(...)` is introduced as an xfail condition (D-03 compliance)
- Per D-04, every marker uses `strict=True`
</success_criteria>

<output>
After completion, create `.planning/phases/02-test-coverage-and-ci/02-01-SUMMARY.md`
</output>
</output>
