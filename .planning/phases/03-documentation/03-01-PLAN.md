---
phase: 03-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/source/pyspark_sql.md
  - docs/source/supported_libraries.md
autonomous: true
requirements: [DOCS-01]

must_haves:
  truths:
    - "A reader of docs/source/pyspark_sql.md learns the narwhals backend exists, how to enable it (env var + pip install), and that the public API is unchanged"
    - "A reader of docs/source/supported_libraries.md sees PySpark named as a supported SQL-lazy backend alongside Polars and Ibis"
    - "Both pages state the SQL-lazy limitations explicitly: no element-wise checks, no row sampling"
  artifacts:
    - path: "docs/source/pyspark_sql.md"
      provides: "Narwhals opt-in {note} block after the install code block, before '## What's different?'"
      contains: "PANDERA_USE_NARWHALS_BACKEND"
    - path: "docs/source/supported_libraries.md"
      provides: "PySpark added to narwhals-backends section (top note box, opening paragraph, enabling section, what-it-changes bullets, known gaps)"
      contains: "Pyspark SQL"
  key_links:
    - from: "docs/source/pyspark_sql.md note block"
      to: "narwhals-backends anchor in supported_libraries.md"
      via: "{ref}`Narwhals-powered backend <narwhals-backends>`"
      pattern: "ref.*narwhals-backends"
    - from: "docs/source/supported_libraries.md narwhals section"
      to: "pyspark_sql.md page"
      via: "{ref}`Pyspark SQL <native-pyspark>`"
      pattern: "ref.*native-pyspark"
---

<objective>
Document PySpark as a supported SQL-lazy backend in the narwhals backend
documentation, mirroring how Ibis is documented. Two edits: a `{note}` block
in `docs/source/pyspark_sql.md` and prose updates across the
narwhals-backends content in `docs/source/supported_libraries.md`.

Purpose: Phase 1 wired PySpark into the narwhals backend; Phase 2 added test
coverage. The docs still describe narwhals as Polars+Ibis only. This closes
DOCS-01 so a user can enable PySpark narwhals support and understand its
constraints without reading source code.

Output: Updated `pyspark_sql.md` and `supported_libraries.md`.
</objective>

<execution_context>
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-documentation/03-CONTEXT.md
@.planning/phases/03-documentation/03-RESEARCH.md

# Structural model — the PySpark note MUST mirror this {note} block:
@docs/source/ibis.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add narwhals opt-in {note} block to pyspark_sql.md</name>
  <files>docs/source/pyspark_sql.md</files>
  <read_first>
    - docs/source/pyspark_sql.md — the file being modified; the install code block ends at line 27 (`pip install 'pandera[pyspark]'`), the `## What's different?` heading is at line 29. The note goes between them.
    - docs/source/ibis.md — lines 34-48 are the exact structural model (the narwhals `:::{note}` ... `:::` block). Replicate its structure: opening sentence with `{ref}`Narwhals-powered backend <narwhals-backends>`` link, "It is **opt-in**" sentence with env var + `pandera.config.CONFIG.use_narwhals_backend = True` programmatic alternative, "public API ... unchanged" statement, then a fenced `bash` block with pip install + export lines, closing `:::`.
    - docs/source/supported_libraries.md — lines 214-225 (the "Known gaps" list) for the exact limitation phrasing to mirror: "no element-wise checks" and `sample=` row-sampling wording.
  </read_first>
  <action>
    Insert a MyST `:::{note}` ... `:::` block in docs/source/pyspark_sql.md immediately after the closing fence of the `pip install 'pandera[pyspark]'` code block and immediately before the `## What's different?` heading. Separate it from surrounding content with one blank line on each side (match the spacing pattern used around the ibis.md note).

    The note MUST mirror the structure of the ibis.md narwhals note (D-04) and contain, in this order:
    1. An opt-in framing sentence that references the narwhals backend via `{ref}`Narwhals-powered backend <narwhals-backends>``, adapted for PySpark — say it runs validation against the native PySpark SQL execution path and shares its check implementations with the Polars and Ibis backends. Replace any `pandera.ibis` reference with `pandera.pyspark`.
    2. The opt-in instruction sentence: install the `narwhals` extra and set `PANDERA_USE_NARWHALS_BACKEND=True` (or `pandera.config.CONFIG.use_narwhals_backend = True`) before importing `pandera.pyspark`. State that Pandera uses the native PySpark backend by default and the public API shown on the page is unchanged either way.
    3. An explicit SQL-lazy limitations sentence (D-06, D-07): the narwhals backend for PySpark is SQL-lazy, so it does not support element-wise checks and does not support row sampling (`sample=` / `tail=` params). Use phrasing consistent with the supported_libraries.md "Known gaps" wording. Do NOT soften this by noting the restrictions were already true for native PySpark — state them plainly.
    4. A fenced `bash` code block (D-05) containing exactly two lines: `pip install 'pandera[pyspark,narwhals]' pyspark` and `export PANDERA_USE_NARWHALS_BACKEND=True`.

    Version marker: omit the `*new in 0.XX.0*` marker (the release version is unconfirmed per RESEARCH §Open Questions; omitting is the safe documented choice). Do not invent a version number.

    Do not modify any other part of pyspark_sql.md. Do not add executable `{code-cell}` blocks — the note is prose + a bash fence only.
  </action>
  <verify>
    <automated>grep -q "PANDERA_USE_NARWHALS_BACKEND" docs/source/pyspark_sql.md && grep -q "pandera\[pyspark,narwhals\]" docs/source/pyspark_sql.md && grep -q "narwhals-backends" docs/source/pyspark_sql.md && grep -qi "element-wise" docs/source/pyspark_sql.md && grep -qi "sampling\|sample=" docs/source/pyspark_sql.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `docs/source/pyspark_sql.md` contains a `:::{note}` block positioned after the `pip install 'pandera[pyspark]'` fenced block and before the `## What's different?` heading (note appears between line ~27 and the `## What's different?` line)
    - The note contains the literal `{ref}`Narwhals-powered backend <narwhals-backends>`` cross-reference (grep `ref.*narwhals-backends` matches)
    - The note contains `PANDERA_USE_NARWHALS_BACKEND=True` and `pandera.config.CONFIG.use_narwhals_backend = True`
    - The note references `pandera.pyspark` (not `pandera.ibis`)
    - The note's bash block contains the exact line `pip install 'pandera[pyspark,narwhals]' pyspark` and the exact line `export PANDERA_USE_NARWHALS_BACKEND=True`
    - The note text contains both "element-wise" and a row-sampling reference (`sample=` and/or "row sampling")
    - The note states the public API is unchanged
    - No `*new in` version marker was added to the note
    - `grep -c "## What's different?" docs/source/pyspark_sql.md` returns 1 (heading not duplicated or removed)
  </acceptance_criteria>
  <done>The narwhals opt-in note exists in pyspark_sql.md after the install block, mirrors the ibis.md note structure, uses PySpark-specific commands, and explicitly states the no-element-wise-checks / no-row-sampling limitations.</done>
</task>

<task type="auto">
  <name>Task 2: Add PySpark to narwhals-backends content in supported_libraries.md</name>
  <files>docs/source/supported_libraries.md</files>
  <read_first>
    - docs/source/supported_libraries.md — the file being modified. Relevant regions: top narwhals note box at lines 30-41 ("unifies the Polars and Ibis validation paths"); narwhals-backends opening paragraph at lines 125-133 ("powers both the {ref}`Polars <polars>` and {ref}`Ibis <ibis>` integrations"); "Enabling the Narwhals backend" section at lines 134-139 ("To switch the Polars and Ibis integrations"); "What it changes for you" bullets at lines 183-197; "Known gaps" list at lines 214-225. The PySpark page anchor is `native-pyspark` (referenced elsewhere in this file as `{ref}`Pyspark SQL <native-pyspark>``).
    - docs/source/ibis.md — lines 34-48 for consistent narwhals phrasing/tone.
    - .planning/phases/03-documentation/03-RESEARCH.md — §"Exact Change Points" Locations A/B/C/D and §Pitfall 3 (do NOT edit inside the `lru_cache` cache-clear code block at lines ~162-168).
  </read_first>
  <action>
    Update the narwhals-backends content in docs/source/supported_libraries.md to include PySpark as a SQL-lazy backend alongside Polars and Ibis (D-01, D-02). Make these specific edits; do NOT touch any code block (especially the `register_*.cache_clear()` block) and do NOT edit the DataFrame Library Support list-table near the top of the file:

    Location 0 — Top narwhals note box (lines 30-41): the sentence "unifies the Polars and Ibis validation paths behind a single implementation" must be updated to also name PySpark (e.g. Polars, Ibis, and PySpark SQL validation paths). Keep the rest of that note box intact.

    Location A — Opening paragraph of `## Narwhals-powered backends` (lines 125-133): the phrase "powers both the {ref}`Polars <polars>` and {ref}`Ibis <ibis>` integrations" must be rewritten to enumerate three backends and add `{ref}`Pyspark SQL <native-pyspark>``. Adjust "both" → "the" (or equivalent) so the sentence is grammatical with three integrations. Update the "native Polars and Ibis backends" mention in the same paragraph to include PySpark.

    Location B — "Enabling the Narwhals backend" intro (lines 136-139): "To switch the Polars and Ibis integrations onto the Narwhals-powered backend ... before importing `pandera.polars` or `pandera.ibis`" must add PySpark and `pandera.pyspark` to those enumerations.

    Location C — "What it changes for you" bullets (lines 183-197): update the "Unified checks across Polars and Ibis" bullet heading/body and the "Lazy validation stays lazy" bullet so PySpark SQL is included as a SQL-lazy backend whose built-in checks run as Narwhals expressions, with the same SQL-lazy constraints as Ibis (no element-wise checks, no row sampling). State the PySpark limitations explicitly per D-06/D-07. Either extend existing bullets or add a dedicated PySpark SQL bullet — either is acceptable as long as PySpark is named and its SQL-lazy limitations appear.

    Location D — "Known gaps" list (lines 214-225): add wording that the SQL-lazy gaps (no element-wise checks; `sample=` row sampling not supported) apply to PySpark SQL the same as to Ibis, so the hub page is self-consistent with the new pyspark_sql.md note. This is the discretionary item from CONTEXT.md — implement it for self-consistency.

    Preserve all existing anchors (`narwhals-backends`), `{ref}` links, and MyST directives. Use existing anchors only — do not invent new ones.
  </action>
  <verify>
    <automated>grep -q "native-pyspark" docs/source/supported_libraries.md && grep -ci "pyspark" docs/source/supported_libraries.md | awk '{exit !($1>=3)}' && grep -q "register_polars_backends.cache_clear()" docs/source/supported_libraries.md && grep -q "register_ibis_backends.cache_clear()" docs/source/supported_libraries.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - The top note box (lines ~30-41) text no longer says only "Polars and Ibis validation paths" — it names PySpark (grep for "PySpark" or "Pyspark SQL" within that note region)
    - The narwhals-backends opening paragraph contains the literal `{ref}`Pyspark SQL <native-pyspark>`` cross-reference
    - The "Enabling the Narwhals backend" section text mentions `pandera.pyspark` alongside `pandera.polars` / `pandera.ibis`
    - The "What it changes for you" section names PySpark SQL and states it has no element-wise checks and no row sampling (grep finds "element-wise" and a sampling reference in proximity to PySpark content)
    - The "Known gaps" region references PySpark SQL sharing the SQL-lazy gaps
    - The cache-clear code block is unchanged: `grep -c "register_polars_backends.cache_clear()" docs/source/supported_libraries.md` returns 1 and `grep -c "register_ibis_backends.cache_clear()" docs/source/supported_libraries.md` returns 1 (no accidental edits inside the code block; no `register_pyspark_backends.cache_clear()` added since that is not part of scope)
    - The DataFrame Library Support list-table at the top of the file is unchanged (the existing `{ref}`Pyspark SQL <native-pyspark>`` entry at line 26 already exists; no duplicate added there)
    - `grep -c "(narwhals-backends)=" docs/source/supported_libraries.md` returns 1 (anchor preserved, not duplicated)
  </acceptance_criteria>
  <done>supported_libraries.md names PySpark as a supported SQL-lazy narwhals backend in the top note box, opening paragraph, enabling section, what-it-changes bullets, and known gaps, with explicit SQL-lazy limitations, and no code blocks were modified.</done>
</task>

</tasks>

<verification>
After both tasks:

1. Sphinx/MyST build smoke test (catches broken `{ref}` links and directive syntax):
   `cd docs && python -m sphinx -b html -W --keep-going source _build/html 2>&1 | tail -20`
   (If a full `-W` build is too heavy/flaky in the environment, at minimum run a non-`-W` build of the two pages and confirm no errors reference `pyspark_sql.md` or `supported_libraries.md`.)

2. Cross-reference integrity:
   `grep -n "native-pyspark\|narwhals-backends" docs/source/pyspark_sql.md docs/source/supported_libraries.md`
   Both anchors must resolve to existing targets (`(native-pyspark)=` exists in pyspark_sql.md line 8; `(narwhals-backends)=` exists in supported_libraries.md line 121).

3. Limitation language present in BOTH files (Pitfall 1 guard):
   `grep -li "element-wise" docs/source/pyspark_sql.md docs/source/supported_libraries.md`
   Must list both files.
</verification>

<success_criteria>
- DOCS-01 satisfied: narwhals backend documentation lists PySpark as a supported SQL-lazy backend alongside Polars/Ibis with SQL-lazy limitation notes (no element-wise checks, no row sampling)
- ROADMAP Phase 3 success criteria all TRUE:
  1. The narwhals backend documentation page names PySpark as a supported SQL-lazy backend alongside Ibis/Polars
  2. The same SQL-lazy limitations listed for Ibis (no element-wise checks, no row sampling) are listed for PySpark
  3. A reader of only the narwhals docs can determine how to enable PySpark support (`PANDERA_USE_NARWHALS_BACKEND=True`, `pip install 'pandera[pyspark,narwhals]' pyspark`) and what constraints apply, without consulting source code
- Sphinx build does not introduce new warnings/errors for the two modified files
- No out-of-scope files modified (configuration.md, index.md, ibis.md, polars.md untouched)
</success_criteria>

<output>
After completion, create `.planning/phases/03-documentation/03-01-SUMMARY.md`
</output>
