---
phase: 11-round-4-pr-review-fixes
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - docs/source/pyspark_sql.md
  - docs/source/supported_libraries.md
  - pandera/backends/narwhals/base.py
  - pandera/backends/ibis/container.py
  - tests/ibis/test_ibis_container.py
  - tests/pyspark/conftest.py
autonomous: true
requirements:
  - SE-03
  - NIT-01
  - NIT-02
  - NIT-03
  - NIT-04

must_haves:
  truths:
    - "docs/source/pyspark_sql.md no longer contains the 'df.pandera.errors not SchemaErrors' bullet"
    - "docs/source/pyspark_sql.md install command is `pip install 'pandera[pyspark,narwhals]'` (no trailing `pyspark`)"
    - "docs/source/supported_libraries.md narwhals-backends section references version 0.32.0 (not 0.26.0)"
    - "Proper nouns Narwhals, Ibis, Polars, PySpark are correctly capitalized in all PR-touched comments/docs/tests"
    - "pandera/backends/ibis/container.py has no unnecessary 'The component validate() not raising is the success signal.' comment"
    - "tests/pyspark/conftest.py _cmp_errors docstring is updated to capitalize Narwhals"
  artifacts:
    - path: "docs/source/pyspark_sql.md"
      provides: "PySpark narwhals opt-in note updated for SchemaErrors contract + simplified install"
    - path: "docs/source/supported_libraries.md"
      provides: "Updated version reference 0.32.0"
    - path: "pandera/backends/narwhals/base.py"
      provides: "Comments with proper-noun capitalization for Narwhals/Polars/Ibis/PySpark"
  key_links:
    - from: "docs/source/pyspark_sql.md"
      to: "PR #2339 SE-03 contract change"
      via: "narwhals note documents SchemaErrors behavior"
      pattern: "SchemaErrors"
---

<objective>
Apply the documentation and capitalization nits from PR review #4395365611:

- **SE-03**: Update `docs/source/pyspark_sql.md` narwhals opt-in note to remove the `df.pandera.errors` bullet (now obsolete after Plan 01 makes PySpark Narwhals raise SchemaErrors); simplify the install command (NIT-03 — pyspark is already required by `pandera[pyspark]`).
- **NIT-01**: Capitalize proper nouns (Narwhals, Ibis, Polars, PySpark) in PR-touched comments, docs, and tests.
- **NIT-02**: Update version reference `0.26.0` → `0.32.0` in `docs/source/supported_libraries.md`.
- **NIT-04**: Remove the unnecessary `# The component validate() not raising is the success signal.` comment from `pandera/backends/ibis/container.py`.

Purpose: PR #2339 round-4 review (deepyaman, 2026-05-30) — documentation and consistency cleanup. None of these changes touch production code semantics; they correct user-facing prose, comments, and version references.

Output: Updated docs, comment, and capitalization across the touched files.

Wave 3 (after Plan 02): Plan 02 introduces a new helper in `tests/pyspark/conftest.py`. This plan updates the docstring of the existing `_cmp_errors` helper in the same file, so it must run after Plan 02 to avoid file conflict.
</objective>

<execution_context>
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-round-4-pr-review-fixes/11-01-PLAN.md
@.planning/phases/11-round-4-pr-review-fixes/11-02-PLAN.md
@docs/source/pyspark_sql.md
@docs/source/supported_libraries.md
@pandera/backends/narwhals/base.py
@pandera/backends/ibis/container.py
@tests/ibis/test_ibis_container.py
@tests/pyspark/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/source/pyspark_sql.md narwhals note and install command (SE-03, NIT-01, NIT-03)</name>
  <files>docs/source/pyspark_sql.md</files>
  <read_first>
    - docs/source/pyspark_sql.md lines 29-68 (the entire narwhals opt-in note block)
  </read_first>
  <acceptance_criteria>
    - `grep -Fc "df.pandera.errors" docs/source/pyspark_sql.md` outputs `0` globally — both line-57 and line-59 occurrences are removed; line 78's existing native-backend reference uses the distinct `dataframe.pandera.errors` token (NOT `df.pandera.errors`) and is left intact
    - `grep -c "SchemaErrors" docs/source/pyspark_sql.md` outputs at least `1` (the new bullet describing the unified contract)
    - `grep -Fc "pip install 'pandera[pyspark,narwhals]' pyspark" docs/source/pyspark_sql.md` outputs `0` (the trailing `pyspark` token is gone)
    - `grep -Fc "pip install 'pandera[pyspark,narwhals]'" docs/source/pyspark_sql.md` outputs at least `1`
    - `grep -c "Polars Narwhals" docs/source/pyspark_sql.md` outputs at least `1` (capitalized Narwhals after Polars per review comment on line 48)
    - `grep -nE "polars narwhals|Pyspark|pyspark Narwhals" docs/source/pyspark_sql.md` outputs `0` lines (no lowercase proper-noun violations in the narwhals note paragraphs)
    - The narwhals note retains the coerce=True bullet (still accurate) and the PysparkDataframeColumnObject bullet (still accurate)
    - `pixi run -e dev-3.11-pyspark-narwhals pytest tests/narwhals/ -x -q -k "not docs"` exits 0 (docs change is non-functional but should not regress the test suite)
  </acceptance_criteria>
  <action>
    Edit `docs/source/pyspark_sql.md` lines 29-68 (the narwhals opt-in note block — the `:::{note}` ... `:::` block):

    1. Remove the bullet that begins "**Error reporting uses `df.pandera.errors`, not `SchemaErrors`.**" (currently lines 57-62) entirely. Replace with a new bullet:
       - `**Unified \`SchemaErrors\` contract.** Like the Polars and Ibis Narwhals backends, the PySpark Narwhals backend raises \`pandera.errors.SchemaErrors\` on validation failure (or \`SchemaError\` for the first error when \`lazy=False\`). This differs from the native PySpark backend, which attaches errors to \`df.pandera.errors\`. If you depend on the \`df.pandera.errors\` accessor, use the native PySpark backend (\`PANDERA_USE_NARWHALS_BACKEND=False\`).`
    2. Update the coerce=True bullet (line 48): change "Polars narwhals backend" → "Polars Narwhals backend" (capitalize Narwhals).
    3. Update the install command block (lines 64-67): change `pip install 'pandera[pyspark,narwhals]' pyspark` to `pip install 'pandera[pyspark,narwhals]'` (drop trailing `pyspark`). Keep the `export PANDERA_USE_NARWHALS_BACKEND=True` line.
    4. Scan the entire note block once more for `narwhals` (lowercase) where it appears as a proper noun (not in `pandera[...,narwhals]` extras or in environment variable names) and capitalize to `Narwhals`. Do NOT change `narwhals` inside literal code (extras like `pandera[narwhals]`, env var `PANDERA_USE_NARWHALS_BACKEND`, attribute paths like `narwhals.stable.v1`).
    5. Verify the note block still has correct MyST syntax (the closing `:::` is intact).
    6. CRITICAL: Do NOT touch the prose at line 78 which references `dataframe.pandera.errors` (with the distinct `dataframe.` prefix, not `df.`). That line documents the native PySpark backend contract and remains accurate.
  </action>
  <verify>
    <automated>
      grep -Fc "df.pandera.errors" docs/source/pyspark_sql.md | grep -qx 0 &amp;&amp; \
      grep -c "SchemaErrors" docs/source/pyspark_sql.md | grep -qvx 0 &amp;&amp; \
      grep -Fc "pip install 'pandera[pyspark,narwhals]' pyspark" docs/source/pyspark_sql.md | grep -qx 0 &amp;&amp; \
      grep -Fc "pip install 'pandera[pyspark,narwhals]'" docs/source/pyspark_sql.md | grep -qvx 0 &amp;&amp; \
      grep -Fc "dataframe.pandera.errors" docs/source/pyspark_sql.md | grep -qvx 0
    </automated>
  </verify>
  <done>
    pyspark_sql.md narwhals note documents the SchemaErrors contract; install command simplified; proper nouns capitalized; native-backend section's `dataframe.pandera.errors` reference preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update docs/source/supported_libraries.md version reference (NIT-02)</name>
  <files>docs/source/supported_libraries.md</files>
  <read_first>
    - docs/source/supported_libraries.md lines 30-50 and lines 120-135 (the two `0.26.0` references in the narwhals section)
  </read_first>
  <acceptance_criteria>
    - `grep -c "0.26.0" docs/source/supported_libraries.md` outputs `0` (both occurrences in the narwhals-backends content are updated)
    - `grep -c "0.32.0" docs/source/supported_libraries.md` outputs at least `1`
    - The line at the top of the narwhals note (currently `*new in 0.26.0* &mdash; Pandera ships an optional`) and the prose at line 125 (`As of *0.26.0*, Pandera ships`) both reference `0.32.0`
  </acceptance_criteria>
  <action>
    Edit `docs/source/supported_libraries.md`:

    1. Line 31: `*new in 0.26.0* &mdash; Pandera ships an optional` → `*new in 0.32.0* &mdash; Pandera ships an optional`
    2. Line 125: `As of *0.26.0*, Pandera ships an optional` → `As of *0.32.0*, Pandera ships an optional`
    3. Do not touch any other `0.X.Y` version references elsewhere in the file (they reference prior Pandera releases for unrelated features).
  </action>
  <verify>
    <automated>
      grep -c "0.26.0" docs/source/supported_libraries.md | grep -qx 0 &amp;&amp; \
      grep -c "0.32.0" docs/source/supported_libraries.md
    </automated>
  </verify>
  <done>
    Both narwhals-backend version references updated to 0.32.0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Apply NIT-01 capitalization + NIT-04 comment removal across pandera/backends/narwhals/base.py, pandera/backends/ibis/container.py, tests/ibis/test_ibis_container.py, tests/pyspark/conftest.py</name>
  <files>pandera/backends/narwhals/base.py, pandera/backends/ibis/container.py, tests/ibis/test_ibis_container.py, tests/pyspark/conftest.py</files>
  <read_first>
    - pandera/backends/narwhals/base.py lines 41-129 (the _concat_failure_cases function with its docstring and comments — review comments flag lines 53, 55, 57, 67 for capitalization)
    - pandera/backends/ibis/container.py lines 175-210 (the run_schema_component_checks method with the `# The component validate() not raising is the success signal.` comment at line ~185)
    - tests/ibis/test_ibis_container.py lines 195-210 (the xfail decorator on test_column_absent_error — review flag at line 200 for capitalization)
    - tests/pyspark/conftest.py lines 190-210 (the _cmp_errors helper docstring — review flag at line 197 for capitalization). Note: Plan 02 already added a new `validate_collecting_errors` helper to this file — do not touch that helper here; only update the existing `_cmp_errors` docstring line.
  </read_first>
  <acceptance_criteria>
    - `pandera/backends/narwhals/base.py` `_concat_failure_cases` docstring (lines 42-63 region) reads "For Ibis-backed Narwhals frames" and "For Polars-backed Narwhals LazyFrame:" with Ibis, Polars, and Narwhals capitalized; the surrounding inline comments capitalize Narwhals when used as a proper noun
    - `grep -nE "ibis-backed narwhals|polars-backed narwhals" pandera/backends/narwhals/base.py` outputs `0` lines (lowercase proper-noun usage gone in the touched docstring)
    - `pandera/backends/ibis/container.py` no longer contains the comment `# The component validate() not raising is the success signal.` (`grep -F "The component validate() not raising is the success signal." pandera/backends/ibis/container.py` outputs `0`)
    - `tests/ibis/test_ibis_container.py` line 200 (the xfail reason string) uses the capitalized form "Narwhals" — review suggested keeping it but capitalizing; `grep -F "narwhals backend) vs 'not found' (native ibis backend)" tests/ibis/test_ibis_container.py` outputs `0` (lowercase form removed); `grep -F "Narwhals backend) vs 'not found' (native Ibis backend)" tests/ibis/test_ibis_container.py` outputs at least `1`
    - `tests/pyspark/conftest.py` _cmp_errors docstring reads "Error message format varies by backend (Narwhals vs native PySpark)," — `grep -F "Error message format varies by backend (Narwhals vs native PySpark)" tests/pyspark/conftest.py` outputs `1`; the lowercase form `grep -F "narwhals vs native PySpark" tests/pyspark/conftest.py` outputs `0`
    - `pixi run -e dev-3.11-pyspark-narwhals pytest tests/narwhals/ tests/pyspark/ tests/ibis/ -x -q` exits 0 (capitalization changes do not regress anything)
  </acceptance_criteria>
  <action>
    Edit `pandera/backends/narwhals/base.py` `_concat_failure_cases` docstring and inline comments:

    1. Docstring (lines 42-63):
       - Line 46: `(polars LazyFrame, ibis, PySpark)` → `(Polars LazyFrame, Ibis, PySpark)`
       - Line 48: `(eager polars path)` → `(eager Polars path)`
       - Line 50: `For PySpark-backed narwhals frames` → `For PySpark-backed Narwhals frames` (per review L55)
       - Line 55: `For ibis-backed narwhals frames` → `For Ibis-backed Narwhals frames` (per review L55 directly)
       - Line 57: `For polars-backed narwhals LazyFrame` → `For Polars-backed Narwhals LazyFrame` (per review L57)
    2. Inline comments (lines 67-128):
       - Line 67: `# Separate narwhals-wrapped items from native polars items` → `# Separate Narwhals-wrapped items from native Polars items`
       - Line 84: `# Scalar polars items` → `# Scalar Polars items`
       - Line 96: `"output because scalar polars frames cannot be converted"` (inside the warning message) → `"output because scalar Polars frames cannot be converted"`
       - Line 124: `# SQL-lazy path (ibis, DuckDB, etc.)` → `# SQL-lazy path (Ibis, DuckDB, etc.)`
       - Line 128: `# All-polars path` → `# All-Polars path`
    3. Do NOT modify the actual `pl.DataFrame()`, `pl.concat`, `nw.Implementation.POLARS`, `narwhals.stable.v1` identifiers — they're code, not prose.

    Edit `pandera/backends/ibis/container.py`:

    1. Find the line that reads `# The component validate() not raising is the success signal.` (in `run_schema_component_checks`, around line 185) and delete that comment line entirely. Keep the surrounding code intact.

    Edit `tests/ibis/test_ibis_container.py`:

    1. Line ~200 xfail decorator's `reason="..."` string: replace `"narwhals backend"` with `"Narwhals backend"` and `"native ibis backend"` with `"native Ibis backend"`.

    Edit `tests/pyspark/conftest.py`:

    1. Locate the existing `_cmp_errors` docstring (around line 197 — added in v1.3 Phase 7/8). Replace `"narwhals vs native PySpark"` with `"Narwhals vs native PySpark"`. Do NOT touch the `validate_collecting_errors` helper added by Plan 02 — that helper's own docstring may also need capitalization but is owned by Plan 02; this task only edits the pre-existing `_cmp_errors` docstring.
  </action>
  <verify>
    <automated>
      grep -Fc "The component validate() not raising is the success signal." pandera/backends/ibis/container.py | grep -qx 0 &amp;&amp; \
      grep -Fc "ibis-backed narwhals" pandera/backends/narwhals/base.py | grep -qx 0 &amp;&amp; \
      grep -Fc "polars-backed narwhals" pandera/backends/narwhals/base.py | grep -qx 0 &amp;&amp; \
      grep -Fc "Narwhals backend) vs 'not found' (native Ibis backend)" tests/ibis/test_ibis_container.py | grep -qE '^[1-9]' &amp;&amp; \
      grep -Fc "Error message format varies by backend (Narwhals vs native PySpark)" tests/pyspark/conftest.py | grep -qx 1
    </automated>
  </verify>
  <done>
    Capitalization corrected across 4 files; the unnecessary ibis comment removed.
  </done>
</task>

</tasks>

<verification>
- Capitalization audit: no remaining lowercase proper-noun usage in the touched files for Narwhals/Polars/Ibis/PySpark
- `grep -F "df.pandera.errors" docs/source/pyspark_sql.md` returns 0 hits (line 78's `dataframe.pandera.errors` is preserved as a distinct token)
- `grep -F "0.26.0" docs/source/supported_libraries.md` returns 0 hits
- `grep -F "The component validate() not raising is the success signal." pandera/backends/ibis/container.py` returns 0 hits
- `pixi run -e dev-3.11-pyspark-narwhals pytest tests/narwhals/ tests/pyspark/ tests/ibis/ -x -q` exits 0
</verification>

<success_criteria>
- pyspark_sql.md narwhals note reflects SchemaErrors behavior and simplified install
- supported_libraries.md references 0.32.0
- Proper nouns capitalized; ibis container comment removed
- All tests still pass
</success_criteria>

<output>
Create `.planning/phases/11-round-4-pr-review-fixes/11-03-SUMMARY.md` when done.
</output>
</content>
</invoke>