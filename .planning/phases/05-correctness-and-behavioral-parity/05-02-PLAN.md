---
phase: 05-correctness-and-behavioral-parity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/pyspark/test_pyspark_config.py
autonomous: true
requirements:
  - TEST-FIX-01
tags:
  - pyspark
  - narwhals
  - test-fix
  - config

must_haves:
  truths:
    - "Under PANDERA_USE_NARWHALS_BACKEND=True, all five TestPanderaConfig tests in test_pyspark_config.py pass without xfail decorators (TEST-FIX-01)"
    - "Under PANDERA_USE_NARWHALS_BACKEND unset/False, the same five tests continue to pass (no regression to the native PySpark path)"
    - "Expected config dicts assert against CONFIG.use_narwhals_backend dynamically rather than hardcoded False, preventing future regressions from a silently-dropped key"
  artifacts:
    - path: tests/pyspark/test_pyspark_config.py
      provides: "Five TestPanderaConfig.test_* functions with CORR-band-aid xfail decorators removed and 'use_narwhals_backend' assertions made dynamic"
      contains: "CONFIG.use_narwhals_backend"
  key_links:
    - from: "tests/pyspark/test_pyspark_config.py"
      to: "pandera.config.CONFIG.use_narwhals_backend"
      via: "Each of 5 expected dicts uses CONFIG.use_narwhals_backend instead of hardcoded False"
      pattern: "\"use_narwhals_backend\": CONFIG\\.use_narwhals_backend"
---

<objective>
Remove the five band-aid `@pytest.mark.xfail(condition=CONFIG.use_narwhals_backend, ...)`
decorators from `TestPanderaConfig` in `tests/pyspark/test_pyspark_config.py` by
replacing the hardcoded `"use_narwhals_backend": False` value inside each test's
`expected` dict with `CONFIG.use_narwhals_backend` (dynamic). This is TEST-FIX-01,
the third pre-merge correctness item identified in PR #2339 review.

Purpose: Restore meaningful coverage to the PanderaConfig dict-shape assertions
under the narwhals backend. The current xfails mask the assertion entirely rather
than fixing the shallow root cause (one hardcoded `False` per test). Dynamic
assertion against `CONFIG.use_narwhals_backend` validates the full config shape
under both backend modes, so future config-dataclass regressions are caught.

Output:
- `tests/pyspark/test_pyspark_config.py` with 5 hardcoded `False` values replaced
  by `CONFIG.use_narwhals_backend` and 5 xfail decorator blocks removed.
- All five `TestPanderaConfig.test_*` functions pass under both
  `PANDERA_USE_NARWHALS_BACKEND=True` and unset/False.
</objective>

<execution_context>
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-correctness-and-behavioral-parity/05-RESEARCH.md
@.planning/phases/05-correctness-and-behavioral-parity/05-VALIDATION.md

# Source of truth for CONFIG and config_context
@pandera/config.py

# File being modified
@tests/pyspark/test_pyspark_config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace hardcoded False with CONFIG.use_narwhals_backend in five expected dicts</name>
  <files>tests/pyspark/test_pyspark_config.py</files>
  <read_first>
    - tests/pyspark/test_pyspark_config.py (current state — see lines 8-13 for the existing `CONFIG` import, then the five `TestPanderaConfig` tests: `test_disable_validation` line 38, `test_schema_only` line 76, `test_data_only` line 170, `test_schema_and_data` line 257, `test_cache_dataframe_settings` line 374; hardcoded `"use_narwhals_backend": False` occurs at lines 62, 91, 184, 274, 390; xfail decorators at lines 33-37, 71-75, 165-169, 252-256, 369-373)
    - pandera/config.py (see `CONFIG` module-level object and `_config_from_env_vars()` — confirms `CONFIG.use_narwhals_backend` is the canonical startup-read value; `config_context()` does not expose this parameter, so module-level `CONFIG` is the correct reference)
    - .planning/phases/05-correctness-and-behavioral-parity/05-RESEARCH.md (section "TEST-FIX-01: Five Tests and Their Fix" — table of all 5 line numbers; section "Pitfall 5: `config_context` does not expose `use_narwhals_backend`" — confirms using module-level `CONFIG` is correct)
  </read_first>
  <behavior>
    - In each of the five expected dicts inside `TestPanderaConfig` test methods,
      the line `"use_narwhals_backend": False,` becomes
      `"use_narwhals_backend": CONFIG.use_narwhals_backend,`. The surrounding
      keys (`validation_enabled`, `validation_depth`, `cache_dataframe`,
      `keep_cached_dataframe`, `silenced_warnings`) and their values are
      unchanged. The dict key ordering is preserved.
    - The five `@pytest.mark.xfail(condition=CONFIG.use_narwhals_backend, reason="Narwhals backend sets use_narwhals_backend=True; config dict assertions hardcode False", strict=True)` decorator blocks immediately above each of the five test methods are removed in full. No other test decorators (e.g. `pytestmark`, `parametrize`) are touched.
    - The existing `CONFIG` import at the top of the file (`from pandera.config import CONFIG, ...`) is left in place — it is now also used inside the test bodies via `CONFIG.use_narwhals_backend`.
    - Under `PANDERA_USE_NARWHALS_BACKEND=True`: each `expected` dict has `"use_narwhals_backend": True` (because `CONFIG.use_narwhals_backend` is `True`) and `asdict(get_config_context())` also returns `True`, so the assertion passes.
    - Under `PANDERA_USE_NARWHALS_BACKEND` unset/False: each `expected` dict has `"use_narwhals_backend": False` (because `CONFIG.use_narwhals_backend` is `False`) and `asdict(get_config_context())` also returns `False`, so the assertion still passes — no regression to the native PySpark path.
    - No new imports, no new helpers, no changes to test bodies beyond the single literal-value substitution per test.
  </behavior>
  <action>
    Edit `tests/pyspark/test_pyspark_config.py` to apply the TEST-FIX-01 fix per RESEARCH.md "TEST-FIX-01: Five Tests and Their Fix" and the "Config dict fix pattern" Code Example:

    1. For each of the five test methods (`test_disable_validation`, `test_schema_only`, `test_data_only`, `test_schema_and_data`, `test_cache_dataframe_settings`), find the line `"use_narwhals_backend": False,` inside the `expected = {...}` block and replace it with `"use_narwhals_backend": CONFIG.use_narwhals_backend,`. Per RESEARCH.md the five occurrences are at lines 62, 91, 184, 274, 390 (line numbers may shift after the decorator removals in step 2; rely on the `expected = {` block context, not absolute line numbers).
    2. For each of the same five test methods, delete the entire 5-line `@pytest.mark.xfail(...)` decorator block immediately above the `def test_...` line. Each block matches the pattern:
       `@pytest.mark.xfail(\n        condition=CONFIG.use_narwhals_backend,\n        reason="Narwhals backend sets use_narwhals_backend=True; config dict assertions hardcode False",\n        strict=True,\n    )`
    3. Do NOT modify the module-level `pytestmark = pytest.mark.parametrize("spark_session", ["spark", "spark_connect"])` at line 23.
    4. Do NOT add or remove imports. `CONFIG` is already imported at line 9.
    5. Do NOT touch the other tests in this file that do not have the matching xfail decorator (e.g. `test_disabling_logging`, `test_use_validate_in_init`, etc.).
    6. Preserve all other lines, comments, docstrings, and dict key ordering exactly.
  </action>
  <verify>
    <automated>grep -cE "\"use_narwhals_backend\": CONFIG\.use_narwhals_backend" tests/pyspark/test_pyspark_config.py | grep -qx 5 &amp;&amp; grep -cE "\"use_narwhals_backend\": False" tests/pyspark/test_pyspark_config.py | grep -qx 0 &amp;&amp; grep -c "Narwhals backend sets use_narwhals_backend=True; config dict assertions hardcode False" tests/pyspark/test_pyspark_config.py | grep -qx 0 &amp;&amp; pixi run pyspark-test tests/pyspark/test_pyspark_config.py::TestPanderaConfig -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "\"use_narwhals_backend\": CONFIG\.use_narwhals_backend" tests/pyspark/test_pyspark_config.py` outputs exactly 5
    - `grep -cE "\"use_narwhals_backend\": False" tests/pyspark/test_pyspark_config.py` outputs exactly 0
    - `grep -c "Narwhals backend sets use_narwhals_backend=True; config dict assertions hardcode False" tests/pyspark/test_pyspark_config.py` outputs exactly 0 (all five xfail reason strings removed)
    - `grep -cE "^    @pytest\.mark\.xfail" tests/pyspark/test_pyspark_config.py` is unchanged-or-decreased-by-exactly-5 from baseline (verify the five class-method-level xfails — at original lines 33, 71, 165, 252, 369 — are gone; any unrelated xfails elsewhere in the file are untouched)
    - `from pandera.config import CONFIG` (or the multi-line equivalent) is still present at the top of the file
    - The module-level `pytestmark = pytest.mark.parametrize("spark_session", ["spark", "spark_connect"])` is unchanged
    - `pixi run pyspark-test tests/pyspark/test_pyspark_config.py::TestPanderaConfig -x` exits 0 with all five `TestPanderaConfig.test_*` tests passing (not xfail/xpass) under `PANDERA_USE_NARWHALS_BACKEND=True`
    - `PANDERA_USE_NARWHALS_BACKEND=False pixi run pyspark-test tests/pyspark/test_pyspark_config.py::TestPanderaConfig -x` exits 0 (no regression to the native PySpark path)
  </acceptance_criteria>
  <done>
    All five `TestPanderaConfig` tests pass without xfail under both narwhals and native PySpark modes; expected dicts assert against `CONFIG.use_narwhals_backend` dynamically.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Environment variable → CONFIG module global | `PANDERA_USE_NARWHALS_BACKEND` env var is read once at import time via `_config_from_env_vars()`; module-level `CONFIG` exposes it as `CONFIG.use_narwhals_backend`. Tests read the same global. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | Test assertion silently masking a config-dataclass regression | mitigate | Dynamic `CONFIG.use_narwhals_backend` in the expected dict (rather than removing the key entirely, as the roadmap offered as an alternative) keeps the full-shape assertion intact. If `use_narwhals_backend` is ever accidentally removed from the `PanderaConfig` dataclass, `asdict(get_config_context())` will no longer contain the key and the dict-equality assertion will fail loudly. RESEARCH.md "Why not remove the key from the assertion" documents this rationale. |
| T-05-06 | Repudiation | Tests passing under both backend modes without clear evidence of which path was exercised | accept | The existing parametrized `pytestmark` already runs every test under both `spark` and `spark_connect`; CI nox session sets `PANDERA_USE_NARWHALS_BACKEND=True` for narwhals and unset for native, producing two distinct CI signals. No additional logging required. |
</threat_model>

<verification>
After the single task completes, run the full TestPanderaConfig class plus its
neighbors to confirm no collateral damage to other config tests:

- `pixi run pyspark-test tests/pyspark/test_pyspark_config.py -x` exits 0 (entire file green under narwhals backend)
- `PANDERA_USE_NARWHALS_BACKEND=False pixi run pyspark-test tests/pyspark/test_pyspark_config.py -x` exits 0 (no regression to native backend)
- The diff to `test_pyspark_config.py` is small and surgical: 5 single-line substitutions inside the `expected =` dicts + 5 contiguous decorator-block deletions, no other lines touched.
</verification>

<success_criteria>
- The single task's `<acceptance_criteria>` are all met.
- TEST-FIX-01 is closed: the five band-aid xfails are removed and the assertions are dynamic against `CONFIG.use_narwhals_backend`.
- No other tests in `tests/pyspark/test_pyspark_config.py` regress under either backend mode.
</success_criteria>

<output>
Create `.planning/phases/05-correctness-and-behavioral-parity/05-02-SUMMARY.md` when done, summarizing:
- Confirmation of 5 substitutions and 5 decorator removals
- Test counts before/after (xfail-strict count decreased by 5; passing count increased by 5)
- Confirmation that both narwhals-on and narwhals-off runs pass
</output>
