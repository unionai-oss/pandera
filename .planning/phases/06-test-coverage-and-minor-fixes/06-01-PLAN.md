---
phase: 06-test-coverage-and-minor-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/narwhals/test_e2e.py
  - tests/narwhals/conftest.py
autonomous: true
requirements:
  - TEST-E2E-01
tags:
  - pyspark
  - narwhals
  - e2e
  - test-coverage

must_haves:
  truths:
    - "tests/narwhals/test_e2e.py contains a PySpark section that runs only when pyspark is installed (skipped otherwise, matching the existing ibis_only pattern)"
    - "The PySpark section asserts NarwhalsCheckBackend is the registered Check backend for pyspark.sql.DataFrame when PANDERA_USE_NARWHALS_BACKEND=True"
    - "The PySpark section asserts schema.validate(spark_df) returns the original pyspark.sql.DataFrame (return-type preservation)"
    - "The PySpark section has at least one passing built-in check (e.g. Check.greater_than) and one failing built-in check where df_out.pandera.errors is inspected for the expected DATA-section error entry"
    - "The PySpark section has at least one nullable=False test asserting the failure is recorded in df_out.pandera.errors when a null value is present"
    - "The PySpark section has at least one unique constraint test asserting the failure is recorded in df_out.pandera.errors when duplicate values are present"
    - "The full test_e2e.py module passes under PANDERA_USE_NARWHALS_BACKEND=True with pyspark installed (the existing polars/ibis tests are not regressed)"
  artifacts:
    - path: tests/narwhals/test_e2e.py
      provides: "PySpark e2e section covering registration, return-type preservation, passing/failing built-in check with failure_cases inspection, nullable, and unique behavior"
      contains: "import pandera.pyspark"
    - path: tests/narwhals/conftest.py
      provides: "Updated module-level backend registration that also clears + re-registers pyspark backends when pyspark is importable"
      contains: "register_pyspark_backends"
  key_links:
    - from: "tests/narwhals/test_e2e.py::PySpark section"
      to: "pandera.backends.pyspark.register.register_pyspark_backends"
      via: "Module-scope fixture re-registers pyspark backends after CONFIG.use_narwhals_backend is True"
      pattern: "register_pyspark_backends"
    - from: "tests/narwhals/test_e2e.py::PySpark section"
      to: "pandera.backends.narwhals.checks.NarwhalsCheckBackend"
      via: "Check.get_backend(pyspark.sql.DataFrame(...)) is NarwhalsCheckBackend"
      pattern: "NarwhalsCheckBackend"
    - from: "tests/narwhals/test_e2e.py::PySpark section"
      to: "df_out.pandera.errors"
      via: "PySpark validation contract: validate() returns the original DataFrame with errors set on .pandera.errors (NOT raise SchemaError, unlike polars/ibis)"
      pattern: "df_out\\.pandera\\.errors"
---

<objective>
Add a PySpark section to tests/narwhals/test_e2e.py that exercises the narwhals backend end-to-end for pyspark.sql.DataFrame inputs, mirroring the existing polars/ibis sections.

Purpose: TEST-E2E-01 is the only end-to-end smoke test of the narwhals backend that is co-located with the polars and ibis e2e coverage. Without a PySpark section, regressions to the narwhals PySpark wiring (backend registration, return-type preservation, accessor-based error contract) would only surface in the much-larger tests/pyspark/ suite. The phase-2 nox session already runs tests/pyspark/ under PANDERA_USE_NARWHALS_BACKEND=True, so this plan only needs to add the focused e2e coverage — it does NOT need to re-cover the full pyspark behavior matrix.

Output: A new "PySpark" section in tests/narwhals/test_e2e.py with: pyspark_only skip marker, fixture(s) producing a pyspark.sql.DataFrame, backend registration assertion, return-type preservation test, passing+failing built-in check with errors inspection, and nullable + unique constraint tests using the pyspark accessor error contract. Updated tests/narwhals/conftest.py module-level fixture additionally re-registers the pyspark backends when pyspark is importable.
</objective>

<execution_context>
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# E2E test file (existing patterns to mirror — polars + ibis sections, ibis_only skip marker, _register_backends fixture)
@tests/narwhals/test_e2e.py

# Narwhals conftest (existing module-level backend registration that this plan extends)
@tests/narwhals/conftest.py

# Reference patterns for PySpark accessor-based error contract (used in tests/pyspark/)
@tests/pyspark/conftest.py
@tests/pyspark/test_pyspark_error.py

# PySpark backend registration that the e2e section needs to trigger
@pandera/backends/pyspark/register.py

# Narwhals container — confirms validate() returns native PySpark DataFrame and sets .pandera.errors via _handle_pyspark_validation_result
@pandera/backends/narwhals/container.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend tests/narwhals/conftest.py to re-register pyspark backends when pyspark is importable</name>
  <files>tests/narwhals/conftest.py</files>
  <read_first>
    - tests/narwhals/conftest.py (current `_ensure_narwhals_backends_registered` only clears and re-calls `register_polars_backends` and `register_ibis_backends`; pyspark must be added behind a try/except ImportError guard so polars/ibis-only environments still work)
    - pandera/backends/pyspark/register.py (use the `register_pyspark_backends` symbol and the `.cache_clear()` pattern — already used identically for polars and ibis)
    - tests/pyspark/test_pyspark_narwhals_register.py (reference: `register_pyspark_backends.cache_clear(); register_pyspark_backends()` is the exact 2-line activation pattern; we will replicate it module-scoped)
  </read_first>
  <behavior>
    - Test 1 (manual sanity, no test file required): with pyspark installed and PANDERA_USE_NARWHALS_BACKEND=True, after the autouse fixture runs, `Check.get_backend(pyspark.sql.DataFrame())` returns NarwhalsCheckBackend
    - Test 2 (regression): without pyspark installed, the fixture still completes cleanly (no ImportError surfaces) and polars/ibis tests are unaffected
  </behavior>
  <action>
    In `_ensure_narwhals_backends_registered` (the `autouse=True, scope="module"` fixture), add a try/except ImportError block that imports `register_pyspark_backends` from `pandera.backends.pyspark.register`, calls `register_pyspark_backends.cache_clear()`, then `register_pyspark_backends()`. Wrap in `try: import pyspark.sql  # noqa: F401  except ImportError: pass` so the conftest stays compatible with the polars-only / ibis-only narwhals nox runs. Place the pyspark block AFTER the polars+ibis re-registration calls so ordering matches the order in which the e2e test file imports `pandera.polars`, `pandera.ibis`, and (new) `pandera.pyspark`. Keep the existing yield. Do not add any new fixtures here — pyspark fixtures (spark session + sample DataFrame) live in the test file itself so the pyspark-only assertions are co-located with the section that uses them.
  </action>
  <verify>
    <automated>JAVA_HOME=/Users/deepyaman/Library/Caches/rattler/cache/pkgs/openjdk-17.0.17-h99a4030_0/lib/jvm PANDERA_USE_NARWHALS_BACKEND=True pytest tests/narwhals/test_e2e.py -x -q --collect-only 2>&1 | tail -20</automated>
  </verify>
  <done>
    tests/narwhals/conftest.py imports `register_pyspark_backends` inside the autouse fixture under a `try: import pyspark.sql / except ImportError: pass` guard, calls `register_pyspark_backends.cache_clear()` then `register_pyspark_backends()`. The existing polars/ibis registration calls remain intact and the yield still fires. Collection of tests/narwhals/test_e2e.py succeeds without raising ImportError even when pyspark is absent.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add PySpark section to tests/narwhals/test_e2e.py</name>
  <files>tests/narwhals/test_e2e.py</files>
  <read_first>
    - tests/narwhals/test_e2e.py (mirror the ibis_only skip pattern at the top of the file, the section banner comment style `# ====...` between sections, the `_register_backends` autouse fixture, and the existing test naming `test_narwhals_backend_registered_for_*`, `test_*_returns_*`, `TestBuiltinChecks*`, `test_*_nullable_false_raises_on_null`)
    - tests/pyspark/conftest.py (reuse the spark fixture pattern: `SparkSession.builder.config("spark.sql.ansi.enabled", False).getOrCreate()` plus the Java 17 workaround for pyspark >= 4.0 setting `spark.hadoop.fs.defaultFS=file:///` and `spark.sql.warehouse.dir=file:///tmp/spark-warehouse`; the `SPARK_LOCAL_IP=127.0.0.1` and `PYARROW_IGNORE_TIMEZONE=1` env vars are also required — set them in an `autouse=True` function-scoped fixture so they take effect before the session-scoped spark fixture runs)
    - tests/pyspark/test_pyspark_error.py (lines 39-79 show the failing-check error inspection contract: `df_out = schema.validate(df_fail)` then `dict(df_out.pandera.errors["DATA"])` indexed by `"DATAFRAME_CHECK"` key — under narwhals the error string is `"Check '<Check {name}: {repr}>' failed."`)
    - pandera/backends/narwhals/container.py (`_handle_pyspark_validation_result` sets `check_obj.pandera.errors = error_dicts` on failure and `= {}` on success; this confirms the test contract for PySpark is accessor-based, NOT exception-based)
    - pandera/api/pyspark/types.py (use `pyspark.sql.DataFrame` for return-type assertions; the `pyspark_connect.DataFrame` flavor does not need separate coverage here — phase-1 already tested registration for both)
  </read_first>
  <behavior>
    - Test 1: `test_narwhals_backend_registered_for_pyspark_dataframe` — `Check.get_backend(spark.createDataFrame(...))` is `NarwhalsCheckBackend` when PANDERA_USE_NARWHALS_BACKEND=True
    - Test 2: `test_pyspark_dataframe_returns_pyspark_dataframe` — `schema.validate(spark_df)` returns an instance of `pyspark.sql.DataFrame`
    - Test 3: `test_pyspark_builtin_check_passes` — `Check.greater_than(0)` on a column of all-positive ints leaves `df_out.pandera.errors == {}`
    - Test 4: `test_pyspark_builtin_check_fails` — `Check.greater_than(0)` on a column containing negative values produces `dict(df_out.pandera.errors["DATA"])["DATAFRAME_CHECK"]` with the expected `column`, `check`, and `error` keys (the error string under narwhals is `"Check '<Check greater_than: greater_than(0)>' failed."`)
    - Test 5: `test_pyspark_nullable_false_fails` — `Column("int", nullable=False)` on a column with a null produces a non-empty `df_out.pandera.errors`
    - Test 6: `test_pyspark_unique_constraint_fails` — `Column("int", unique=True)` on a column with duplicate values produces a non-empty `df_out.pandera.errors`
  </behavior>
  <action>
    Add a "PySpark" section at the bottom of tests/narwhals/test_e2e.py, after the existing lazy validation section. Structure:

    1. After the existing `try: import ibis ... HAS_IBIS = True` block, add a parallel `try: import pyspark.sql; from pyspark.sql import SparkSession; import pandera.pyspark as pa_pyspark; HAS_PYSPARK = True; except ImportError: HAS_PYSPARK = False` block. Define `pyspark_only = pytest.mark.skipif(not HAS_PYSPARK, reason="pyspark not installed")`.

    2. Add an `autouse=True, scope="function"` fixture `_spark_env_vars` that sets `os.environ["SPARK_LOCAL_IP"] = "127.0.0.1"` and `os.environ["PYARROW_IGNORE_TIMEZONE"] = "1"` (no-op if pyspark is not installed — gate the body with `if HAS_PYSPARK:`).

    3. Add a `scope="module"` `spark` fixture gated by `@pyspark_only` that builds the SparkSession with the same config used in tests/pyspark/conftest.py (`spark.sql.ansi.enabled=False`, plus the pyspark-4.0+ Java-17 workaround setting `spark.hadoop.fs.defaultFS=file:///` and `spark.sql.warehouse.dir=file:///tmp/spark-warehouse`). Yield then `spark.stop()`.

    4. Add a `pyspark_df` fixture (function-scope, depends on `spark`) returning a pyspark.sql.DataFrame with the same `{"x": [1,2,3], "y": [10,20,30]}` shape as the polars/ibis fixtures, using `IntegerType` for both columns.

    5. Section banner comment `# ==... PySpark ==...` followed by six tests, all decorated `@pyspark_only`:
       - `test_narwhals_backend_registered_for_pyspark_dataframe`: imports `from pandera.api.checks import Check`, calls `Check.get_backend(spark.createDataFrame([(1,)], "x int"))`, asserts `backend is NarwhalsCheckBackend`.
       - `test_pyspark_dataframe_returns_pyspark_dataframe(pyspark_df)`: builds `schema = pa_pyspark.DataFrameSchema({"x": pa_pyspark.Column("int"), "y": pa_pyspark.Column("int")})`; asserts `isinstance(schema.validate(pyspark_df), pyspark.sql.DataFrame)`.
       - `test_pyspark_builtin_check_passes(pyspark_df)`: schema with `pa_pyspark.Check.greater_than(0)` on `x`; asserts `schema.validate(pyspark_df).pandera.errors == {}`.
       - `test_pyspark_builtin_check_fails(spark)`: builds a failing df `spark.createDataFrame([(-1,),(2,),(-3,)], "x int")`; same `Check.greater_than(0)` schema; asserts `dict(df_out.pandera.errors["DATA"])["DATAFRAME_CHECK"]` is a non-empty list whose first entry has `"column" == "x"`, `"check" == "greater_than(0)"`, and `"error"` containing `"greater_than"`. Use the narwhals error string format `"Check '<Check greater_than: greater_than(0)>' failed."` for an exact match (consistent with tests/pyspark/test_pyspark_error.py:60-66 under `if CONFIG.use_narwhals_backend`).
       - `test_pyspark_nullable_false_fails(spark)`: builds a df with a null in column `x` via a schema declaring `IntegerType()` nullable=True at the StructField level, then `pa_pyspark.DataFrameSchema({"x": pa_pyspark.Column("int", nullable=False)})`; assert `df_out.pandera.errors` is non-empty.
       - `test_pyspark_unique_constraint_fails(spark)`: builds a df with duplicate `int` values; schema `{"x": pa_pyspark.Column("int", unique=True)}`; assert `df_out.pandera.errors` is non-empty.

    6. Do NOT add `nox` or CI changes — phase-2 nox session already runs tests/narwhals/ as part of the polars/ibis matrix entries; the pyspark matrix entry runs tests/pyspark/ (where the conftest fixture changes from Task 1 will simply no-op because tests/pyspark/ has its own conftest). The new tests in tests/narwhals/test_e2e.py will run under any narwhals matrix entry where pyspark happens to be installed (which is the pyspark entry); on polars-only / ibis-only entries the `@pyspark_only` marker skips them.

    7. Imports: at the top of test_e2e.py add `import os` if not present, and place the new `try: import pyspark.sql ...` block immediately after the existing ibis try-except so the import flag order matches the test ordering in the file.
  </action>
  <verify>
    <automated>JAVA_HOME=/Users/deepyaman/Library/Caches/rattler/cache/pkgs/openjdk-17.0.17-h99a4030_0/lib/jvm PANDERA_USE_NARWHALS_BACKEND=True pytest tests/narwhals/test_e2e.py -x -q -k pyspark 2>&1 | tail -25</automated>
  </verify>
  <done>
    Running `JAVA_HOME=... PANDERA_USE_NARWHALS_BACKEND=True pytest tests/narwhals/test_e2e.py -k pyspark` exits 0 with six PASSED outcomes (the six new pyspark tests). Running `JAVA_HOME=... PANDERA_USE_NARWHALS_BACKEND=True pytest tests/narwhals/test_e2e.py` exits 0 with all polars + ibis + pyspark tests passing (no regressions to the existing 30+ tests). `grep -c "@pyspark_only" tests/narwhals/test_e2e.py` returns at least 6 (one per new pyspark test or one per test class). The PySpark section asserts on `df_out.pandera.errors` (NOT `pytest.raises(SchemaError)`), confirming the accessor-based pyspark error contract is exercised.
  </done>
</task>

</tasks>

<verification>
1. With pyspark installed and `JAVA_HOME` pointing at JDK 17:
   ```
   JAVA_HOME=/Users/deepyaman/Library/Caches/rattler/cache/pkgs/openjdk-17.0.17-h99a4030_0/lib/jvm \
   PANDERA_USE_NARWHALS_BACKEND=True \
   pytest tests/narwhals/test_e2e.py -v
   ```
   exits 0; the new pyspark tests run alongside the existing polars/ibis tests with no skips on the pyspark side.

2. Without pyspark installed (polars-only narwhals run):
   ```
   PANDERA_USE_NARWHALS_BACKEND=True pytest tests/narwhals/test_e2e.py -v
   ```
   exits 0; the six new pyspark tests are SKIPPED (not errored) and the existing polars/ibis tests still pass.

3. `grep -c "^@pyspark_only" tests/narwhals/test_e2e.py` returns >= 6 (or the count of pyspark tests if grouped under a class with a class-level marker).

4. `grep -c "df_out.pandera.errors" tests/narwhals/test_e2e.py` returns >= 4 (the failing-check, nullable, and unique tests all assert on the accessor).
</verification>

<success_criteria>
- TEST-E2E-01 satisfied: tests/narwhals/test_e2e.py includes a PySpark section with backend registration assertion, return-type preservation, passing+failing built-in check with `df_out.pandera.errors` inspection, nullable, and unique behavior.
- The PySpark section is properly skipped when pyspark is not installed (no false test failures in polars-only or ibis-only narwhals nox runs).
- The narwhals conftest registers pyspark backends when pyspark is importable, so the e2e section sees the narwhals backend (not the native pyspark backend) when `PANDERA_USE_NARWHALS_BACKEND=True`.
- No regressions to the existing 30+ polars/ibis tests in tests/narwhals/test_e2e.py.
</success_criteria>

<output>
Create `.planning/phases/06-test-coverage-and-minor-fixes/06-01-SUMMARY.md` when done
</output>
