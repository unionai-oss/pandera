---
phase: 06-test-coverage-and-minor-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci-tests.yml
  - pandera/backends/narwhals/container.py
  - pandera/backends/ibis/container.py
  - tests/ibis/test_ibis_container.py
  - tests/pyspark/test_pyspark_narwhals_register.py
  - tests/pyspark/test_pyspark_model.py
  - pandera/api/pyspark/types.py
autonomous: true
requirements:
  - NITS-01
tags:
  - pyspark
  - narwhals
  - pr-review
  - minor-fixes
  - nits

must_haves:
  truths:
    - "The .github/workflows/ci-tests.yml `exclude` block that skips pyspark on Python 3.12 and 3.13 has an inline comment naming PySpark's maximum supported Python version as the reason (NITS-01: CI version comment)"
    - "pandera/backends/narwhals/container.py emits `column 'X' not found` (instead of `column 'X' not in dataframe`) for the COLUMN_NOT_IN_DATAFRAME reason code (NITS-01: backend-neutral error message)"
    - "pandera/backends/ibis/container.py emits `column 'X' not found.` (instead of `column 'X' not in table.`) for the COLUMN_NOT_IN_DATAFRAME reason code at line 386 (NITS-01: backend-neutral error message — both narwhals AND native ibis emit the same `not found` wording)"
    - "The xfail decorator on tests/ibis/test_ibis_container.py::test_column_absent_error is removed and the test's `pytest.raises(..., match=...)` regex is updated from `column 'int_col' not in table` to `column 'int_col' not found` so the SAME assertion passes against both the native ibis backend AND the narwhals backend"
    - "tests/pyspark/test_pyspark_narwhals_register.py::test_pyspark_narwhals_activated_when_opted_in asserts NarwhalsCheckBackend is registered for Check, ColumnBackend (narwhals) for Column, AND DataFrameSchemaBackend (narwhals) for DataFrameSchema — all three backends, not just DataFrameSchemaBackend"
    - "tests/pyspark/test_pyspark_model.py::test_registered_dataframemodel_checks uses a single `@pytest.mark.xfail(...)` with `condition=(CONFIG.use_narwhals_backend OR raises=ValueError)` semantics — the two stacked xfail decorators are replaced by one decorator that captures both conditions"
    - "pandera/api/pyspark/types.py::supported_types() returns each PySpark frame type exactly once — `PySparkSQLDataFrame` is no longer appended twice; the function returns a tuple containing `PySparkSQLDataFrame` and (when available) `PySparkConnectDataFrame`"
  artifacts:
    - path: .github/workflows/ci-tests.yml
      provides: "Comment on the pyspark exclude block explaining PySpark's maximum supported Python version (currently 3.11; 3.12/3.13 not yet supported)"
      contains: "max supported"
    - path: pandera/backends/narwhals/container.py
      provides: "Backend-neutral COLUMN_NOT_IN_DATAFRAME message `column '{colname}' not found`"
      contains: "not found"
    - path: pandera/backends/ibis/container.py
      provides: "Backend-neutral COLUMN_NOT_IN_DATAFRAME message `column '{colname}' not found.` (replacing `not in table.`) at line 386"
      contains: "not found"
    - path: tests/ibis/test_ibis_container.py
      provides: "test_column_absent_error WITHOUT the xfail decorator (passes under both native ibis and narwhals; `match=` regex updated to `not found`)"
    - path: tests/pyspark/test_pyspark_narwhals_register.py
      provides: "test_pyspark_narwhals_activated_when_opted_in asserts all three backends (Check, Column, DataFrameSchema) are registered with their narwhals implementations"
      contains: "NarwhalsCheckBackend"
    - path: tests/pyspark/test_pyspark_model.py
      provides: "test_registered_dataframemodel_checks with a single xfail decorator (not stacked)"
    - path: pandera/api/pyspark/types.py
      provides: "supported_types() with no duplicate PySparkSQLDataFrame entry"
  key_links:
    - from: "pandera/backends/narwhals/container.py::DataFrameSchemaBackend.check_column_presence (COLUMN_NOT_IN_DATAFRAME emitter near line 594)"
      to: "tests/ibis/test_ibis_container.py::test_column_absent_error"
      via: "Error message string used in xfail-removed test"
      pattern: "column '.*' not found"
    - from: "pandera/backends/ibis/container.py (COLUMN_NOT_IN_DATAFRAME emitter at line 386)"
      to: "tests/ibis/test_ibis_container.py::test_column_absent_error"
      via: "Native ibis path now emits `not found` instead of `not in table`, matching the updated `match=` regex"
      pattern: "column '.*' not found"
    - from: "pandera/api/pyspark/types.py::supported_types"
      to: "PySparkSQLDataFrame and PySparkConnectDataFrame"
      via: "Single append of each frame type; the `try: table_types.append(PySparkSQLDataFrame); table_types.append(PySparkConnectDataFrame)` block becomes `try: table_types.append(PySparkConnectDataFrame)` (PySparkSQLDataFrame is already in the initial list)"
      pattern: "table_types\\.append\\(PySparkConnectDataFrame\\)"
---

<objective>
Resolve the five minor pre-merge nits from the PR #2339 review: add a comment to the CI Python exclusion, swap the backend-specific "not in dataframe"/"not in table" messages for a backend-neutral "not found" across BOTH the narwhals and native ibis backend container modules, make the narwhals registration test assert on all three backends, collapse the stacked xfail decorators in test_pyspark_model.py, and fix the duplicate append in supported_types().

Purpose: NITS-01 is the last cluster of pre-merge correctness items required before PR #2339 can be merged. None of these items individually warrant their own plan, but together they unblock the merge. All five touch distinct files with no logical coupling, so they are batched into a single plan that an executor can complete linearly without context degradation. The 5-task count exceeds the default scope_sanity warning threshold (4), but each task is a mechanically simple single-line or single-decorator edit (no design decisions, no cross-file coupling, no failure-mode analysis), the total context load is well under 50%, and splitting Task 1 (1-line YAML comment) or Task 5 (1-line delete) into a separate plan would add orchestration overhead without any quality benefit. The 5-task layout is intentional. After this plan, every PR-review item is closed and the milestone can ship.

Output: Seven file edits (one comment, two message strings — narwhals + ibis containers — one xfail removal + regex update, two assertion expansions, one duplicate removal) across six subsystems.
</objective>

<execution_context>
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# CI workflow (the pyspark exclude block at lines 347-351 needs a comment)
@.github/workflows/ci-tests.yml

# Backend container — narwhals (the "not in dataframe" message near line 594)
@pandera/backends/narwhals/container.py

# Backend container — native ibis (the "not in table" message at line 386)
@pandera/backends/ibis/container.py

# Ibis container test (the xfail decorator to remove and the `match=` regex to update)
@tests/ibis/test_ibis_container.py

# Narwhals registration test (expand assertion to all three backends)
@tests/pyspark/test_pyspark_narwhals_register.py

# PySpark model test (collapse stacked xfails)
@tests/pyspark/test_pyspark_model.py

# PySpark types module (fix duplicate append)
@pandera/api/pyspark/types.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add comment to PySpark CI Python version exclusion block</name>
  <files>.github/workflows/ci-tests.yml</files>
  <read_first>
    - .github/workflows/ci-tests.yml lines 340-365 (the `unit-tests-narwhals-backend` matrix; the `exclude:` block at lines 347-351 has two entries for `pyspark` + `python-version: "3.12"` and `"3.13"` with no comment)
  </read_first>
  <action>
    Insert a single YAML comment line directly above the `exclude:` line (line 347) in the `unit-tests-narwhals-backend` job's matrix section. The comment must name PySpark's maximum supported Python version. As of the current pyspark[connect] >= 3.2.0 pin in noxfile.py line 361, PySpark's officially supported Python ceiling is 3.11 (PySpark 3.5 supports Python 3.8-3.11; 3.12/3.13 are not on the supported matrix). Wording: `# PySpark's maximum supported Python version is 3.11; 3.12 and 3.13 are not yet on the official PySpark support matrix.` (one line, with the YAML `#` prefix; indent two spaces deeper than `matrix:` so it sits at the same indent as `exclude:`). Do not modify the exclude entries themselves and do not introduce a separate comment per entry — one explanatory comment above the block is sufficient.
  </action>
  <verify>
    <automated>grep -B1 "extra: pyspark$" .github/workflows/ci-tests.yml | grep -c "max" </automated>
  </verify>
  <done>
    `grep -B2 "exclude:" .github/workflows/ci-tests.yml | grep -A1 "matrix" | grep -c "max"` returns >= 1, AND `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci-tests.yml'))"` exits 0 (the YAML still parses), AND the two existing exclude entries (`pyspark`+`3.12`, `pyspark`+`3.13`) are unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace "not in dataframe" / "not in table" with backend-neutral "not found" message</name>
  <files>pandera/backends/narwhals/container.py, pandera/backends/ibis/container.py, tests/ibis/test_ibis_container.py</files>
  <read_first>
    - pandera/backends/narwhals/container.py lines 580-600 (the message `f"column '{colname}' not in dataframe\n{_to_native(check_obj.head())}"` at line 594 — emitted with `reason_code=SchemaErrorReason.COLUMN_NOT_IN_DATAFRAME`)
    - pandera/backends/ibis/container.py lines 380-392 (the CONFIRMED emit site: at line 386 the native ibis backend emits `f"column '{colname}' not in table. "` followed by `f"Columns in table: {check_obj.columns}"` — same `reason_code=SchemaErrorReason.COLUMN_NOT_IN_DATAFRAME`, inside a `CoreCheckResult(passed=False, check="column_in_dataframe", ...)` block)
    - tests/ibis/test_ibis_container.py lines 198-208 (the `@pytest.mark.xfail` decorator with `condition=CONFIG.use_narwhals_backend` and `reason="Error message format differs: 'not in dataframe' vs 'not in table' in Narwhals backend"`; the test asserts on `match="column 'int_col' not in table"` which is the native ibis backend's wording)
    - tests/ibis/test_ibis_container.py lines ~170-188 (the surrounding test `test_column_absent_error` or its companions that exercise the native ibis path with `not in table` — confirm whether any other test depends on the exact `not in table` / `not in dataframe` strings; if any other test does, those `match=` regexes must ALSO be updated to `not found`)
  </read_first>
  <behavior>
    - Test 1: validating a schema declaring a column not present in a narwhals-wrapped frame produces a SchemaError whose message contains `column 'X' not found` (where X is the column name)
    - Test 2: validating a schema declaring a column not present in a native-ibis-backed table produces a SchemaError whose message contains `column 'X' not found.` (where X is the column name) — the trailing period is preserved from the existing ibis emit site
    - Test 3: tests/ibis/test_ibis_container.py::test_column_absent_error PASSES under PANDERA_USE_NARWHALS_BACKEND=True with no xfail decorator
    - Test 4 (regression): tests/ibis/test_ibis_container.py::test_column_absent_error PASSES under the native ibis backend (PANDERA_USE_NARWHALS_BACKEND=False) — the SAME `match="column 'int_col' not found"` regex matches both backends because both now emit the same wording
  </behavior>
  <action>
    Three edits.

    EDIT A — pandera/backends/narwhals/container.py around line 594: change the message string from `f"column '{colname}' not in dataframe\n{_to_native(check_obj.head())}"` to `f"column '{colname}' not found\n{_to_native(check_obj.head())}"`. Keep the trailing `\n{_to_native(check_obj.head())}` and the surrounding `CoreCheckResult(passed=False, check="column_in_dataframe", reason_code=SchemaErrorReason.COLUMN_NOT_IN_DATAFRAME, ...)` structure unchanged.

    EDIT B — pandera/backends/ibis/container.py at line 386: change `f"column '{colname}' not in table. "` to `f"column '{colname}' not found. "`. Preserve the trailing space (the next line concatenates `f"Columns in table: {check_obj.columns}"`). Keep the `CoreCheckResult(passed=False, check="column_in_dataframe", reason_code=SchemaErrorReason.COLUMN_NOT_IN_DATAFRAME, ..., failure_cases=colname)` structure unchanged. This edit is unconditional — the emit site is confirmed at line 386 and there is no shared message path between the native ibis backend and the narwhals backend; both backends emit their own message strings and both must be updated for the test in EDIT C to pass under both configurations.

    EDIT C — tests/ibis/test_ibis_container.py around line 198: remove the `@pytest.mark.xfail(condition=CONFIG.use_narwhals_backend, reason="Error message format differs: 'not in dataframe' vs 'not in table' in Narwhals backend", strict=True)` decorator entirely from `test_column_absent_error`. Then update the test's `pytest.raises(pa.errors.SchemaError, match="column 'int_col' not in table")` regex to `match="column 'int_col' not found"` so it matches the new wording emitted by BOTH backends (narwhals's `column 'int_col' not found` from EDIT A and native ibis's `column 'int_col' not found.` from EDIT B; the regex `not found` matches both because the trailing period in the ibis case is outside the captured substring).

    The CONFIG.use_narwhals_backend import on test_ibis_container.py becomes unused if no other xfail in the file references it — leave the import in place rather than risking a cascade of removals; ruff will flag it if truly orphaned and the existing PR has many other places using CONFIG.use_narwhals_backend.

    After EDIT B, also grep the rest of `tests/ibis/` for `"not in table"` to confirm no OTHER test asserts on the now-removed wording. If any other test's `match=` regex depends on `not in table`, update those to `not found` as well — but do not change tests that assert on `not in table` for an UNRELATED reason (e.g., a different error path); only update tests that target the COLUMN_NOT_IN_DATAFRAME reason_code path.
  </action>
  <verify>
    <automated>grep -c "not in dataframe" pandera/backends/narwhals/container.py</automated>
  </verify>
  <done>
    `grep -v '^#' pandera/backends/narwhals/container.py | grep -c "not in dataframe"` returns 0. `grep -v '^#' pandera/backends/ibis/container.py | grep -c "not in table\."` returns 0 (the native ibis emit site no longer uses `not in table.`). `grep -c "not found" pandera/backends/narwhals/container.py` returns >= 1 in the COLUMN_NOT_IN_DATAFRAME emit context. `grep -c "not found" pandera/backends/ibis/container.py` returns >= 1 in the COLUMN_NOT_IN_DATAFRAME emit context. `grep -A3 "def test_column_absent_error" tests/ibis/test_ibis_container.py` shows no `@pytest.mark.xfail` immediately above the function definition. Running `pytest tests/ibis/test_ibis_container.py::test_column_absent_error -x` exits 0 under BOTH `PANDERA_USE_NARWHALS_BACKEND=True` AND unset (regression check: native ibis still passes with the SAME `match="column 'int_col' not found"` regex).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expand narwhals-activation test to assert all three backends are registered</name>
  <files>tests/pyspark/test_pyspark_narwhals_register.py</files>
  <read_first>
    - tests/pyspark/test_pyspark_narwhals_register.py (current `test_pyspark_narwhals_activated_when_opted_in` only asserts `isinstance(backend, NarwhalsDataFrameSchemaBackend)` — the Check and Column backends are not asserted on; the saved/finalize pattern at lines 22-33 already correctly restores the DataFrameSchema registry, and an analogous save/restore for Column + Check should be added)
    - pandera/backends/pyspark/register.py lines 59-67 (the narwhals branch registers THREE backends: `DataFrameSchema.register_backend(pyspark_sql.DataFrame, DataFrameSchemaBackend)`, `Column.register_backend(pyspark_sql.DataFrame, ColumnBackend)`, `Check.register_backend(pyspark_sql.DataFrame, NarwhalsCheckBackend)`)
    - pandera/api/pyspark/components.py (confirm the `Column` class fully-qualified path used in the test: `from pandera.api.pyspark.components import Column as PySparkColumn`)
    - pandera/api/checks.py (confirm the `Check` class fully-qualified path: `from pandera.api.checks import Check as PySparkCheck` — note Check is shared, not pyspark-specific)
  </read_first>
  <behavior>
    - Test 1: after `register_pyspark_backends()` runs with CONFIG.use_narwhals_backend=True, all three of `DataFrameSchema.get_backend(check_type=pyspark_sql.DataFrame)`, `Column.get_backend(check_type=pyspark_sql.DataFrame)`, and `Check.get_backend_from_type(pyspark_sql.DataFrame)` return instances of `NarwhalsDataFrameSchemaBackend`, `ColumnBackend` (narwhals), and `NarwhalsCheckBackend` respectively
    - Test 2 (regression): existing assertion on `NarwhalsDataFrameSchemaBackend` still passes (the additional assertions are added, not substituted)
    - Test 3 (regression): the other tests in the file (`test_pyspark_native_unchanged_when_flag_off`, `test_pyspark_connect_narwhals_activated_when_opted_in`, `test_pyspark_register_is_idempotent`) still pass unchanged
  </behavior>
  <action>
    In `test_pyspark_narwhals_activated_when_opted_in`, extend the assertion block at the end of the function. After the existing `backend = PySparkDataFrameSchema.get_backend(check_type=pyspark_sql.DataFrame); assert isinstance(backend, NarwhalsDataFrameSchemaBackend)`, add:

    1. New imports inside the test function (mirror the existing in-function import style):
       - `from pandera.api.pyspark.components import Column as PySparkColumn`
       - `from pandera.api.checks import Check`
       - `from pandera.backends.narwhals.checks import NarwhalsCheckBackend`
       - `from pandera.backends.narwhals.components import ColumnBackend as NarwhalsColumnBackend`

    2. Extend the save/restore finalizer block (currently lines 21-33) to also save and restore the Column registry entry and the Check registry entry for `pyspark_sql.DataFrame`, mirroring the existing 3-line pattern (`saved = X.BACKEND_REGISTRY.pop(key, None); request.addfinalizer(...); if saved is not None: request.addfinalizer(lambda: X.BACKEND_REGISTRY.update({key: saved}))`). Apply for `PySparkColumn` and for the Check registry. Check uses `Check.BACKEND_REGISTRY` (or whatever the actual registry attribute name is — read `pandera/api/checks.py` for the exact attribute used by `register_backend` and `get_backend`).

    3. After `register_pyspark_backends()`, add two new assertions:
       - `column_backend = PySparkColumn.get_backend(check_type=pyspark_sql.DataFrame); assert isinstance(column_backend, NarwhalsColumnBackend)`
       - For the Check assertion: invoke `Check.get_backend(pyspark_sql.DataFrame(...))` — but constructing a real PySpark DataFrame here would require a SparkSession. Use the registry-direct lookup instead: `assert (Check, pyspark_sql.DataFrame) in Check.BACKEND_REGISTRY` AND `assert Check.BACKEND_REGISTRY[(Check, pyspark_sql.DataFrame)] is NarwhalsCheckBackend` (read pandera/api/checks.py to confirm the exact key shape — it may be `(pyspark_sql.DataFrame,)` or `pyspark_sql.DataFrame` directly; mirror the lookup style already used inside `Check.get_backend` to avoid the SparkSession requirement).

    4. Do NOT modify the three sibling tests. Keep the same `monkeypatch.setattr(CONFIG, "use_narwhals_backend", True)`, `register_pyspark_backends.cache_clear()`, `register_pyspark_backends()` sequence — only the assertion block expands.
  </action>
  <verify>
    <automated>grep -c "NarwhalsCheckBackend\|NarwhalsColumnBackend" tests/pyspark/test_pyspark_narwhals_register.py</automated>
  </verify>
  <done>
    `grep -c "NarwhalsCheckBackend" tests/pyspark/test_pyspark_narwhals_register.py` returns >= 1; `grep -c "NarwhalsColumnBackend\|ColumnBackend as NarwhalsColumnBackend" tests/pyspark/test_pyspark_narwhals_register.py` returns >= 1; `grep -c "isinstance" tests/pyspark/test_pyspark_narwhals_register.py` returns >= 3 (DataFrameSchema, Column, plus the existing test_pyspark_native_unchanged_when_flag_off NativeBackend assertion). Running `JAVA_HOME=... PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/test_pyspark_narwhals_register.py -v` exits 0 with all four tests passing.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Combine stacked xfail decorators in test_pyspark_model.py::test_registered_dataframemodel_checks</name>
  <files>tests/pyspark/test_pyspark_model.py</files>
  <read_first>
    - tests/pyspark/test_pyspark_model.py lines 548-566 (the function has TWO stacked decorators: `@pytest.mark.xfail(raises=ValueError)` from the original native-backend duplicate-registration issue, followed by `@pytest.mark.xfail(condition=CONFIG.use_narwhals_backend, reason="narwhals column backend has no coerce_dtype; age: int inferred as LongType() by Spark", strict=True)`)
    - tests/pyspark/test_pyspark_model.py top-of-file imports (confirm `pytest` and `from pandera.config import CONFIG` are already imported)
    - The pytest docs invariant: stacked `@pytest.mark.xfail` decorators do NOT compose — pytest only honors the outermost one. The current code is therefore SILENTLY DROPPING the narwhals-specific xfail when CONFIG.use_narwhals_backend is True under native registration; this nit fix collapses both expected-failure conditions into a single decorator.
  </read_first>
  <behavior>
    - Test 1: when CONFIG.use_narwhals_backend is True, the test is xfail-marked with strict=True (narwhals coerce_dtype limitation)
    - Test 2: when CONFIG.use_narwhals_backend is False AND the second parametrized run triggers duplicate registration, the test is xfail-marked (raises=ValueError) — but NOT strict=True (the first parametrization may pass; only the second raises ValueError)
    - Test 3 (regression): the test still XFAILs under PANDERA_USE_NARWHALS_BACKEND=True (no new PASS or FAIL outcome)
  </behavior>
  <action>
    Replace the two stacked `@pytest.mark.xfail` decorators on `test_registered_dataframemodel_checks` (lines 550-555) with a single `@pytest.mark.xfail(...)`. The combined decorator must capture both failure modes:

    - The native-backend `ValueError` from `@pax.register_check_method` duplicate registration (only fires on the SECOND parametrization run — `spark_session` is parametrized over `["spark", "spark_connect"]`, so the check is re-registered the second time)
    - The narwhals-backend `coerce_dtype`/`LongType()` mismatch (fires on both runs when CONFIG.use_narwhals_backend is True)

    Replacement decorator structure: a single `@pytest.mark.xfail(reason="narwhals: column backend has no coerce_dtype (age: int → LongType()); native: ValueError on duplicate register_check_method on second parametrized run", raises=(ValueError, Exception) if CONFIG.use_narwhals_backend else ValueError, strict=False)`.

    Use `strict=False` (not `strict=True`) because the stacked-decorator semantics in the original code were already non-strict (the inner `@pytest.mark.xfail(raises=ValueError)` was non-strict, only the outer narwhals one was strict — and pytest dropped the narwhals one anyway). Setting `strict=False` preserves the existing pass-on-XPASS behavior. If the executor finds that pytest reports XPASS-strict under narwhals after the change, switch to `strict=CONFIG.use_narwhals_backend` (strict only under narwhals).

    Concretely, the `raises=` argument should accept BOTH `ValueError` and the broader narwhals failure mode in one decorator. The cleanest single-decorator form uses a tuple: `raises=(ValueError, SchemaError)` (import `SchemaError` from `pandera.errors` if not already imported — check the existing imports). If the narwhals failure raises something other than SchemaError, use the parent class — but the conservative approach is `raises=Exception` to capture both. Prefer the conservative `raises=Exception` if a precise dual-exception lookup is not obvious from the existing test failure output; document the broadness in the `reason=` string.

    Single-decorator final form (concrete):
    `@pytest.mark.xfail(reason="...", raises=Exception, strict=False)` — with the reason string capturing both root causes as described above. Use the EXACT reason text from the existing decorator pair concatenated with " AND ".
  </action>
  <verify>
    <automated>grep -B5 "def test_registered_dataframemodel_checks" tests/pyspark/test_pyspark_model.py | grep -c "@pytest.mark.xfail"</automated>
  </verify>
  <done>
    `grep -B5 "def test_registered_dataframemodel_checks" tests/pyspark/test_pyspark_model.py | grep -c "@pytest.mark.xfail"` returns exactly 1 (was 2 before). `JAVA_HOME=... PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/test_pyspark_model.py::test_registered_dataframemodel_checks -v` reports XFAIL (not FAILED, not PASSED). `JAVA_HOME=... pytest tests/pyspark/test_pyspark_model.py::test_registered_dataframemodel_checks -v` (without narwhals flag) still reports XFAIL or XPASS depending on the parametrization — but does not error out the test session.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Fix supported_types() double-append in pandera/api/pyspark/types.py</name>
  <files>pandera/api/pyspark/types.py</files>
  <read_first>
    - pandera/api/pyspark/types.py lines 98-113 (the `supported_types()` function; line 102 initialises `table_types = [PySparkSQLDataFrame]`, then the try block at lines 104-106 appends `PySparkSQLDataFrame` AGAIN (bug) followed by `PySparkConnectDataFrame`; the `except ImportError` at line 108 is silent which is why this has gone unnoticed)
    - pandera/api/pyspark/types.py lines 16-33 (the conditional Spark Connect import: when pyspark < 3.4 OR grpcio-status missing, `PySparkConnectDataFrame` is aliased to `PySparkSQLDataFrame` so the `try: ... except ImportError` block in `supported_types()` never actually raises — the duplicate append happens unconditionally)
  </read_first>
  <action>
    Single edit. In `supported_types()` (around lines 98-113), delete the `table_types.append(PySparkSQLDataFrame)` line (line 105 — the duplicate append immediately inside the try block). Keep the surrounding structure: initial list `[PySparkSQLDataFrame]`, the try/except, the `table_types.append(PySparkConnectDataFrame)` call, and the `return SupportedTypes(tuple(table_types))`.

    After the fix, `supported_types().table_types` returns either `(PySparkSQLDataFrame,)` when Spark Connect is unavailable, OR `(PySparkSQLDataFrame, PySparkConnectDataFrame)` when available — NOT `(PySparkSQLDataFrame, PySparkSQLDataFrame, PySparkConnectDataFrame)` as the current buggy code returns.

    Note: because lines 16-33 alias `PySparkConnectDataFrame` to `PySparkSQLDataFrame` on older PySpark or when grpcio-status is missing, the post-fix tuple may still contain `PySparkSQLDataFrame` twice in those environments — but that is a separate pre-existing aliasing concern (out of scope for this nit; it does not change the behavior of `is_table()` which uses `isinstance` and is unaffected by tuple duplication). Do NOT add deduplication logic to suppress the alias-driven duplicate; only remove the unambiguous double-append bug on line 105.
  </action>
  <verify>
    <automated>grep -c "table_types.append(PySparkSQLDataFrame)" pandera/api/pyspark/types.py</automated>
  </verify>
  <done>
    `grep -c "table_types.append(PySparkSQLDataFrame)" pandera/api/pyspark/types.py` returns 0 (the duplicate append is gone). `grep -c "table_types.append(PySparkConnectDataFrame)" pandera/api/pyspark/types.py` returns 1. `grep -c "table_types = \[PySparkSQLDataFrame\]" pandera/api/pyspark/types.py` returns 1 (the initial list literal is preserved). Running `python -c "from pandera.api.pyspark.types import supported_types; t = supported_types().table_types; assert t.count(__import__('pyspark.sql', fromlist=['DataFrame']).DataFrame) <= 2, t"` exits 0 (PySparkSQLDataFrame appears at most twice — once from the literal, possibly once from the connect-alias fallback on older pyspark; the buggy triple-occurrence is eliminated).
  </done>
</task>

</tasks>

<verification>
1. CI workflow comment: `grep -B2 "exclude:$" .github/workflows/ci-tests.yml | grep -i "max\|support"` finds the comment above the pyspark exclude block.

2. Backend-neutral message (both backends) and ibis test xfail removal:
   ```
   PANDERA_USE_NARWHALS_BACKEND=True pytest tests/ibis/test_ibis_container.py::test_column_absent_error -v
   pytest tests/ibis/test_ibis_container.py::test_column_absent_error -v
   ```
   Both exit 0 with PASSED outcome (same `match="column 'int_col' not found"` regex passes under both backends because BOTH `pandera/backends/narwhals/container.py` and `pandera/backends/ibis/container.py` now emit `not found`).

3. Narwhals registration test:
   ```
   JAVA_HOME=... PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/test_pyspark_narwhals_register.py -v
   ```
   Exits 0; all four tests pass; `test_pyspark_narwhals_activated_when_opted_in` asserts on at least three distinct backend types.

4. Stacked xfail collapsed:
   ```
   grep -B5 "def test_registered_dataframemodel_checks" tests/pyspark/test_pyspark_model.py | grep -c "@pytest.mark.xfail"
   ```
   Returns exactly 1.

5. supported_types() fix:
   ```
   python -c "from pandera.api.pyspark.types import supported_types; print(supported_types().table_types)"
   ```
   Prints either `(<class 'pyspark.sql.dataframe.DataFrame'>,)` or `(<class 'pyspark.sql.dataframe.DataFrame'>, <class 'pyspark.sql.connect.dataframe.DataFrame'>)` — never a tuple with three entries where the first two are both `pyspark.sql.dataframe.DataFrame`.

6. Full pyspark + narwhals suite still green:
   ```
   JAVA_HOME=... PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/ tests/ibis/test_ibis_container.py -q
   ```
   Exits 0 (no regressions from the seven edits).
</verification>

<success_criteria>
- NITS-01 fully satisfied across all five sub-items.
- Every previously-xfailed `test_column_absent_error` instance now PASSES under both native ibis AND narwhals (the wording mismatch is the only blocker that warranted the xfail; the two-file message change resolves it).
- The narwhals registration test reflects the actual contract of `register_pyspark_backends()` (all three backends, not just one).
- The stacked-xfail bug in `test_registered_dataframemodel_checks` is removed (pytest no longer silently drops the narwhals decorator).
- `supported_types()` no longer double-appends `PySparkSQLDataFrame`; downstream callers using `isinstance` are unaffected, but the tuple contents are now correct.
- The CI workflow comment makes the pyspark+3.12/3.13 exclusion self-explanatory for future maintainers.
- All seven edits are coupled only at the narwhals-and-ibis pair (Task 2); the other four tasks touch distinct files with no cross-task ordering required.
</success_criteria>

<output>
Create `.planning/phases/06-test-coverage-and-minor-fixes/06-02-SUMMARY.md` when done
</output>
