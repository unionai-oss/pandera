---
phase: 10-pr-review-response-documentation-and-code-comment-fixes-from
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pandera/backends/narwhals/container.py
  - pandera/api/narwhals/utils.py
autonomous: true
requirements:
  - COMMENT-L1
  - COMMENT-L2
must_haves:
  truths:
    - "A future maintainer reading the `is_pyspark` dispatch in `pandera/backends/narwhals/container.py` understands WHY the PySpark branch cannot be abstracted via `_is_sql_lazy()` — because PySpark uses an accessor-error protocol while ibis raises `SchemaErrors`, even though both are SQL-lazy (L1)"
    - "A future maintainer reading `_materialize()` in `pandera/api/narwhals/utils.py` understands the `nw.DataFrame` PySpark branch is effectively dead code because PySpark frames are always lazified by `_to_lazy_nw()` in `container.py` before `_materialize()` is called, and the branch is kept as a defensive fallback (L2)"
  artifacts:
    - path: "pandera/backends/narwhals/container.py"
      provides: "Inline comment at the `is_pyspark` definition (lines 102-105) AND at the `elif is_pyspark:` error-path dispatch (lines 231-234) explaining the accessor-protocol rationale and why `_is_sql_lazy()` cannot replace this check"
      contains: "accessor"
    - path: "pandera/api/narwhals/utils.py"
      provides: "Docstring or inline comment on the `nw.DataFrame` PySpark branch of `_materialize()` (lines 55-75) explaining the branch is effectively dead code under the current `container.py` flow and is retained as a defensive fallback for direct callers"
      contains: "dead code"
  key_links:
    - from: "pandera/backends/narwhals/container.py is_pyspark dispatch (lines 102-105, 231-244)"
      to: "pandera/api/narwhals/utils.py::_is_sql_lazy"
      via: "Comment explicitly states why _is_sql_lazy cannot replace the check (ibis uses raise-SchemaErrors protocol; PySpark uses accessor protocol)"
      pattern: "_is_sql_lazy"
    - from: "pandera/api/narwhals/utils.py _materialize() nw.DataFrame PySpark branch (lines 55-75)"
      to: "pandera/backends/narwhals/container.py::_to_lazy_nw"
      via: "Comment names _to_lazy_nw as the function that lazifies PySpark frames before _materialize() sees them"
      pattern: "_to_lazy_nw"
---

<objective>
Add two code-comment clarifications flagged by PR review 4393093479:

1. L1 (COMMENT-L1): In `pandera/backends/narwhals/container.py`, the `check_lf.implementation in (nw.Implementation.PYSPARK, nw.Implementation.PYSPARK_CONNECT)` dispatch (assigned to `is_pyspark` at lines 102-105 and re-used at the error-path branch `elif is_pyspark:` at lines 231-234, plus the success-path `if is_pyspark:` at lines 242-245) MUST carry a comment explaining that the accessor-error protocol (`df.pandera.errors`) is PySpark-only and CANNOT be abstracted through the existing `_is_sql_lazy()` helper — because ibis is also SQL-lazy but uses the raise-`SchemaErrors` protocol. The existing docstring on `_handle_pyspark_validation_result` (lines 256-279) already covers this rationale; the gap is that there is no comment AT THE DISPATCH SITE itself, so a maintainer scanning `validate()` will not see why this is not `_is_sql_lazy(check_lf)`.

2. L2 (COMMENT-L2): In `pandera/api/narwhals/utils.py`, the `_materialize()` function's `nw.DataFrame` PySpark branch (lines 55-75, inside `if isinstance(frame, nw.DataFrame) and _is_sql_lazy(frame): ... if frame.implementation in (nw.Implementation.PYSPARK, ...):`) MUST carry a docstring or inline comment explaining that this branch is effectively dead code under the current container flow because `_to_lazy_nw()` in `pandera/backends/narwhals/container.py` (lines 45-50) eagerly converts PySpark `nw.DataFrame` instances to `nw.LazyFrame` before `_materialize()` is ever called. The branch is retained as a defensive fallback for direct callers (e.g. tests) but in production it will never fire — the `isinstance(frame, nw.LazyFrame)` branch (line 53-54) handles PySpark.

Purpose: Both comments are documentation-of-intent for future maintainers. Without L1, someone refactoring `validate()` will see "this looks like every other SQL-lazy backend, let me unify it with `_is_sql_lazy()`" and break the PySpark accessor contract. Without L2, someone reading `_materialize()` will think the PySpark `nw.DataFrame` branch is a live code path they need to test/maintain, when in fact it is only reachable via direct calls bypassing `_to_lazy_nw()`.

Output: Two modified source files with inline/docstring comments at the exact dispatch sites.
</objective>

<execution_context>
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Files being modified
@pandera/backends/narwhals/container.py
@pandera/api/narwhals/utils.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add accessor-protocol rationale comment to is_pyspark dispatch in container.py (COMMENT-L1)</name>
  <files>pandera/backends/narwhals/container.py</files>
  <read_first>
    - pandera/backends/narwhals/container.py (the `is_pyspark` assignment is at lines 102-105, the error-path dispatch `elif is_pyspark:` is at lines 231-234, the success-path `if is_pyspark:` is at lines 242-245, and the existing `_handle_pyspark_validation_result` docstring at lines 256-279 already explains the rationale — the new comment must summarize that rationale AT THE DISPATCH SITES so it is visible without jumping to the helper method)
    - pandera/api/narwhals/utils.py (to confirm what `_is_sql_lazy()` does and which implementations it covers — see `_SQL_LAZY_IMPLEMENTATIONS` at lines 10-18, which includes both IBIS and PYSPARK; this is the helper that the comment must explicitly say CANNOT be used here)
  </read_first>
  <action>
Edit `pandera/backends/narwhals/container.py`. Make THREE additions:

1. Above the `is_pyspark = check_lf.implementation in (...)` assignment at lines 102-105, insert an explanatory comment block. Replace the current 4-line assignment (lines 102-105):

```
        is_pyspark = check_lf.implementation in (
            nw.Implementation.PYSPARK,
            nw.Implementation.PYSPARK_CONNECT,
        )
```

with:

```
        # PySpark uses an accessor-error protocol (`df.pandera.errors`) for
        # surfacing validation results, while every other backend (including
        # the SQL-lazy ibis backend) raises `SchemaErrors`. This is a genuine
        # protocol difference, not a backend-capability difference, so it
        # cannot be abstracted via `_is_sql_lazy(check_lf)` — ibis is also
        # SQL-lazy but uses the raise-SchemaErrors protocol. See
        # `_handle_pyspark_validation_result` below for the full rationale.
        is_pyspark = check_lf.implementation in (
            nw.Implementation.PYSPARK,
            nw.Implementation.PYSPARK_CONNECT,
        )
```

2. Above the `elif is_pyspark:` line in the error-path block (currently at line 231), insert a single-line reminder comment immediately before the `elif`:

```
            # PySpark error path: attach errors to df.pandera.errors instead of
            # raising SchemaErrors (accessor protocol — see is_pyspark above).
            elif is_pyspark:
```

3. Above the `if is_pyspark:` line in the success-path block (currently at line 242), insert a single-line reminder comment immediately before the `if`:

```
        # PySpark success path: set df.pandera.errors = {} via accessor protocol;
        # other backends just return the parsed frame.
        if is_pyspark:
```

Do NOT modify the `is_pyspark` dispatch logic itself, the calls to `_handle_pyspark_validation_result`, or the existing docstring on `_handle_pyspark_validation_result`. The comments are pure documentation — no behavior changes.

Per COMMENT-L1 (decision from PR review 4393093479).
  </action>
  <verify>
    <automated>python -c "import ast, pathlib; ast.parse(pathlib.Path('pandera/backends/narwhals/container.py').read_text())" && grep -c 'accessor-error protocol' pandera/backends/narwhals/container.py | awk '$1 >= 1 { exit 0 } { exit 1 }' && grep -q '_is_sql_lazy' pandera/backends/narwhals/container.py && grep -q 'PySpark error path' pandera/backends/narwhals/container.py && grep -q 'PySpark success path' pandera/backends/narwhals/container.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File parses as valid Python: `python -c "import ast, pathlib; ast.parse(pathlib.Path('pandera/backends/narwhals/container.py').read_text())"` exits 0.
    - The literal substring `accessor-error protocol` appears at least once in `pandera/backends/narwhals/container.py` (the new comment block above `is_pyspark = ...`).
    - The literal substring `_is_sql_lazy` appears in `pandera/backends/narwhals/container.py` inside a comment (the rationale must name the helper it is rejecting). Specifically: `grep -B0 -A0 '_is_sql_lazy' pandera/backends/narwhals/container.py` shows at least one match on a line beginning with whitespace + `#`.
    - The literal substring `PySpark error path` appears once in `pandera/backends/narwhals/container.py` (the reminder comment above `elif is_pyspark:`).
    - The literal substring `PySpark success path` appears once in `pandera/backends/narwhals/container.py` (the reminder comment above `if is_pyspark:`).
    - The string `is_pyspark = check_lf.implementation in (` is preserved verbatim — no behavior change to the dispatch assignment.
    - The string `elif is_pyspark:` is preserved verbatim.
    - The string `if is_pyspark:` is preserved verbatim (with consistent indentation).
    - Behavioral parity: `PANDERA_USE_NARWHALS_BACKEND=True nox -s tests_narwhals_backend -- tests/pyspark/test_pyspark_accessor.py` exits 0 (or, if nox is not available in the sandbox, `python -c "import pandera.backends.narwhals.container"` exits 0 — the module imports cleanly).
    - No other functions or methods in the file are modified — `git diff pandera/backends/narwhals/container.py` only shows added comment lines, no changed code lines.
  </acceptance_criteria>
  <done>
    - Three comment additions land at the three dispatch sites: above `is_pyspark = ...` (block comment with rationale, names `_is_sql_lazy`), above `elif is_pyspark:` (error-path reminder), above `if is_pyspark:` (success-path reminder).
    - File still parses; PySpark tests still pass (where runnable in the sandbox).
    - No behavior change.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add dead-code clarification comment to _materialize() PySpark nw.DataFrame branch in utils.py (COMMENT-L2)</name>
  <files>pandera/api/narwhals/utils.py</files>
  <read_first>
    - pandera/api/narwhals/utils.py (the `_materialize()` function is at lines 43-80; the `nw.DataFrame` SQL-lazy branch is the `if isinstance(frame, nw.DataFrame) and _is_sql_lazy(frame):` block at lines 55-78; the PySpark sub-branch is lines 56-75)
    - pandera/backends/narwhals/container.py (the `_to_lazy_nw()` helper at lines 45-50 is what eagerly converts PySpark `nw.DataFrame` → `nw.LazyFrame` before `_materialize()` is called; this is the source of truth for "why the PySpark `nw.DataFrame` branch is dead code in production flow")
  </read_first>
  <action>
Edit `pandera/api/narwhals/utils.py`. The `_materialize()` function currently has a docstring at lines 43-52 that reads:

```
def _materialize(frame) -> nw.DataFrame:
    """Materialize a LazyFrame or SQL-lazy DataFrame to a Narwhals DataFrame.

    - nw.LazyFrame (Polars): call .collect()
    - nw.DataFrame wrapping a PySpark backend: use .first() for bounded
      single-row collect (the only context _materialize is called for PySpark),
      then wrap the result via pyarrow into a narwhals DataFrame.
    - nw.DataFrame wrapping another SQL-lazy backend (Ibis, DuckDB): call
      nw.to_native().execute() then wrap with nw.from_native()
    """
```

The "the only context _materialize is called for PySpark" clause in the existing docstring is stale — see L2 analysis. Update the docstring to make the dead-code status of the PySpark `nw.DataFrame` branch explicit. Replace the existing docstring with:

```
    """Materialize a LazyFrame or SQL-lazy DataFrame to a Narwhals DataFrame.

    - ``nw.LazyFrame`` (Polars, or PySpark after ``_to_lazy_nw``): call
      ``.collect()``.
    - ``nw.DataFrame`` wrapping another SQL-lazy backend (Ibis, DuckDB):
      call ``nw.to_native().execute()`` then wrap with ``nw.from_native()``.
    - ``nw.DataFrame`` wrapping a PySpark backend: defensive fallback only.
      In the production validation flow, ``_to_lazy_nw`` in
      ``pandera/backends/narwhals/container.py`` eagerly converts every
      incoming PySpark ``nw.DataFrame`` to ``nw.LazyFrame`` before
      ``_materialize`` is reached, so this branch is effectively dead code
      under ``DataFrameSchemaBackend.validate``. The branch is retained as a
      defensive fallback for direct callers (e.g. unit tests that construct
      a PySpark ``nw.DataFrame`` and call ``_materialize`` directly) and uses
      ``.first()`` for bounded single-row collection wrapped via PyArrow,
      because ``.execute()`` is absent on ``pyspark.sql.DataFrame``.
    """
```

Also add an inline `# Defensive fallback: see docstring — unreachable under DataFrameSchemaBackend.validate.` comment immediately inside the `if frame.implementation in (nw.Implementation.PYSPARK, nw.Implementation.PYSPARK_CONNECT):` block (currently line 56-59), placed right BEFORE the existing comment "PySpark: use .first() for bounded single-row collection." (line 60). So the resulting block looks like:

```
        if frame.implementation in (
            nw.Implementation.PYSPARK,
            nw.Implementation.PYSPARK_CONNECT,
        ):
            # Defensive fallback: see docstring — unreachable under
            # DataFrameSchemaBackend.validate because _to_lazy_nw converts
            # PySpark nw.DataFrame to nw.LazyFrame before _materialize runs.
            # PySpark: use .first() for bounded single-row collection.
            # .execute() is absent on pyspark.sql.DataFrame — it uses .collect()
            # for full frames, but we only need a single aggregated row here.
            import pyarrow as pa
```

Do NOT modify the actual implementation of the PySpark branch (the `import pyarrow as pa`, `native = nw.to_native(frame)`, `row = native.first()`, etc. — lines 63-75 — stay exactly as they are). The comment is pure documentation — no behavior change.

Per COMMENT-L2 (decision from PR review 4393093479).
  </action>
  <verify>
    <automated>python -c "import ast, pathlib; ast.parse(pathlib.Path('pandera/api/narwhals/utils.py').read_text())" && grep -q 'dead code' pandera/api/narwhals/utils.py && grep -q '_to_lazy_nw' pandera/api/narwhals/utils.py && grep -q 'Defensive fallback' pandera/api/narwhals/utils.py && python -c "from pandera.api.narwhals.utils import _materialize; print('ok')" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File parses as valid Python: `python -c "import ast, pathlib; ast.parse(pathlib.Path('pandera/api/narwhals/utils.py').read_text())"` exits 0.
    - The literal substring `dead code` appears in `pandera/api/narwhals/utils.py` inside the `_materialize` docstring.
    - The literal substring `_to_lazy_nw` appears in `pandera/api/narwhals/utils.py` (the docstring names the function that causes the branch to be unreachable). It did NOT appear before this task — `git diff pandera/api/narwhals/utils.py` shows it as an added line.
    - The literal substring `Defensive fallback` appears in `pandera/api/narwhals/utils.py` inside the `nw.DataFrame` PySpark sub-branch as an inline comment.
    - The existing implementation of the PySpark branch is preserved: `grep -q 'native.first()' pandera/api/narwhals/utils.py` succeeds, `grep -q 'import pyarrow as pa' pandera/api/narwhals/utils.py` succeeds, and `grep -q 'row.asDict()' pandera/api/narwhals/utils.py` succeeds.
    - The `_materialize` symbol still imports cleanly: `python -c "from pandera.api.narwhals.utils import _materialize; print('ok')"` exits 0.
    - No other functions in the file are modified — `git diff pandera/api/narwhals/utils.py` only shows changes to the `_materialize` docstring and the inline comment additions inside the PySpark sub-branch. No changes outside `_materialize`.
    - The existing docstring sentences about `.collect()` for Polars and `.execute()` + `nw.from_native()` for ibis/duckdb are preserved (possibly reworded, but semantically equivalent).
  </acceptance_criteria>
  <done>
    - `_materialize` docstring updated to call out the PySpark `nw.DataFrame` branch as a defensive fallback / dead code under `DataFrameSchemaBackend.validate`, and names `_to_lazy_nw` as the function that makes it unreachable.
    - Inline `Defensive fallback` comment placed immediately inside the PySpark sub-branch.
    - No behavior change — implementation lines for the PySpark branch are byte-identical.
    - Module imports cleanly.
  </done>
</task>

</tasks>

<verification>
- Both modified files parse as valid Python.
- `pandera/backends/narwhals/container.py` contains `accessor-error protocol`, `_is_sql_lazy` (in a comment), `PySpark error path`, and `PySpark success path` substrings.
- `pandera/api/narwhals/utils.py` contains `dead code`, `_to_lazy_nw`, and `Defensive fallback` substrings.
- `git diff` for both files shows only comment / docstring additions — no code lines changed.
- (Where runnable) `PANDERA_USE_NARWHALS_BACKEND=True nox -s tests_narwhals_backend` for the affected PySpark accessor test stays green.
</verification>

<success_criteria>
- COMMENT-L1: `pandera/backends/narwhals/container.py` `is_pyspark`/`is_pyspark_connect` dispatch branch carries a comment explaining the accessor-protocol is PySpark-only and cannot be abstracted via `_is_sql_lazy`.
- COMMENT-L2: `pandera/api/narwhals/utils.py` `_materialize()` docstring AND inline comment explain that the `nw.DataFrame` PySpark branch is effectively dead code because PySpark frames are always converted to `nw.LazyFrame` by `_to_lazy_nw` before `_materialize()` is called.
</success_criteria>

<output>
Create `.planning/phases/10-pr-review-response-documentation-and-code-comment-fixes-from/10-02-SUMMARY.md` when done.
</output>
