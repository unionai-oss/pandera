---
phase: 09-round-2-pr-review-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pandera/backends/narwhals/components.py
  - pandera/api/pyspark/types.py
  - pandera/backends/pyspark/register.py
  - pandera/accessors/pyspark_sql_accessor.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "ColumnBackend.validate() does not unconditionally convert nw.DataFrame to nw.LazyFrame for SQL-lazy frames (PySpark)"
    - "No `except Exception:` guards remain in the three production PySpark import sites"
    - "PySparkDtypeInputTypes Union has no duplicate `type` entry"
    - "supported_types() has no dead try/except ImportError block around the PySparkConnectDataFrame append"
  artifacts:
    - path: "pandera/backends/narwhals/components.py"
      provides: "ColumnBackend.validate guarded with _is_sql_lazy()"
      contains: "_is_sql_lazy(check_lf)"
    - path: "pandera/api/pyspark/types.py"
      provides: "ImportError-only guard for Spark Connect import; deduplicated PySparkDtypeInputTypes; no dead handler in supported_types()"
    - path: "pandera/backends/pyspark/register.py"
      provides: "ImportError-only guard for pyspark_connect import"
    - path: "pandera/accessors/pyspark_sql_accessor.py"
      provides: "ImportError-only guard for register_connect_dataframe_accessor"
  key_links:
    - from: "pandera/backends/narwhals/components.py:validate"
      to: "pandera/api/narwhals/utils.py:_is_sql_lazy"
      via: "already-imported helper from `from pandera.api.narwhals.utils import _is_sql_lazy`"
      pattern: "_is_sql_lazy\\(check_lf\\)"
---

<objective>
Fix the Round 2 PR review production-code findings: the ColumnBackend.validate()
lazy-conversion correctness bug (B-01), broad `except Exception:` guards in
three PySpark import paths (M-01), the duplicate `type` entry in
`PySparkDtypeInputTypes` (M-05), and the dead `try/except ImportError` wrapper
around the `PySparkConnectDataFrame` append in `supported_types()` (M-06).

Purpose: Restore correctness of PySpark validation (the unconditional `.lazy()`
collapses the `.first()` materialization path into `.collect()`, which can
crash or unbounded-materialize) and tighten import guards so genuine bugs are
not silently swallowed.

Output: Four production source files modified — one correctness fix, three
narrowed exception guards, one Union dedupe, one dead-code removal.
</objective>

<execution_context>
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@pandera/api/narwhals/utils.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Guard ColumnBackend.validate() .lazy() conversion with _is_sql_lazy</name>
  <files>pandera/backends/narwhals/components.py</files>
  <read_first>
    - pandera/backends/narwhals/components.py (lines 1-70 — see existing import of `_is_sql_lazy` on line 14 and the unconditional `.lazy()` at lines 67-69 inside `validate()`)
    - pandera/api/narwhals/utils.py (lines 1-50 — confirm `_is_sql_lazy` signature: returns False for non-nw frames, True for IBIS/DUCKDB/PYSPARK/PYSPARK_CONNECT/SQLFRAME)
    - pandera/backends/narwhals/container.py (grep for `_is_sql_lazy` to confirm the pattern used elsewhere in this codebase — replicate that calling convention)
  </read_first>
  <behavior>
    - Wrapping a PySpark DataFrame via `nw.from_native(check_obj, eager_or_interchange_only=False)` returns an `nw.DataFrame` whose implementation is `nw.Implementation.PYSPARK` (SQL-lazy).
    - After this fix: calling `ColumnBackend.validate(pyspark_df, schema)` must NOT convert that `nw.DataFrame` to a `nw.LazyFrame`. The frame stays an `nw.DataFrame` so downstream `_materialize()` uses the `.first()` bounded-row path instead of `.collect()`.
    - For a polars-backed `nw.DataFrame` (eager, NOT SQL-lazy), the `.lazy()` conversion must still happen — existing polars validation behavior is preserved.
    - For an input that is already an `nw.LazyFrame` (polars LazyFrame), no conversion happens (existing behavior preserved).
  </behavior>
  <action>
    In `pandera/backends/narwhals/components.py` inside `ColumnBackend.validate()`, replace the existing two-line block:

        check_lf = nw.from_native(check_obj, eager_or_interchange_only=False)
        if isinstance(check_lf, nw.DataFrame):
            check_lf = check_lf.lazy()

    with the guarded variant that delegates SQL-laziness detection to the already-imported `_is_sql_lazy` helper:

        check_lf = nw.from_native(check_obj, eager_or_interchange_only=False)
        if isinstance(check_lf, nw.DataFrame) and not _is_sql_lazy(check_lf):
            check_lf = check_lf.lazy()

    Do NOT add a new import — `_is_sql_lazy` is already imported on line 14 from `pandera.api.narwhals.utils`. Do not change any other logic in `validate()`. Do not touch the regex / non-regex branches below this guard. Do not touch other methods in this file.
  </action>
  <verify>
    <automated>grep -n "_is_sql_lazy(check_lf)" pandera/backends/narwhals/components.py | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'and not _is_sql_lazy(check_lf)' pandera/backends/narwhals/components.py` returns 1
    - The exact phrase `check_lf = check_lf.lazy()` appears in the file exactly once, on the line guarded by the new `and not _is_sql_lazy(check_lf)` condition
    - No new `import` line was added (the file's import block from lines 1-29 is unchanged)
    - Running `PANDERA_USE_NARWHALS_BACKEND=True python -c "import pandera.backends.narwhals.components"` completes without error
  </acceptance_criteria>
  <done>The `.lazy()` conversion in `ColumnBackend.validate()` is guarded by `_is_sql_lazy(check_lf)` so PySpark-wrapped `nw.DataFrame` instances stay as DataFrame and use the `.first()` path inside `_materialize()`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Narrow three `except Exception:` guards to `except ImportError:`</name>
  <files>pandera/api/pyspark/types.py, pandera/backends/pyspark/register.py, pandera/accessors/pyspark_sql_accessor.py</files>
  <read_first>
    - pandera/api/pyspark/types.py (lines 16-34 — Spark Connect import block with `except Exception:` at line 23)
    - pandera/backends/pyspark/register.py (lines 10-19 — `pyspark_connect` import with `except Exception:` at line 16; note the comment about `grpcio-status` — that surfaces as ImportError)
    - pandera/accessors/pyspark_sql_accessor.py (lines 154-162 — `register_connect_dataframe_accessor` call with `except Exception:` at line 159)
  </read_first>
  <action>
    Make exactly three single-token edits — change the word `Exception` to `ImportError` in the three guard clauses:

    1. `pandera/api/pyspark/types.py` line 23: replace `    except Exception:` with `    except ImportError:`. Leave the surrounding comment (`# grpcio-status or other Spark Connect deps not installed`) unchanged.

    2. `pandera/backends/pyspark/register.py` line 16: replace `    except Exception:` with `    except ImportError:`. Leave the surrounding comment unchanged.

    3. `pandera/accessors/pyspark_sql_accessor.py` line 159: replace `    except Exception:` with `    except ImportError:`. Leave the surrounding comment unchanged.

    Do NOT also add `AttributeError`. The comments in all three sites cite missing `grpcio-status`/Spark Connect deps, which raise `ImportError` at the indicated import lines. If a future version raises `AttributeError`, that should be a separate intentional addition with its own justification — not a speculative widening here.
  </action>
  <verify>
    <automated>grep -n "except Exception" pandera/api/pyspark/types.py pandera/backends/pyspark/register.py pandera/accessors/pyspark_sql_accessor.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "except Exception" pandera/api/pyspark/types.py` returns 0
    - `grep -c "except Exception" pandera/backends/pyspark/register.py` returns 0
    - `grep -c "except Exception" pandera/accessors/pyspark_sql_accessor.py` returns 0
    - `grep -c "except ImportError:" pandera/api/pyspark/types.py` is >= 1 (the new guard)
    - `grep -c "except ImportError:" pandera/backends/pyspark/register.py` returns 1
    - `grep -c "except ImportError:" pandera/accessors/pyspark_sql_accessor.py` returns 1
    - `python -c "import pandera.api.pyspark.types; import pandera.backends.pyspark.register; import pandera.accessors.pyspark_sql_accessor"` completes without error
  </acceptance_criteria>
  <done>All three production PySpark/Spark-Connect import guards catch `ImportError` only; bare `except Exception:` is gone from those three files.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Dedupe `type` in PySparkDtypeInputTypes and remove dead ImportError block in supported_types()</name>
  <files>pandera/api/pyspark/types.py</files>
  <read_first>
    - pandera/api/pyspark/types.py (full file — particularly lines 64-84 for the `PySparkDtypeInputTypes` Union and lines 98-112 for `supported_types()`)
  </read_first>
  <action>
    Two edits inside `pandera/api/pyspark/types.py`:

    Edit A — `PySparkDtypeInputTypes` Union dedupe:
    The Union spans roughly lines 64-84 and contains `type,` on BOTH line 69 and line 71. Remove the SECOND occurrence (line 71, the one immediately preceding `pst.BooleanType,`). The Union must retain `type,` exactly once (the original at line 69 stays). Do not reorder any other entry.

    Edit B — Remove dead handler in `supported_types()`:
    Replace the body of `supported_types()` that currently reads:

        # pylint: disable=import-outside-toplevel
        table_types = [PySparkSQLDataFrame]

        try:
            table_types.append(PySparkConnectDataFrame)

        except ImportError:  # pragma: no cover
            pass

        return SupportedTypes(
            tuple(table_types),
        )

    with the simplified body (no try/except, no `# pragma: no cover` line, no blank line between `append` and `return`):

        # pylint: disable=import-outside-toplevel
        table_types = [PySparkSQLDataFrame, PySparkConnectDataFrame]
        return SupportedTypes(
            tuple(table_types),
        )

    Justification: `PySparkConnectDataFrame` is bound unconditionally at module top (lines 17-33) — either to the Spark Connect type or a fallback to `pyspark.sql.DataFrame`. The `append(PySparkConnectDataFrame)` call cannot raise `ImportError`. Keep `# pylint: disable=import-outside-toplevel` because it documents intent for the file as a whole.
  </action>
  <verify>
    <automated>grep -cE "^\s*type,\s*$" pandera/api/pyspark/types.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^\s*type,\s*$" pandera/api/pyspark/types.py` returns 1 (only one `type,` line in the Union)
    - `grep -c "except ImportError" pandera/api/pyspark/types.py` returns 1 (only the narrowed guard from Task 2 remains; the dead `# pragma: no cover` one is gone)
    - `grep -c "# pragma: no cover" pandera/api/pyspark/types.py` returns 0
    - `grep -n "supported_types" pandera/api/pyspark/types.py` shows the function still exists
    - `python -c "from pandera.api.pyspark.types import PySparkDtypeInputTypes, supported_types; print(supported_types())"` runs without error and prints a `SupportedTypes(table_types=(...))` value containing both `PySparkSQLDataFrame` and `PySparkConnectDataFrame` (or the fallback `pyspark.sql.DataFrame`)
  </acceptance_criteria>
  <done>`PySparkDtypeInputTypes` lists `type` exactly once; `supported_types()` calls `table_types.append(PySparkConnectDataFrame)` (or list-literal includes it) unconditionally with no try/except wrapper and no `# pragma: no cover` annotation on that path.</done>
</task>

</tasks>

<verification>
After all three tasks complete:

1. `grep -n "except Exception" pandera/api/pyspark/types.py pandera/backends/pyspark/register.py pandera/accessors/pyspark_sql_accessor.py` returns no matches (exit code 1).
2. `grep -n "and not _is_sql_lazy(check_lf)" pandera/backends/narwhals/components.py` returns exactly one match.
3. `grep -cE "^\s*type,\s*$" pandera/api/pyspark/types.py` returns 1.
4. `grep -c "# pragma: no cover" pandera/api/pyspark/types.py` returns 0.
5. `PANDERA_USE_NARWHALS_BACKEND=True python -c "import pandera"` runs without ImportError.
6. PySpark tests still load: `PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/test_pyspark_dtypes.py --collect-only -q` exits 0.
</verification>

<success_criteria>
- B-01 resolved: `.lazy()` in `ColumnBackend.validate()` is guarded by `_is_sql_lazy`.
- M-01 resolved: three `except Exception:` clauses are now `except ImportError:`.
- M-05 resolved: `PySparkDtypeInputTypes` has a single `type,` entry.
- M-06 resolved: the dead `try/except ImportError` around `PySparkConnectDataFrame` append is removed.
- All four production files import cleanly under both `PANDERA_USE_NARWHALS_BACKEND=True` and `False`.
</success_criteria>

<output>
Create `.planning/phases/09-round-2-pr-review-fixes/09-01-SUMMARY.md` when done, recording each finding (B-01, M-01, M-05, M-06) and the file:line of the fix.
</output>
