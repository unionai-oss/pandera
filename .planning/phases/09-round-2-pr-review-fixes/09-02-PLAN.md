---
phase: 09-round-2-pr-review-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - tests/pyspark/test_pyspark_dtypes.py
  - tests/pyspark/conftest.py
  - tests/narwhals/test_e2e.py
  - tests/narwhals/test_arch03_schema_driven_dispatch.py
  - tests/narwhals/conftest.py
  - tests/pyspark/test_pyspark_config.py
  - tests/pyspark/test_pyspark_decorators.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "No `if CONFIG.use_narwhals_backend:` inline branches remain in test_pyspark_dtypes.py"
    - "tests/narwhals/conftest.py owns both `_spark_env_vars` (autouse) and `spark` session fixtures; the two test modules no longer define them"
    - "TestPanderaConfig._cmp_errors static wrapper is gone; all callers use the module-level _cmp_errors from tests/pyspark/conftest.py"
    - "test_pyspark_decorators.py no longer calls pytest.xfail() inline; the cache_enabled+use_narwhals_backend skip is expressed as a parametrize-level mark"
  artifacts:
    - path: "tests/narwhals/conftest.py"
      provides: "Shared _spark_env_vars autouse fixture and `spark` session fixture for tests/narwhals/"
      contains: "def _spark_env_vars"
    - path: "tests/pyspark/test_pyspark_dtypes.py"
      provides: "Dtype tests free of inline use_narwhals_backend branching"
    - path: "tests/pyspark/test_pyspark_config.py"
      provides: "Test class that calls module-level _cmp_errors directly"
    - path: "tests/pyspark/test_pyspark_decorators.py"
      provides: "Parametrize-level xfail mark for the cache_enabled+narwhals combination"
  key_links:
    - from: "tests/narwhals/test_e2e.py"
      to: "tests/narwhals/conftest.py"
      via: "pytest auto-discovery of conftest fixtures"
      pattern: "spark fixture used in tests, no `def spark` definition in test file"
    - from: "tests/pyspark/test_pyspark_config.py"
      to: "tests/pyspark/conftest.py:_cmp_errors"
      via: "module-level import `from tests.pyspark.conftest import _cmp_errors`"
      pattern: "_cmp_errors\\("
---

<objective>
Resolve the Round 2 PR review test-only findings: remove inline
`if CONFIG.use_narwhals_backend:` branching in `test_pyspark_dtypes.py` (M-02),
consolidate the duplicated `_spark_env_vars`/`spark` fixtures in
`tests/narwhals/` into the shared conftest (M-03), drop the redundant
`TestPanderaConfig._cmp_errors` static wrapper (M-04), and convert the inline
`pytest.xfail()` call in `test_pyspark_decorators.py` to a parametrize-level
mark (M-07).

Purpose: Improve test-suite hygiene per project conventions
(`feedback_pytest_style.md`, `feedback_pytest_backend_fixtures.md`,
`feedback_naming_conventions.md`) — visible skip reasons, deduplicated
fixtures, fewer inline branches, no inline `pytest.xfail()` calls.

Output: Seven test files modified — four cleanup fixes, all independent of
Plan 01 production changes (Plan 01 listed as depends_on for execution
ordering only, not because tasks here reference Plan 01 artifacts).
</objective>

<execution_context>
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace inline use_narwhals_backend branch in test_pyspark_dtypes.py with pytest.mark.skipif on validate_data, and drop the inner narwhals branch from validate_datatype</name>
  <files>tests/pyspark/test_pyspark_dtypes.py</files>
  <read_first>
    - tests/pyspark/test_pyspark_dtypes.py (full file — particularly lines 1-90 for the BaseClass `validate_datatype` and `validate_data` methods and the `if CONFIG.use_narwhals_backend:` branches at lines 31 and 78)
    - tests/pyspark/conftest.py (search for `def spark_df` and `verifySchema` — confirm whether the fixture sets `verifySchema=False`)
  </read_first>
  <action>
    The two inline branches in `BaseClass` mask narwhals-backend-incompatible test cases. Both stem from the shared `spark_df` fixture using `verifySchema=False`, which lets test data have a different column-count than the schema. Replacing the fixture is out of scope (it affects too many tests). Use pytest markers to make the skip visible.

    Edit A — `validate_datatype` (around lines 25-35):
    Currently:

        def validate_datatype(self, df, pandera_schema):
            df_out = pandera_schema(df, lazy=True)
            if not CONFIG.use_narwhals_backend:
                assert df.pandera.schema == pandera_schema
            assert isinstance(pandera_schema.validate(df, lazy=True), DataFrame)
            assert isinstance(df_out, DataFrame)
            return df_out

    Replace with:

        def validate_datatype(self, df, pandera_schema):
            df_out = pandera_schema(df, lazy=True)
            assert df.pandera.schema == pandera_schema
            assert isinstance(pandera_schema.validate(df, lazy=True), DataFrame)
            assert isinstance(df_out, DataFrame)
            return df_out

    Then add a class-level pytest mark to each test class whose tests call `validate_datatype` via `validate_data`. Use a single `pytestmark` module-level constant at the top of the file (after imports), and apply it via `pytest.mark.skipif` so the whole module skips under narwhals:

        pytestmark = pytest.mark.skipif(
            CONFIG.use_narwhals_backend,
            reason=(
                "tests/pyspark/conftest.py::spark_df uses verifySchema=False, which "
                "yields multi-value rows for single-column schemas. Under the "
                "narwhals backend, validation calls .first() on a STRUCT-typed row "
                "and PySpark raises STRUCT_ARRAY_LENGTH_MISMATCH. Fixing this "
                "requires a fixture refactor that is out of scope for this PR — "
                "see Phase 4 ARCH-03 notes. Also restores the df.pandera.schema "
                "assertion that was previously hidden behind a CONFIG branch."
            ),
        )

    Edit B — `validate_data` (around lines 50-89):
    Remove the inline `if CONFIG.use_narwhals_backend:` block (currently lines ~78-81 which create an empty single-column DataFrame) AND remove the long explanatory comment block (currently lines ~61-77). The module-level `pytestmark` above replaces both. The final method body should be:

        def validate_data(
            self, df, pandera_equivalent, column_name, return_error=False
        ):
            """
            This function runs the actual validation of object on the dataframe
            """
            pandera_schema = DataFrameSchema(
                columns={
                    column_name: Column(pandera_equivalent),
                },
            )
            df_out = self.validate_datatype(df, pandera_schema)
            if df_out.pandera.errors:
                if return_error:
                    return df_out.pandera.errors
                else:
                    print(df_out.pandera.errors)
                    assert False

    Confirm `pytest` is already imported at the top of the file. If `CONFIG` is no longer referenced anywhere else in the file after edits, leave the import in place — it is used by `pytestmark`.

    Do NOT touch the `params` dicts of subclass test classes. Do NOT modify `tests/pyspark/conftest.py::spark_df` — fixture refactor is explicitly out of scope.
  </action>
  <verify>
    <automated>grep -nE "if (not )?CONFIG.use_narwhals_backend" tests/pyspark/test_pyspark_dtypes.py | grep -v '^[^:]*:[0-9]*:#'; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "if (not )?CONFIG.use_narwhals_backend" tests/pyspark/test_pyspark_dtypes.py` returns 0 when comments are excluded: `grep -vE '^\s*#' tests/pyspark/test_pyspark_dtypes.py | grep -cE "if (not )?CONFIG.use_narwhals_backend"` returns 0
    - `grep -c "pytestmark" tests/pyspark/test_pyspark_dtypes.py` is >= 1
    - `grep -c "pytest.mark.skipif" tests/pyspark/test_pyspark_dtypes.py` is >= 1
    - The line `assert df.pandera.schema == pandera_schema` appears exactly once in the file and is NOT guarded by any `if`
    - `PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/test_pyspark_dtypes.py --collect-only -q` exits 0 and reports the module-level skip (count of skipped > 0)
    - `pytest tests/pyspark/test_pyspark_dtypes.py --collect-only -q` (native PySpark backend) exits 0 and collects tests normally (no skip)
  </acceptance_criteria>
  <done>The dtypes test file has no inline `if CONFIG.use_narwhals_backend:` branches; skip behavior under narwhals is expressed via a module-level `pytestmark = pytest.mark.skipif(...)` with the documented reason, and the `df.pandera.schema == pandera_schema` assertion runs unconditionally on the native backend.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Move duplicated _spark_env_vars and spark fixtures into tests/narwhals/conftest.py</name>
  <files>tests/narwhals/conftest.py, tests/narwhals/test_e2e.py, tests/narwhals/test_arch03_schema_driven_dispatch.py</files>
  <read_first>
    - tests/narwhals/conftest.py (full file — confirm current structure and existing fixtures, especially what scope and import patterns are used)
    - tests/narwhals/test_e2e.py (lines 700-750 — the `_spark_env_vars` autouse fixture at line 707 and `spark` fixture at line 727)
    - tests/narwhals/test_arch03_schema_driven_dispatch.py (lines 35-85 — the duplicate `_spark_env_vars` at line 41 and `spark` at line 61, plus any comment acknowledging the copy)
  </read_first>
  <action>
    Three-file edit.

    Edit A — append fixtures to `tests/narwhals/conftest.py`:

    Read the EXACT body of `_spark_env_vars` (autouse) and `spark` from `tests/narwhals/test_e2e.py` (lines 706-744 approximately — read the full fixture bodies including imports they rely on). Copy both fixtures verbatim into `tests/narwhals/conftest.py`, appended at end of file. Preserve:
    - the `@pytest.fixture(autouse=True, scope="function")` decorator on `_spark_env_vars`
    - the `@pytest.fixture(scope="module")` decorator on `spark`
    - any `HAS_PYSPARK` import / module-level guard the fixtures depend on
    - the yield-on-early-exit fix from Phase 7 (`yield` on the `HAS_PYSPARK=False` path) — DO NOT regress this

    If `tests/narwhals/conftest.py` does not already import `pytest`, `os`, or `HAS_PYSPARK`, add the imports it needs at the top of the file.

    Edit B — delete `_spark_env_vars` and `spark` fixture definitions from `tests/narwhals/test_e2e.py`:

    Remove the fixture definitions (decorator + function body) for both `_spark_env_vars` (around line 707) and `spark` (around line 727). Leave all OTHER fixtures in the file alone (notably the test-module-specific data fixtures around lines 85-105 and 745). Tests in the file that use `spark` as an argument keep working via conftest auto-discovery — no test-body changes needed.

    Edit C — delete `_spark_env_vars` and `spark` fixture definitions from `tests/narwhals/test_arch03_schema_driven_dispatch.py`:

    Same operation as Edit B for this file. Remove the fixture definitions at lines ~40-65. If the file has a comment acknowledging the copy (something like "duplicated from test_e2e — TODO consolidate"), remove that comment as well.

    Verification step inside this task: after edits, `pytest tests/narwhals/test_arch03_schema_driven_dispatch.py::TestSchemaDispatch --collect-only -q` should still discover all tests that previously used `spark` (no fixture-not-found errors).

    Do NOT change fixture scopes (`function` for env vars, `module` for spark session). Do NOT change the fixture names. Do NOT prefix `spark` with underscore — `feedback_naming_conventions.md` discourages underscore prefix.
  </action>
  <verify>
    <automated>{ grep -nE "^def _spark_env_vars|^def spark\b" tests/narwhals/test_e2e.py tests/narwhals/test_arch03_schema_driven_dispatch.py && echo "FAIL: still defined"; } || grep -nE "^def _spark_env_vars|^def spark\b" tests/narwhals/conftest.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^def _spark_env_vars" tests/narwhals/test_e2e.py` returns 0
    - `grep -cE "^def spark\b" tests/narwhals/test_e2e.py` returns 0
    - `grep -cE "^def _spark_env_vars" tests/narwhals/test_arch03_schema_driven_dispatch.py` returns 0
    - `grep -cE "^def spark\b" tests/narwhals/test_arch03_schema_driven_dispatch.py` returns 0
    - `grep -cE "^def _spark_env_vars" tests/narwhals/conftest.py` returns 1
    - `grep -cE "^def spark\b" tests/narwhals/conftest.py` returns 1
    - `grep -c "@pytest.fixture(autouse=True, scope=\"function\")" tests/narwhals/conftest.py` is >= 1 (the env-vars decorator)
    - `grep -c "@pytest.fixture(scope=\"module\")" tests/narwhals/conftest.py` is >= 1 (the spark decorator)
    - The conftest copy of `_spark_env_vars` contains a `yield` keyword on its early-exit branch (regression guard from Phase 7): `awk '/def _spark_env_vars/,/^def [a-z]/' tests/narwhals/conftest.py | grep -c "yield"` >= 2 (yield in the early-exit path AND yield after the env setup) OR equivalent assertion that both code paths yield
    - `PANDERA_USE_NARWHALS_BACKEND=True pytest tests/narwhals/test_e2e.py tests/narwhals/test_arch03_schema_driven_dispatch.py --collect-only -q` exits 0
  </acceptance_criteria>
  <done>Both `_spark_env_vars` and `spark` fixtures live exclusively in `tests/narwhals/conftest.py`; the two test modules no longer define them and rely on conftest auto-discovery.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Remove TestPanderaConfig._cmp_errors wrapper; call module-level _cmp_errors directly</name>
  <files>tests/pyspark/test_pyspark_config.py</files>
  <read_first>
    - tests/pyspark/test_pyspark_config.py (lines 1-50 for imports + class header, and lines 30-40 for the `_cmp_errors` static method; also scan all `self._cmp_errors(` call sites at lines 113, 145, 192, 225, 281, 285, 323, 327)
    - tests/pyspark/conftest.py (line 194 — confirm the module-level `_cmp_errors(actual, expected)` signature exactly matches what the static wrapper forwards)
  </read_first>
  <action>
    Edit A — delete the static method.
    In `tests/pyspark/test_pyspark_config.py`, find the `_cmp_errors` definition inside `TestPanderaConfig`:

        @staticmethod
        def _cmp_errors(actual, expected):
            \"\"\"Delegates to module-level _cmp_errors in conftest.\"\"\"
            _cmp_errors(actual, expected)

    (decorator + def + docstring + body — roughly 4-6 lines around line 33-37). Delete the entire block including the `@staticmethod` decorator. Do not delete the surrounding methods.

    Edit B — convert all `self._cmp_errors(` call sites to bare `_cmp_errors(`:
    At each of these line numbers (approximate, may shift after Edit A): 113, 145, 192, 225, 281, 285, 323, 327, replace `self._cmp_errors(` with `_cmp_errors(`. Use a careful find-and-replace constrained to lines inside `class TestPanderaConfig`. The argument lists and any multi-line formatting must remain untouched.

    Confirm the existing import at line 21 already brings `_cmp_errors` into module scope:

        from tests.pyspark.conftest import _cmp_errors, spark_df

    If for any reason the import is missing, add it. Do NOT alias the import — `feedback_pytest_style.md` discourages aliases for this pattern.
  </action>
  <verify>
    <automated>{ grep -c "def _cmp_errors" tests/pyspark/test_pyspark_config.py; grep -c "self._cmp_errors" tests/pyspark/test_pyspark_config.py; grep -c "^from tests.pyspark.conftest import _cmp_errors" tests/pyspark/test_pyspark_config.py; }</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def _cmp_errors" tests/pyspark/test_pyspark_config.py` returns 0
    - `grep -c "@staticmethod" tests/pyspark/test_pyspark_config.py` decreases by exactly 1 compared to the pre-edit count (other staticmethods, if any, are preserved)
    - `grep -c "self._cmp_errors" tests/pyspark/test_pyspark_config.py` returns 0
    - `grep -cE "(^|\s)_cmp_errors\(" tests/pyspark/test_pyspark_config.py` returns at least 8 (the 8 former call sites, all now bare calls)
    - The line `from tests.pyspark.conftest import _cmp_errors, spark_df` remains present
    - `pytest tests/pyspark/test_pyspark_config.py --collect-only -q` exits 0
  </acceptance_criteria>
  <done>`TestPanderaConfig` no longer defines `_cmp_errors`; every former `self._cmp_errors(...)` call site now invokes the module-level `_cmp_errors` from `tests/pyspark/conftest.py` directly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Convert inline pytest.xfail() to parametrize-level pytest.mark.xfail in test_pyspark_decorators.py</name>
  <files>tests/pyspark/test_pyspark_decorators.py</files>
  <read_first>
    - tests/pyspark/test_pyspark_decorators.py (lines 60-120 — the parametrize block at line 67 and the inline `pytest.xfail()` at line 91 inside `test_cache_dataframe_settings`)
    - `.claude/projects/-Users-deepyaman-github-unionai-oss-pandera/memory/feedback_pytest_style.md` (if accessible) — confirms use of `contextlib.nullcontext` + parametrize for conditional behavior; here the analog is `pytest.param(..., marks=...)` for conditional xfail
  </read_first>
  <action>
    The current parametrize block is:

        @pytest.mark.parametrize(
            "cache_enabled,keep_cache_enabled,"
            "expected_caching_message,expected_unpersisting_message",
            [
                (True, True, True, None),
                (True, False, True, True),
                (False, True, None, None),
                (False, False, None, None),
            ],
            scope="function",
        )

    And inside the test body (around line 90-94):

        if CONFIG.use_narwhals_backend and cache_enabled:
            pytest.xfail(
                "narwhals backend does not use PySpark caching decorators; "
                "cache/unpersist log messages are not emitted"
            )

    Edit A — replace the parametrize value list with `pytest.param(...)` entries that carry a conditional xfail mark for the two `cache_enabled=True` cases:

        @pytest.mark.parametrize(
            "cache_enabled,keep_cache_enabled,"
            "expected_caching_message,expected_unpersisting_message",
            [
                pytest.param(
                    True,
                    True,
                    True,
                    None,
                    marks=pytest.mark.xfail(
                        CONFIG.use_narwhals_backend,
                        reason=(
                            "narwhals backend does not use PySpark caching "
                            "decorators; cache/unpersist log messages are not "
                            "emitted"
                        ),
                        strict=False,
                    ),
                ),
                pytest.param(
                    True,
                    False,
                    True,
                    True,
                    marks=pytest.mark.xfail(
                        CONFIG.use_narwhals_backend,
                        reason=(
                            "narwhals backend does not use PySpark caching "
                            "decorators; cache/unpersist log messages are not "
                            "emitted"
                        ),
                        strict=False,
                    ),
                ),
                (False, True, None, None),
                (False, False, None, None),
            ],
            scope="function",
        )

    Edit B — delete the inline `if CONFIG.use_narwhals_backend and cache_enabled:` block and its `pytest.xfail(...)` call (the 4-5 lines around line 90-94). Leave the rest of the test body unchanged.

    Use `strict=False` to mirror the prior runtime `pytest.xfail()` semantics (which is non-strict — the test is allowed to pass under non-narwhals). If the test currently always fails for narwhals + cache_enabled, `strict=True` is preferable; default to `strict=False` to be safe and let a follow-up tighten it.

    Confirm `pytest` and `CONFIG` are already imported at the top of the file (they are — `pytest.xfail` and `CONFIG.use_narwhals_backend` are both already referenced). Do not add new imports.
  </action>
  <verify>
    <automated>{ grep -n "pytest.xfail(" tests/pyspark/test_pyspark_decorators.py; echo "---"; grep -c "pytest.mark.xfail(" tests/pyspark/test_pyspark_decorators.py; }</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pytest.xfail(" tests/pyspark/test_pyspark_decorators.py` returns 0 (no inline xfail call)
    - `grep -c "pytest.param(" tests/pyspark/test_pyspark_decorators.py` is >= 2 (the two new parametrize entries with marks)
    - `grep -c "pytest.mark.xfail(" tests/pyspark/test_pyspark_decorators.py` is >= 2 (one per `pytest.param` for the True/True and True/False combinations)
    - The test body for `test_cache_dataframe_settings` no longer contains `if CONFIG.use_narwhals_backend and cache_enabled:` — `grep -c "CONFIG.use_narwhals_backend and cache_enabled" tests/pyspark/test_pyspark_decorators.py` returns 0
    - `pytest tests/pyspark/test_pyspark_decorators.py::TestDecorators::test_cache_dataframe_settings --collect-only -q` exits 0 and shows 4 parametrized cases collected
  </acceptance_criteria>
  <done>`test_cache_dataframe_settings` uses `pytest.param(..., marks=pytest.mark.xfail(CONFIG.use_narwhals_backend, ...))` on the two `cache_enabled=True` parametrize entries; no inline `pytest.xfail()` call remains in the test body.</done>
</task>

</tasks>

<verification>
After all four tasks complete:

1. `grep -vE '^\s*#' tests/pyspark/test_pyspark_dtypes.py | grep -cE "if (not )?CONFIG.use_narwhals_backend"` returns 0.
2. `grep -nE "^def _spark_env_vars|^def spark\b" tests/narwhals/test_e2e.py tests/narwhals/test_arch03_schema_driven_dispatch.py` returns nothing; the same grep against `tests/narwhals/conftest.py` returns 2 matches.
3. `grep -c "def _cmp_errors" tests/pyspark/test_pyspark_config.py` returns 0; `grep -c "self._cmp_errors" tests/pyspark/test_pyspark_config.py` returns 0.
4. `grep -c "pytest.xfail(" tests/pyspark/test_pyspark_decorators.py` returns 0.
5. Collection passes for all touched files under both backend modes:
   - `pytest tests/pyspark/test_pyspark_dtypes.py tests/pyspark/test_pyspark_config.py tests/pyspark/test_pyspark_decorators.py tests/narwhals/test_e2e.py tests/narwhals/test_arch03_schema_driven_dispatch.py --collect-only -q`
   - `PANDERA_USE_NARWHALS_BACKEND=True pytest tests/pyspark/test_pyspark_dtypes.py tests/pyspark/test_pyspark_config.py tests/pyspark/test_pyspark_decorators.py tests/narwhals/test_e2e.py tests/narwhals/test_arch03_schema_driven_dispatch.py --collect-only -q`
</verification>

<success_criteria>
- M-02 resolved: no inline `CONFIG.use_narwhals_backend` branches in `test_pyspark_dtypes.py`; module-level `pytestmark` documents the skip; the previously-hidden `df.pandera.schema` assertion now runs unconditionally on native PySpark.
- M-03 resolved: `_spark_env_vars` and `spark` fixtures live solely in `tests/narwhals/conftest.py`.
- M-04 resolved: `TestPanderaConfig._cmp_errors` is gone; module-level `_cmp_errors` is called directly at all 8 call sites.
- M-07 resolved: inline `pytest.xfail()` replaced with `pytest.param(..., marks=pytest.mark.xfail(...))`.
- All five touched test files collect cleanly under both native PySpark and narwhals backend modes.
</success_criteria>

<output>
Create `.planning/phases/09-round-2-pr-review-fixes/09-02-SUMMARY.md` when done, recording each finding (M-02, M-03, M-04, M-07) and the file changes plus any unanticipated fixture interaction discovered during execution.
</output>
