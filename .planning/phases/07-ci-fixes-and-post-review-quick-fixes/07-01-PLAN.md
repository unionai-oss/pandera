---
phase: 07-ci-fixes-and-post-review-quick-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pandera/backends/narwhals/container.py
  - tests/ibis/test_ibis_container.py
  - tests/narwhals/test_e2e.py
autonomous: true
requirements:
  - CI-FIX-01
  - CI-FIX-02

must_haves:
  truths:
    - "Running `pytest tests/polars/test_polars_container.py::test_column_absent_error` under PANDERA_USE_NARWHALS_BACKEND=True passes — the narwhals container raises a SchemaError whose message matches the regex `column 'int_col' not in dataframe`"
    - "Running `pytest tests/narwhals/test_e2e.py` collects without raising `ValueError: fixture _spark_env_vars did not yield a value` when pyspark is not installed (HAS_PYSPARK=False) — non-PySpark tests in test_e2e.py execute normally"
    - "Running `pytest tests/ibis/test_ibis_container.py::test_column_absent_error` under PANDERA_USE_NARWHALS_BACKEND=True does not produce an unexpected failure — either the test passes or is xfail-marked under the narwhals backend with strict=True"
  artifacts:
    - path: "pandera/backends/narwhals/container.py"
      provides: "COLUMN_NOT_IN_DATAFRAME message reverted to 'not in dataframe'"
      contains: "not in dataframe"
    - path: "tests/narwhals/test_e2e.py"
      provides: "_spark_env_vars fixture that yields on every code path"
      contains: "yield"
    - path: "tests/ibis/test_ibis_container.py"
      provides: "test_column_absent_error with xfail decorator restored for narwhals backend"
      contains: "test_column_absent_error"
  key_links:
    - from: "tests/polars/test_polars_container.py::test_column_absent_error"
      to: "pandera/backends/narwhals/container.py"
      via: "regex match on SchemaError message 'column .* not in dataframe'"
      pattern: "not in dataframe"
    - from: "tests/narwhals/test_e2e.py::_spark_env_vars"
      to: "pytest fixture protocol"
      via: "generator yield (not bare return) on every branch"
      pattern: "yield"
---

<objective>
Fix the two CI failures introduced by Phase 6 so CI goes green:

1. CI-FIX-01: Revert the narwhals container `COLUMN_NOT_IN_DATAFRAME` message from "not found" back to "not in dataframe" — the polars `test_column_absent_error` was never updated when Phase 6 changed the message, and the user-locked decision is that "not in dataframe" is the correct message (pandera is a dataframe library, "not in dataframe" is accurate). The ibis test (which was updated in the same Phase 6 commit) must have its xfail decorator restored for the narwhals backend so it does not start failing.
2. CI-FIX-02: Fix the `_spark_env_vars` autouse fixture in `tests/narwhals/test_e2e.py` — on the `HAS_PYSPARK=False` early-exit branch it uses `return` instead of `yield`, causing pytest to raise `ValueError: fixture _spark_env_vars did not yield a value` for every test in the module when pyspark is not installed.

Purpose: Both CI jobs (`Unit Tests Narwhals Backend (polars)` and `Unit Tests Narwhals`) are currently red. PR #2339 cannot merge until CI is green. Each fix is mechanical with the exact file/line already diagnosed.

Output:
- `pandera/backends/narwhals/container.py` line ~598 message reverted to `f"column '{colname}' not in dataframe"`
- `tests/narwhals/test_e2e.py` `_spark_env_vars` early-exit branch uses `yield` instead of `return`
- `tests/ibis/test_ibis_container.py::test_column_absent_error` regains its `@pytest.mark.xfail(condition=CONFIG.use_narwhals_backend, strict=True)` decorator with an updated reason referencing the "not in dataframe" vs "not found" message difference
</objective>

<execution_context>
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Revert narwhals container COLUMN_NOT_IN_DATAFRAME message to "not in dataframe" and restore ibis test xfail</name>
  <files>pandera/backends/narwhals/container.py, tests/ibis/test_ibis_container.py</files>
  <read_first>
    - pandera/backends/narwhals/container.py (lines 580-605, the message construction in check_column_in_dataframe)
    - tests/polars/test_polars_container.py (lines 325-340, test_column_absent_error — DO NOT MODIFY this file; it is the source of truth for the message format)
    - tests/ibis/test_ibis_container.py (lines 190-205, test_column_absent_error — needs xfail decorator restored)
    - pandera/backends/ibis/container.py (line 392, native ibis container message — DO NOT MODIFY; it correctly says "not found" for the native ibis backend path)
  </read_first>
  <behavior>
    - Behavior 1: Running pandera validation through the narwhals container for a polars LazyFrame missing a required column produces a SchemaError whose message contains the literal substring `column 'int_col' not in dataframe` (where `int_col` is the missing column name)
    - Behavior 2: `tests/polars/test_polars_container.py::test_column_absent_error` passes unchanged under PANDERA_USE_NARWHALS_BACKEND=True without any source modification to that test file
    - Behavior 3: `tests/ibis/test_ibis_container.py::test_column_absent_error` is marked xfail with `strict=True` and `condition=CONFIG.use_narwhals_backend` so that under the narwhals backend (where the message is now "not in dataframe") the existing `match="column 'int_col' not found"` assertion is expected to fail, while under the native ibis backend (where ibis/container.py still emits "not found.") the test passes
  </behavior>
  <action>
    Update `pandera/backends/narwhals/container.py` (around line 598) in the `check_column_in_dataframe` method. The f-string currently reads `f"column '{colname}' not found"` followed by a `\n{_to_native(check_obj.head())}` line. Change the literal `not found` back to `not in dataframe` so the full f-string reads exactly `f"column '{colname}' not in dataframe"` (the trailing newline + head preview line stays untouched). This reverts the change made in commit 7b14c75b for the narwhals container only; do NOT touch `pandera/backends/ibis/container.py` (its native message stays as "not found.").

    Then update `tests/ibis/test_ibis_container.py` `test_column_absent_error` (around line 198): re-add the `@pytest.mark.xfail` decorator that commit 7b14c75b removed. The decorator MUST use `condition=CONFIG.use_narwhals_backend`, `strict=True`, and a `reason` string that explains the message format mismatch under narwhals — concretely: `"Error message format differs: 'not in dataframe' (narwhals backend) vs 'not found' (native ibis backend)"`. Keep the existing `match="column 'int_col' not found"` regex in the `pytest.raises` call so that the test continues to pass against the native ibis container (which still emits "not found.") and is correctly expected-to-fail under narwhals. Verify the `CONFIG` symbol is already imported in `test_ibis_container.py` (it is used in 7 other xfail decorators on lines 225, 254, 279, 305, 325, 476, 510); do not add a duplicate import.

    Do NOT modify `tests/polars/test_polars_container.py::test_column_absent_error` — per the user-locked decision, the polars test is the source of truth and requires no change.
  </action>
  <verify>
    <automated>cd /Users/deepyaman/github/unionai-oss/pandera &amp;&amp; grep -n "not in dataframe" pandera/backends/narwhals/container.py | grep -c "column" &amp;&amp; grep -n "not found" pandera/backends/narwhals/container.py | grep -v "^#" | grep -c "column"</automated>
    <automated>cd /Users/deepyaman/github/unionai-oss/pandera &amp;&amp; PANDERA_USE_NARWHALS_BACKEND=True python -m pytest tests/polars/test_polars_container.py::test_column_absent_error -x -v</automated>
    <automated>cd /Users/deepyaman/github/unionai-oss/pandera &amp;&amp; PANDERA_USE_NARWHALS_BACKEND=True python -m pytest tests/ibis/test_ibis_container.py::test_column_absent_error -x -v --runxfail=no</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "not in dataframe" pandera/backends/narwhals/container.py` returns at least 1 (the reverted message exists)
    - Source assertion: `grep -v '^#' pandera/backends/narwhals/container.py | grep -c "column '{colname}' not found"` returns 0 (the "not found" message no longer appears in non-comment lines of the narwhals container)
    - Behavior assertion: `PANDERA_USE_NARWHALS_BACKEND=True python -m pytest tests/polars/test_polars_container.py::test_column_absent_error` exits 0 (test passes)
    - Behavior assertion: `PANDERA_USE_NARWHALS_BACKEND=True python -m pytest tests/ibis/test_ibis_container.py::test_column_absent_error` exits 0 (test is xfail/XFAIL, not FAILED — the strict xfail decorator correctly anticipates the message mismatch)
    - Behavior assertion: `python -m pytest tests/ibis/test_ibis_container.py::test_column_absent_error` (without PANDERA_USE_NARWHALS_BACKEND) exits 0 (test passes against native ibis container which still says "not found.")
    - Source assertion: `grep -A 4 'def test_column_absent_error' tests/ibis/test_ibis_container.py | grep -c "match=\"column 'int_col' not found\""` returns 1 (the polars-test-symmetric "not found" regex is preserved on the ibis test; only the xfail wrapper changed)
    - Source assertion: `tests/polars/test_polars_container.py` is byte-identical to its current state (no modification)
  </acceptance_criteria>
  <done>
    The narwhals container emits "not in dataframe" again, the polars test passes unchanged, the ibis test is xfail-marked under the narwhals backend with strict=True, and the ibis test still passes under the native ibis backend. Both `Unit Tests Narwhals Backend (polars)` and the narwhals-backend ibis CI matrix entry no longer fail on `test_column_absent_error`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix _spark_env_vars autouse fixture to yield on HAS_PYSPARK=False early-exit branch</name>
  <files>tests/narwhals/test_e2e.py</files>
  <read_first>
    - tests/narwhals/test_e2e.py (lines 60-75 for the HAS_PYSPARK import sentinel, and lines 700-725 for the _spark_env_vars fixture definition)
  </read_first>
  <behavior>
    - Behavior 1: When pyspark is NOT installed (HAS_PYSPARK=False), the autouse `_spark_env_vars` fixture used by all tests in `tests/narwhals/test_e2e.py` does not raise `ValueError: "fixture <function _spark_env_vars at 0x...> did not yield a value"` — it acts as a no-op generator that yields once with no setup/teardown
    - Behavior 2: When pyspark IS installed (HAS_PYSPARK=True), the fixture continues to set `SPARK_LOCAL_IP=127.0.0.1` and `PYARROW_IGNORE_TIMEZONE=1` before the test runs, then restores the previous values after the test — exactly as it does today
    - Behavior 3: `pytest tests/narwhals/test_e2e.py --collect-only` succeeds without fixture errors when pyspark is absent
  </behavior>
  <action>
    Edit the `_spark_env_vars` autouse generator fixture in `tests/narwhals/test_e2e.py` (defined at approximately line 706). The fixture is declared with `@pytest.fixture(autouse=True, scope="function")` and pytest treats it as a generator-style fixture because the HAS_PYSPARK=True branch uses `yield`. Currently the `if not HAS_PYSPARK:` early-exit branch uses a bare `return` (line 713), which makes the function a non-generator under that branch and triggers `ValueError: fixture ... did not yield a value` for every test in the module when pyspark is missing.

    Replace the bare `return` on the early-exit branch with a `yield` (no value), so the function is unconditionally a generator. Concretely: the line `return` immediately under `if not HAS_PYSPARK:` becomes `yield` (the function exits after yielding once, with no env-var setup or teardown). Do NOT change the `if not HAS_PYSPARK:` condition itself, the docstring, the scope, the autouse flag, or the HAS_PYSPARK=True branch (which already yields correctly). Do NOT introduce any new env-var manipulation for the HAS_PYSPARK=False path.
  </action>
  <verify>
    <automated>cd /Users/deepyaman/github/unionai-oss/pandera &amp;&amp; python -c "import ast, sys; tree = ast.parse(open('tests/narwhals/test_e2e.py').read()); fn = next(n for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name == '_spark_env_vars'); yields = [n for n in ast.walk(fn) if isinstance(n, ast.Yield)]; returns = [n for n in ast.walk(fn) if isinstance(n, ast.Return)]; sys.exit(0 if len(yields) >= 2 and len(returns) == 0 else 1)"</automated>
    <automated>cd /Users/deepyaman/github/unionai-oss/pandera &amp;&amp; python -m pytest tests/narwhals/test_e2e.py --collect-only -q 2>&amp;1 | grep -c "did not yield a value" | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (AST): the `_spark_env_vars` function contains at least 2 `yield` statements (one on the HAS_PYSPARK=False branch, one on the HAS_PYSPARK=True branch) and zero bare `return` statements
    - Behavior assertion: running `python -m pytest tests/narwhals/test_e2e.py --collect-only` produces zero occurrences of the string `did not yield a value` in stderr/stdout
    - Behavior assertion: running `python -m pytest tests/narwhals/test_e2e.py -k "not pyspark"` does not error out on every test with the fixture-yield ValueError (specific test outcomes are out of scope for this task — only the fixture-protocol error must be gone)
    - Source assertion: `grep -A 12 'def _spark_env_vars' tests/narwhals/test_e2e.py | grep -B 1 'yield' | head -5` shows a `yield` statement appearing directly under the `if not HAS_PYSPARK:` block
  </acceptance_criteria>
  <done>
    The `_spark_env_vars` autouse fixture yields on every branch, the `Unit Tests Narwhals` CI job no longer raises `ValueError: fixture did not yield a value`, and pytest collection of `tests/narwhals/test_e2e.py` succeeds in environments without pyspark.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-runner → pandera | Test fixtures and assertions exercise pandera's narwhals backend; no external/untrusted input |
| pandera narwhals container → user dataframe | Column lookup against user-provided frame; error message is user-visible diagnostic text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | `check_column_in_dataframe` error message in `pandera/backends/narwhals/container.py` | accept | Message includes `_to_native(check_obj.head())` — same data exposure as before the message-text revert; no change in PII risk. The only change is the literal "not in dataframe" vs "not found"; no field count or value change |
| T-07-02 | Denial of Service | `_spark_env_vars` autouse fixture in `tests/narwhals/test_e2e.py` | mitigate | Current bug causes every test in the module to error out when pyspark is missing — this fix removes that test-suite DoS by ensuring the generator always yields. No new code paths added |
| T-07-03 | Tampering | None — no package installs in this plan | accept | This plan contains no `pip install`, `uv pip install`, `npm install`, `cargo add`, or equivalent. Package legitimacy gate is not applicable |
</threat_model>

<verification>
After both tasks complete:

1. `cd /Users/deepyaman/github/unionai-oss/pandera && PANDERA_USE_NARWHALS_BACKEND=True python -m pytest tests/polars/test_polars_container.py::test_column_absent_error tests/ibis/test_ibis_container.py::test_column_absent_error -x -v` — both tests pass or are xfail-marked, no FAILED.
2. `cd /Users/deepyaman/github/unionai-oss/pandera && python -m pytest tests/narwhals/test_e2e.py --collect-only -q` — collection succeeds, zero occurrences of `did not yield a value`.
3. `cd /Users/deepyaman/github/unionai-oss/pandera && git diff --stat` shows exactly 3 files modified: `pandera/backends/narwhals/container.py`, `tests/ibis/test_ibis_container.py`, `tests/narwhals/test_e2e.py`. `tests/polars/test_polars_container.py` is NOT in the diff.
</verification>

<success_criteria>
- `Unit Tests Narwhals Backend (polars)` CI job no longer fails on `test_column_absent_error`
- `Unit Tests Narwhals` CI job no longer fails with `ValueError: fixture did not yield a value`
- The narwhals-backend ibis CI matrix entry (`tests_narwhals_backend (ibis)`) does not regress — the ibis test_column_absent_error is correctly xfail-marked under narwhals mode
- The polars `test_column_absent_error` source is unchanged
</success_criteria>

<output>
Create `.planning/phases/07-ci-fixes-and-post-review-quick-fixes/07-01-SUMMARY.md` when done, documenting: (a) the literal one-line change in the narwhals container, (b) the xfail decorator restoration on the ibis test with reason string, (c) the `return` → `yield` change in `_spark_env_vars`, and (d) the verification commands that passed locally.
</output>
