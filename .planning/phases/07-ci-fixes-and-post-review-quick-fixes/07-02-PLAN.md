---
phase: 07-ci-fixes-and-post-review-quick-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pandera/backends/narwhals/components.py
  - noxfile.py
  - docs/source/supported_libraries.md
autonomous: true
requirements:
  - NITS-02

must_haves:
  truths:
    - "The `check_dtype` method in `pandera/backends/narwhals/components.py` no longer contains the redundant `assert native_pyspark_schema is not None` statement inside the `if uses_pyspark_dtype:` guarded branch — the surrounding guard already proves the binding is non-None"
    - "Both occurrences of the `tests/common/` invocation in `noxfile.py` carry an inline comment explaining why `extra in (\"polars\", \"ibis\")` excludes pyspark — namely that `tests/common/` has no `pyspark` marker so `pytest -m pyspark` would deselect every test in the directory"
    - "Every occurrence of the literal string `Pyspark SQL` in `docs/source/supported_libraries.md` is corrected to `PySpark SQL` — the capitalisation matches PySpark's official branding and how the rest of the v1.3 docs reference the project"
  artifacts:
    - path: "pandera/backends/narwhals/components.py"
      provides: "check_dtype without redundant non-None assert inside uses_pyspark_dtype guard"
      contains: "if uses_pyspark_dtype:"
    - path: "noxfile.py"
      provides: "tests_narwhals_backend session with inline comment on tests/common/ exclusion"
      contains: "tests/common/"
    - path: "docs/source/supported_libraries.md"
      provides: "PySpark SQL with correct capitalisation throughout"
      contains: "PySpark SQL"
  key_links:
    - from: "pandera/backends/narwhals/components.py::check_dtype"
      to: "pyspark_engine.DataType isinstance probe"
      via: "schema-driven uses_pyspark_dtype boolean already proves native_pyspark_schema is bound"
      pattern: "if uses_pyspark_dtype:"
    - from: "noxfile.py::tests_narwhals_backend"
      to: "tests/common/ pytest invocation"
      via: "extra-conditional dispatch with inline comment justifying the pyspark exclusion"
      pattern: "extra in \\(\"polars\", \"ibis\"\\)"
---

<objective>
Resolve three mechanical post-review nits (NITS-02) that have no architectural impact:

1. Remove the structurally unreachable `assert native_pyspark_schema is not None` from `pandera/backends/narwhals/components.py::check_dtype` — the surrounding `if uses_pyspark_dtype:` guard already proves the binding is non-None, since `native_pyspark_schema` is initialised in the immediately-preceding statement as `nw.to_native(check_obj).schema if uses_pyspark_dtype else None`.
2. Add an inline comment to both `extra in ("polars", "ibis")` exclusions in `noxfile.py` (lines 323 and 389) explaining that `tests/common/` has no `pyspark` marker, so dispatching the pyspark variant of `tests_narwhals_backend` to `tests/common/` would deselect every test (`pytest -m pyspark` matches nothing).
3. Correct the three "Pyspark SQL" misspellings in `docs/source/supported_libraries.md` (lines 26, 49, 128) to "PySpark SQL" to match the rest of the v1.3 documentation and PySpark's official capitalisation.

Purpose: These are post-review polish items called out by reviewers on PR #2339. None of them affect behavior — they affect code clarity, maintainability, and docs quality. Each is a single-file edit with the exact target already identified by line number.

Output: three small edits across three files, with no test changes required (all are non-behavioral).
</objective>

<execution_context>
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepyaman/github/unionai-oss/pandera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove redundant non-None assert from check_dtype in narwhals components</name>
  <files>pandera/backends/narwhals/components.py</files>
  <read_first>
    - pandera/backends/narwhals/components.py (lines 270-320, the check_dtype method including the uses_pyspark_dtype initialisation at line ~277, native_pyspark_schema initialisation at line ~284, and the if uses_pyspark_dtype block containing the redundant assert at line ~297)
  </read_first>
  <behavior>
    - Behavior 1: `check_dtype` continues to perform the schema-driven PySpark dtype comparison path identically when `uses_pyspark_dtype` is True — `pyspark_dtype = native_pyspark_schema[column].dataType` still executes and yields the same string comparison result
    - Behavior 2: Running `python -m pytest tests/narwhals/test_arch03_schema_driven_dispatch.py` (or any test that exercises the PySpark-dtype branch) does not regress
    - Behavior 3: A `mypy`/`pyright`/`ruff` run does not introduce a new "possibly None" warning on the `native_pyspark_schema[column]` subscript access — the existing static-analysis configuration already accepts this code path because `uses_pyspark_dtype` narrows the binding via the ternary on line ~284
  </behavior>
  <action>
    Open `pandera/backends/narwhals/components.py` and locate the `check_dtype` method on `ColumnBackend` (the method starts around line 254). Find the loop body that begins `for column, nw_dtype in zip(schema_obj.names(), schema_obj.dtypes()):` (line ~288). Inside the `if uses_pyspark_dtype:` block (line ~289), delete the single line that reads `assert native_pyspark_schema is not None` (currently line 297). Keep every other line in that branch intact — the docstring comment block immediately above the assert (lines 290-296 explaining the schema-driven dispatch and referencing the ARCH-03 planning doc) stays, and the `pyspark_dtype = native_pyspark_schema[column].dataType` statement immediately below the deleted assert (line 298) stays.

    The deletion is sound because: `native_pyspark_schema` is bound on line ~284 as `nw.to_native(check_obj).schema if uses_pyspark_dtype else None`. The `if uses_pyspark_dtype:` guard on line 289 ensures we only enter this branch when `uses_pyspark_dtype` was True at the point of binding, which means `native_pyspark_schema` is the schema (not None). The assert is therefore structurally unreachable as a failure path — it can never raise AssertionError in correct usage.

    Do NOT replace the assert with an `if native_pyspark_schema is None: raise ...` guard — the user-locked decision in the phase goal is to "simply trust the guard". Do NOT add any new comments to replace the removed line. Do NOT modify the `try/except ImportError` block above (lines 274-279), the `schema_obj` initialisation (line 282), or any code below the `continue` on line 315.
  </action>
  <verify>
    <automated>cd /Users/deepyaman/github/unionai-oss/pandera &amp;&amp; grep -v '^[[:space:]]*#' pandera/backends/narwhals/components.py | grep -c "assert native_pyspark_schema is not None" | grep -q "^0$"</automated>
    <automated>cd /Users/deepyaman/github/unionai-oss/pandera &amp;&amp; python -c "import ast; tree = ast.parse(open('pandera/backends/narwhals/components.py').read()); asserts = [n for n in ast.walk(tree) if isinstance(n, ast.Assert) and 'native_pyspark_schema' in ast.unparse(n.test)]; assert len(asserts) == 0, f'found {len(asserts)} matching asserts'"</automated>
    <automated>cd /Users/deepyaman/github/unionai-oss/pandera &amp;&amp; python -c "import pandera.backends.narwhals.components"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (AST): `ast.walk` over `components.py` finds zero `ast.Assert` nodes whose test expression contains the identifier `native_pyspark_schema`
    - Source assertion (grep): `grep -v '^[[:space:]]*#' pandera/backends/narwhals/components.py | grep -c "assert native_pyspark_schema is not None"` returns 0
    - Source assertion: `grep -c "if uses_pyspark_dtype:" pandera/backends/narwhals/components.py` returns at least 1 (the guard structure is preserved)
    - Source assertion: `grep -c "native_pyspark_schema\[column\]" pandera/backends/narwhals/components.py` returns 1 (the legitimate use site of the binding is preserved)
    - Behavior assertion: `python -c "import pandera.backends.narwhals.components"` exits 0 (module still imports cleanly)
    - Behavior assertion: `python -m pytest tests/narwhals/test_arch03_schema_driven_dispatch.py -x -q` exits with the same outcome as before the change (any pre-existing pass/skip/xfail outcomes are preserved)
  </acceptance_criteria>
  <done>
    The redundant `assert native_pyspark_schema is not None` is gone from `check_dtype`. The surrounding `if uses_pyspark_dtype:` guard remains the only protection against a None binding — which is correct because the binding's ternary mirrors the same condition. Existing tests for the schema-driven dispatch path still pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add inline comment to noxfile tests/common/ exclusion explaining missing pyspark marker</name>
  <files>noxfile.py</files>
  <read_first>
    - noxfile.py (lines 315-330 for the unit_tests session's tests/common/ dispatch on line 323-324, and lines 380-395 for the tests_narwhals_backend session's identical dispatch on line 389-390)
    - tests/common/ directory listing (only `conftest.py`, `test_builtin_checks.py`, `__init__.py` exist — no pyspark marker anywhere) [reference only — no file read needed beyond confirming the existing absence]
  </read_first>
  <behavior>
    - Behavior 1: Running `nox -s tests_narwhals_backend(pyspark)` does NOT attempt to dispatch into `tests/common/ -m pyspark` (which would deselect every test and exit with no-tests-collected) — the existing exclusion logic is preserved
    - Behavior 2: A future maintainer reading `noxfile.py` at the `if extra in ("polars", "ibis"):` line sees an inline comment that explains WHY pyspark is excluded, without needing to grep `tests/common/` for missing markers
    - Behavior 3: Running `nox -l` still lists the same sessions with the same parametrisation — the comment is non-functional
  </behavior>
  <action>
    Open `noxfile.py` and locate two structurally identical `extra in ("polars", "ibis")` conditional blocks that gate dispatch of the `tests/common/` pytest invocation:
    - First occurrence: the `unit_tests` session around line 323 — `if not session.posargs and extra in ("polars", "ibis"):` immediately followed by the `session.run("pytest", *cov_args, "tests/common/", "-m", extra, env=env)` call on line 324
    - Second occurrence: the `tests_narwhals_backend` session around line 389 — `if extra in ("polars", "ibis"):` followed by `session.run("pytest", *cov_args, "tests/common/", "-m", extra, env=env)` on line 390

    Per the phase goal (success criterion 4), only the `tests_narwhals_backend` session needs the comment, but for consistency add an identical inline comment to BOTH occurrences so the codebase reads uniformly. Place the comment on the line IMMEDIATELY ABOVE each `if` statement (not as a trailing comment on the `if` line), at the same indentation as the `if`. Use this exact wording (single-line, terse, factual):

    `# tests/common/ has no pyspark marker — pytest -m pyspark would deselect every test there`

    Do NOT modify the `if` condition itself, the `session.run` call, the `env` dict, or any other code in either session. Do NOT add the comment inside the `if` body or as a trailing comment — the goal is a header-style explanation directly above the gated call.
  </action>
  <verify>
    <automated>cd /Users/deepyaman/github/unionai-oss/pandera &amp;&amp; grep -B 1 'extra in ("polars", "ibis"):' noxfile.py | grep -c "tests/common/ has no pyspark marker"</automated>
    <automated>cd /Users/deepyaman/github/unionai-oss/pandera &amp;&amp; python -c "import ast; src = open('noxfile.py').read(); tree = ast.parse(src); compile(src, 'noxfile.py', 'exec')"</automated>
    <automated>cd /Users/deepyaman/github/unionai-oss/pandera &amp;&amp; grep -c 'extra in ("polars", "ibis")' noxfile.py</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -B 1 'extra in ("polars", "ibis"):' noxfile.py | grep -c "tests/common/ has no pyspark marker"` returns 2 (one comment above each of the two occurrences)
    - Source assertion: `grep -c 'extra in ("polars", "ibis")' noxfile.py` returns 2 (the two conditional gates are still present — no accidental deletions)
    - Source assertion: `grep -c 'session.run("pytest", \*cov_args, "tests/common/"' noxfile.py` returns 2 (the two gated pytest invocations are still present)
    - Behavior assertion: `python -c "import ast; ast.parse(open('noxfile.py').read())"` exits 0 (noxfile remains valid Python)
    - Behavior assertion: `nox -l 2>&1 | grep -E "tests_narwhals_backend|unit_tests"` lists the same sessions and parametrisations as before (the comment did not change parametrisation)
  </acceptance_criteria>
  <done>
    Both `extra in ("polars", "ibis")` gates in `noxfile.py` carry an inline comment directly above them stating that `tests/common/` has no pyspark marker and `pytest -m pyspark` would deselect every test there. The runtime behavior of every nox session is unchanged.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Correct "Pyspark SQL" → "PySpark SQL" capitalisation in supported_libraries.md</name>
  <files>docs/source/supported_libraries.md</files>
  <read_first>
    - docs/source/supported_libraries.md (lines 1-60 for the DataFrame Library Support table containing the first occurrence on line 26 and the toctree containing the second occurrence on line 49)
    - docs/source/supported_libraries.md (lines 120-140 for the third occurrence on line 128 inside the Narwhals-powered backends section)
  </read_first>
  <behavior>
    - Behavior 1: `grep -c 'Pyspark SQL' docs/source/supported_libraries.md` returns 0 — no occurrence of the lowercase-`s` form remains
    - Behavior 2: The three sites previously reading `Pyspark SQL` now read `PySpark SQL`, matching the existing correct usage in lines 33, 137, 186, 189, 193, 226, and 229 of the same file
    - Behavior 3: Sphinx `:ref:` targets (the bracketed `<native-pyspark>` and `<pyspark_sql>` labels) are NOT modified — only the display text changes; cross-references still resolve to the same labels
  </behavior>
  <action>
    Edit `docs/source/supported_libraries.md` and replace `Pyspark SQL` with `PySpark SQL` at exactly three locations:

    1. Line 26: `* - {ref}\`Pyspark SQL <native-pyspark>\`` → `* - {ref}\`PySpark SQL <native-pyspark>\`` (the `<native-pyspark>` reference target is unchanged — only the displayed text is corrected)
    2. Line 49: `Pyspark SQL <pyspark_sql>` → `PySpark SQL <pyspark_sql>` (inside the toctree directive — display text only; the `<pyspark_sql>` toctree target is unchanged)
    3. Line 128: `{ref}\`Pyspark SQL <native-pyspark>\` integrations behind a single unified code` → `{ref}\`PySpark SQL <native-pyspark>\` integrations behind a single unified code`

    Do NOT modify any other lines. Do NOT modify the bracketed reference targets (`<native-pyspark>`, `<pyspark_sql>`) — these are Sphinx label identifiers and changing them would break cross-references. Do NOT introduce new `PySpark SQL` mentions elsewhere — the file already has correctly-capitalised occurrences on lines 33, 137, 186, 189, 193, 226, 229 which must remain unchanged.

    After the edit, the file should contain 10 occurrences of `PySpark SQL` (7 pre-existing + 3 newly-corrected) and 0 occurrences of `Pyspark SQL`.
  </action>
  <verify>
    <automated>cd /Users/deepyaman/github/unionai-oss/pandera &amp;&amp; grep -c "Pyspark SQL" docs/source/supported_libraries.md | grep -q "^0$"</automated>
    <automated>cd /Users/deepyaman/github/unionai-oss/pandera &amp;&amp; test "$(grep -c 'PySpark SQL' docs/source/supported_libraries.md)" -eq 10</automated>
    <automated>cd /Users/deepyaman/github/unionai-oss/pandera &amp;&amp; grep -c "<native-pyspark>" docs/source/supported_libraries.md &amp;&amp; grep -c "<pyspark_sql>" docs/source/supported_libraries.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "Pyspark SQL" docs/source/supported_libraries.md` returns exactly 0
    - Source assertion: `grep -c "PySpark SQL" docs/source/supported_libraries.md` returns exactly 10 (7 pre-existing + 3 corrected)
    - Source assertion: `grep -c "<native-pyspark>" docs/source/supported_libraries.md` returns the same value as before the edit (Sphinx reference targets untouched)
    - Source assertion: `grep -c "<pyspark_sql>" docs/source/supported_libraries.md` returns the same value as before the edit
    - Source assertion: lines 26, 49, and 128 (1-indexed) of the updated file each contain the literal `PySpark SQL`
  </acceptance_criteria>
  <done>
    Every "Pyspark SQL" misspelling in `docs/source/supported_libraries.md` is corrected to "PySpark SQL". Sphinx reference labels (`<native-pyspark>`, `<pyspark_sql>`) are unchanged so cross-references continue to resolve.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs build → published site | Documentation rendered by Sphinx and served as user-facing reference; no executable code |
| noxfile → CI runner | Nox sessions execute test commands inside the CI sandbox; comment additions do not change executed commands |
| pandera narwhals components → schema validation | Removing a dead assert does not change control flow under valid inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | `pandera/backends/narwhals/components.py::check_dtype` after assert removal | accept | The deleted assert was structurally unreachable — `if uses_pyspark_dtype:` already proves `native_pyspark_schema is not None` because the binding is set via `... if uses_pyspark_dtype else None` on the immediately-preceding statement. No new code path is exposed to malicious input |
| T-07-05 | Information Disclosure | `docs/source/supported_libraries.md` capitalisation fix | accept | Public documentation; only display-text capitalisation changes. No new information disclosed |
| T-07-06 | Repudiation | `noxfile.py` inline comment additions | accept | Comments only; no executed-command change. CI commit logs continue to record the same `session.run("pytest", ...)` invocations |
| T-07-07 | Tampering | None — no package installs in this plan | accept | This plan contains no `pip install`, `uv pip install`, `npm install`, `cargo add`, or equivalent. Package legitimacy gate is not applicable |
</threat_model>

<verification>
After all three tasks complete:

1. `cd /Users/deepyaman/github/unionai-oss/pandera && grep -c "Pyspark SQL" docs/source/supported_libraries.md` returns 0.
2. `cd /Users/deepyaman/github/unionai-oss/pandera && grep -v '^[[:space:]]*#' pandera/backends/narwhals/components.py | grep -c "assert native_pyspark_schema"` returns 0.
3. `cd /Users/deepyaman/github/unionai-oss/pandera && grep -B 1 'extra in ("polars", "ibis"):' noxfile.py | grep -c "tests/common/ has no pyspark marker"` returns 2.
4. `cd /Users/deepyaman/github/unionai-oss/pandera && python -c "import pandera.backends.narwhals.components; import ast; ast.parse(open('noxfile.py').read())"` exits 0.
5. `cd /Users/deepyaman/github/unionai-oss/pandera && git diff --stat` shows exactly 3 files modified: `pandera/backends/narwhals/components.py`, `noxfile.py`, `docs/source/supported_libraries.md`.
</verification>

<success_criteria>
- `check_dtype` no longer carries the redundant non-None assert
- `noxfile.py` carries an inline comment justifying the pyspark exclusion in `tests/common/`
- Every `Pyspark SQL` in `docs/source/supported_libraries.md` is corrected to `PySpark SQL`
- No behavior change anywhere — all three are clarity/polish edits
</success_criteria>

<output>
Create `.planning/phases/07-ci-fixes-and-post-review-quick-fixes/07-02-SUMMARY.md` when done, documenting: (a) the removed `assert` line and the guard that obviates it, (b) the exact comment wording added in two noxfile locations, (c) the three `Pyspark SQL` → `PySpark SQL` corrections with line numbers, and (d) the verification commands that passed locally.
</output>
