---
phase: 05-expression-based-check-protocol-eliminate-framework-specific-apply-branching
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - pandera/backends/narwhals/checks.py
autonomous: true
requirements:
  - EXPR-01
  - EXPR-04
  - EXPR-05
  - EXPR-06
  - EXPR-07
must_haves:
  truths:
    - "apply() has no isinstance/hasattr branching between polars and ibis in the native=False path"
    - "The ibis row_number join block (~40 lines) is deleted entirely"
    - "apply() native=False path is a single frame.with_columns(expr.alias(CHECK_OUTPUT_KEY)) call"
    - "apply() native=True path is unchanged"
    - "element_wise path returns frame.with_columns(expr.alias(CHECK_OUTPUT_KEY)) directly"
    - "All routing and builtin tests go GREEN after this plan"
  artifacts:
    - path: "pandera/backends/narwhals/checks.py"
      provides: "Rewritten apply() — no ibis row_number join, no Dispatcher workaround"
      contains: "frame.with_columns"
  key_links:
    - from: "pandera/backends/narwhals/checks.py apply()"
      to: "frame.with_columns(expr.alias(CHECK_OUTPUT_KEY))"
      via: "uniform expression protocol"
      pattern: "with_columns.*alias.*CHECK_OUTPUT_KEY"
    - from: "apply() native=False"
      to: "check_fn(nw.col(key))"
      via: "expression dispatch"
      pattern: "check_fn\\(nw\\.col\\(key\\)\\)"
---

<objective>
Rewrite `apply()` in `NarwhalsCheckBackend` to use the expression protocol uniformly. Delete the ibis row_number join block, the Dispatcher workaround, and the rename/normalization block. All three branches now return `frame.with_columns(expr.alias(CHECK_OUTPUT_KEY))` — no backend-specific code.

Purpose: This is the final implementation step. After this plan, all Phase 5 tests go GREEN — both the updated routing tests (05-01) and the builtin pass/fail tests.
Output: Simplified `checks.py` with ~40 lines removed from `apply()`.
</objective>

<execution_context>
@./.claire/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-expression-based-check-protocol-eliminate-framework-specific-apply-branching/05-RESEARCH.md
@.planning/phases/05-expression-based-check-protocol-eliminate-framework-specific-apply-branching/05-01-SUMMARY.md
@.planning/phases/05-expression-based-check-protocol-eliminate-framework-specific-apply-branching/05-02-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs — read from current source before editing. -->

Current apply() structure (to be replaced):
- Lines 44-58: element_wise branch — does .select(selector) after map_batches, then falls through to reassembly
- Lines 59-63: native=True branch — calls _normalize_native_output, then falls through to reassembly
- Lines 64-83: native=False branch — Dispatcher workaround for ibis, calls fn(frame, key), falls through to reassembly
- Lines 85-97: normalize single/multi-column output to CHECK_OUTPUT_KEY (delete entirely)
- Lines 99-142: polars path + ibis row_number join (delete entirely)

New apply() structure (target):
```python
def apply(self, check_obj: NarwhalsData):
    frame = check_obj.frame
    key = check_obj.key

    if self.check.element_wise:
        selector = nw.col(key or "*")
        try:
            expr = selector.map_batches(self.check_fn, return_dtype=nw.Boolean)
        except NotImplementedError:
            raise NotImplementedError(
                "element_wise checks are not supported on SQL-lazy backends ..."
            )
        return frame.with_columns(expr.alias(CHECK_OUTPUT_KEY))

    elif self.check.native:
        native_frame = nw.to_native(frame)
        out = self.check_fn(native_frame, key)
        return self._normalize_native_output(out, check_obj)

    else:
        # native=False: expression protocol
        # Column check: pass nw.col(key). Frame check (key=="*"): pass frame.
        if key and key != "*":
            expr = self.check_fn(nw.col(key))
        else:
            expr = self.check_fn(frame)
        return frame.with_columns(expr.alias(CHECK_OUTPUT_KEY))
```

Key detail for element_wise: the current code does `.select(selector)` after map_batches, then
the result falls through to reassembly. In the new code, map_batches returns an Expr directly
and we call frame.with_columns(expr.alias(CHECK_OUTPUT_KEY)) — no .select() needed.

The bool return check `if isinstance(out, bool): return out` stays only in the native=True
path — _normalize_native_output handles that. The element_wise and native=False paths
never return bool.

_normalize_native_output is unchanged — still needed for native=True ibis outputs.
postprocess and postprocess_lazyframe_output and postprocess_bool_output are unchanged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite apply() — delete ibis join, Dispatcher workaround, reassembly block</name>
  <files>pandera/backends/narwhals/checks.py</files>

  <behavior>
    - element_wise branch: map_batches on nw.col(key or "*"), return frame.with_columns(expr.alias(CHECK_OUTPUT_KEY))
    - native=True branch: unchanged — to_native → check_fn(native_frame, key) → _normalize_native_output(out, check_obj)
    - native=False branch: check_fn(nw.col(key)) for column checks, check_fn(frame) for key=="*", return frame.with_columns(expr.alias(CHECK_OUTPUT_KEY))
    - No isinstance/hasattr detection for polars vs ibis in apply()
    - No row_number join
    - No .select(selector) after map_batches
    - No rename/all_horizontal normalization block
    - The `from pandera.api.function_dispatch import Dispatcher` import removed from apply()
    - The `_materialize` import at top of file can be removed if no longer used elsewhere
  </behavior>

  <action>
Read the current pandera/backends/narwhals/checks.py fully before editing.

Replace the entire `apply()` method body with the new structure from the interfaces block above.

Specific deletions:
1. The `Dispatcher` import block inside apply() (lines 69-83 approx)
2. The `if isinstance(out, bool): return out` check after the three branches (keep it ONLY inside `_normalize_native_output` which already handles this implicitly — actually, the check at line 85 can be removed since element_wise and native=False never return bool; native=True returns from _normalize_native_output)
3. The rename/all_horizontal normalization block (lines 88-97 approx)
4. The polars/ibis detection and row_number join (lines 99-142 approx)

For the element_wise branch:
- Remove the `.select(selector)` call after map_batches
- Instead: `expr = selector.map_batches(self.check_fn, return_dtype=nw.Boolean)` then return `frame.with_columns(expr.alias(CHECK_OUTPUT_KEY))`
- Keep the try/except NotImplementedError with the same error message

For the native=False branch (formerly the else block with Dispatcher workaround):
- No Dispatcher import, no isinstance check, no registry lookup
- Just: `if key and key != "*": expr = self.check_fn(nw.col(key))` else `expr = self.check_fn(frame)`
- Then: `return frame.with_columns(expr.alias(CHECK_OUTPUT_KEY))`

For the import at top: check whether `_materialize` is still needed anywhere in the file. If the ibis fallback that called `_materialize` is the only usage, remove that import too.

After editing, verify no reference to `_row_col`, `idx_frame`, `idx_out`, `row_number`, `Dispatcher` remains in `apply()`.
  </action>

  <verify>
    <automated>python -m pytest tests/backends/narwhals/test_checks.py -q 2>&1 | tail -15</automated>
  </verify>
  <done>
All tests in test_checks.py pass (GREEN):
- test_builtin_check_routing: PASS
- test_native_false_user_check: PASS
- test_builtin_checks_pass[equal_to-polars] through [str_length-ibis]: all 28 parametrize cases PASS
- test_builtin_checks_fail[..]: all 28 cases PASS
- test_element_wise_sql_lazy_raises: PASS
- test_apply_returns_wide_table: PASS
- test_native_true_user_check_polars, test_native_true_user_check_ibis: PASS
- test_ibis_boolean_scalar_normalization: PASS
No reference to row_number join, Dispatcher workaround, or reassembly block in apply().
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full narwhals backend suite and confirm no regressions</name>
  <files>pandera/backends/narwhals/checks.py</files>

  <action>
Run the full narwhals test suite. Investigate and fix any failures that are caused by the Phase 5 changes (not pre-existing failures).

Pre-existing failures (do NOT attempt to fix):
- 95 ibis backend integration failures unrelated to this phase (noted in STATE.md)
- coerce for Ibis is xfail(strict=True) — intentional

If any failure in tests/backends/narwhals/ appears to be caused by the apply() rewrite or builtin signature change, fix it before this task is done.

Command: `python -m pytest tests/backends/narwhals/ -q 2>&1 | tail -20`
  </action>

  <verify>
    <automated>python -m pytest tests/backends/narwhals/ -q 2>&1 | tail -20</automated>
  </verify>
  <done>
Full narwhals backend suite runs. No NEW failures compared to pre-Phase-5 baseline.
tests/backends/narwhals/test_checks.py: all tests PASS (or xfail as expected for Phase 4 stubs).
tests/backends/narwhals/test_e2e.py: same pass/fail count as before Phase 5.
  </done>
</task>

</tasks>

<verification>
Final check confirming the ibis row_number join is gone:
`grep -n "row_number\|_row_col\|idx_frame\|idx_out" pandera/backends/narwhals/checks.py` — must return no output.

Final check confirming uniform with_columns pattern:
`grep -n "with_columns.*alias.*CHECK_OUTPUT_KEY" pandera/backends/narwhals/checks.py` — must show 2-3 lines (one per branch in apply()).

Full test run: `python -m pytest tests/backends/narwhals/test_checks.py -v 2>&1 | tail -30`
All test_builtin_checks_pass and test_builtin_checks_fail cases must be PASSED for both polars and ibis backends.
</verification>

<success_criteria>
- apply() is ~25 lines total (was ~100 lines with join hack)
- No ibis-specific code in apply() (no `import ibis`, no `hasattr(native_out, "execute")`)
- No Dispatcher import inside apply()
- All 28 builtin check pass cases PASS for polars + ibis
- All 28 builtin check fail cases PASS for polars + ibis
- test_builtin_check_routing PASS (nw.Expr key confirmed)
- test_native_false_user_check PASS (receives nw.Expr)
- test_element_wise_sql_lazy_raises PASS
- Full narwhals suite: no new failures vs pre-Phase-5 baseline
</success_criteria>

<output>
After completion, create `.planning/phases/05-expression-based-check-protocol-eliminate-framework-specific-apply-branching/05-03-SUMMARY.md`
</output>
