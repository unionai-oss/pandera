---
phase: 06-eliminate-unnecessary-materialization-lazy-first-failure-cases-and-check-output
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/backends/narwhals/test_components.py
  - tests/backends/narwhals/test_e2e.py
autonomous: true
requirements:
  - LAZY-FIRST-01
must_haves:
  truths:
    - "New subsample() tests exist and fail RED: subsample(head=) does not call _materialize, subsample(tail=, ibis) raises NotImplementedError"
    - "New failure_cases_metadata() ibis test exists and fails RED: asserts ibis.Table return type"
    - "Updated e2e assertions flip to RED: SchemaError.failure_cases is native (not nw.DataFrame), ibis SchemaErrors.failure_cases is ibis.Table (not pl.DataFrame)"
  artifacts:
    - path: "tests/backends/narwhals/test_components.py"
      provides: "subsample lazy head+tail tests, ibis tail= NotImplementedError test"
    - path: "tests/backends/narwhals/test_e2e.py"
      provides: "Updated type assertions matching Phase 6 contracts"
  key_links:
    - from: "test_components.py"
      to: "pandera/backends/narwhals/base.py subsample()"
      via: "ColumnBackend().subsample() call"
      pattern: "subsample"
    - from: "test_e2e.py"
      to: "SchemaError.failure_cases"
      via: "pytest.raises(SchemaError) exc_info.value.failure_cases"
      pattern: "failure_cases"
---

<objective>
Write the RED baseline tests for Phase 6 — tests that describe the new lazy-first contracts but currently fail because the implementation hasn't changed yet.

Purpose: Establish verifiable acceptance criteria before touching production code. Follows the same RED-baseline pattern used in Phases 4 and 5.

Output: New tests in test_components.py (subsample lazy behavior) and test_e2e.py (updated type assertions). All new/updated tests must be RED after this plan.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-eliminate-unnecessary-materialization-lazy-first-failure-cases-and-check-output/06-CONTEXT.md
@.planning/phases/06-eliminate-unnecessary-materialization-lazy-first-failure-cases-and-check-output/06-RESEARCH.md
@.planning/phases/06-eliminate-unnecessary-materialization-lazy-first-failure-cases-and-check-output/06-VALIDATION.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from existing test infrastructure. -->

From tests/backends/narwhals/conftest.py:
- `make_narwhals_frame` fixture — parametrized over `["polars", "ibis"]`
- Creates `nw.from_native(pl.LazyFrame(data))` for polars
- Creates `nw.from_native(ibis.memtable(pd.DataFrame(data)))` for ibis

From pandera/backends/narwhals/base.py:
```python
class NarwhalsSchemaBackend(BaseSchemaBackend):
    def subsample(self, check_obj, head=None, tail=None, sample=None, random_state=None): ...
    def failure_cases_metadata(self, schema_name, schema_errors): ...
```

From tests/backends/narwhals/test_e2e.py (lines that need updating):
- Line 188-199: `test_greater_than_fails_failure_cases_type` in `TestBuiltinChecksPolars`
  — asserts `isinstance(fc, nw.DataFrame)` → Phase 6 contract: `isinstance(fc, pl.DataFrame)` (native)
- Line 261-275: `test_greater_than_fails_failure_cases_type` in `TestBuiltinChecksIbis`
  — asserts `isinstance(fc, nw.DataFrame)` AND `isinstance(nw.to_native(fc), ibis.Table)`
  → Phase 6 contract: `isinstance(fc, ibis.Table)` (native directly)
- Line 277-...: `test_greater_than_fails_failure_cases_values` in `TestBuiltinChecksIbis`
  — uses `nw.to_native(fc).execute()` → Phase 6 contract: `fc.execute()` (already native)
- Line 633-645: `test_ibis_lazy_failure_cases_is_dataframe` in `TestLazyValidationIbis`
  — asserts `isinstance(fc, pl.DataFrame)` → Phase 6 contract: `isinstance(fc, ibis.Table)` (native)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED subsample() and failure_cases_metadata() tests to test_components.py</name>
  <files>tests/backends/narwhals/test_components.py</files>
  <behavior>
    - Test subsample_head_stays_lazy: call `NarwhalsSchemaBackend().subsample(nw_frame, head=2)` for polars backend — assert result is `nw.LazyFrame`, assert NO full-frame materialization (result is NOT nw.DataFrame wrapping collected data). Currently RED because subsample() calls `_materialize(check_obj).head(head)` which returns nw.DataFrame.
    - Test subsample_tail_stays_lazy: call subsample(tail=2) for polars — assert result is `nw.LazyFrame`. Currently RED for same reason.
    - Test subsample_ibis_tail_raises: call subsample(tail=2) with ibis-backed frame — assert `NotImplementedError` is raised with message containing "tail=". Currently RED because subsample() calls `_materialize()` instead of raising.
    - Test failure_cases_metadata_ibis_returns_ibis_table: create a mock `SchemaError` with `failure_cases` being an ibis-backed `nw.DataFrame` (filtered from ibis.memtable), call `NarwhalsSchemaBackend().failure_cases_metadata(...)`, assert `isinstance(result.failure_cases, ibis.Table)`. Currently RED because `failure_cases_metadata()` always produces `pl.DataFrame`.
  </behavior>
  <action>
    Add a new test class `TestSubsample` and a standalone test `test_failure_cases_metadata_ibis` to `tests/backends/narwhals/test_components.py`.

    For `TestSubsample`:
    - Import `NarwhalsSchemaBackend` from `pandera.backends.narwhals.base`
    - For polars tests: use `nw.from_native(pl.LazyFrame({"x": [1,2,3,4,5]}), eager_or_interchange_only=False)` directly (no fixture needed — no ibis variant)
    - `test_subsample_head_stays_lazy`: `result = backend.subsample(frame, head=2)` → assert `isinstance(result, nw.LazyFrame)`
    - `test_subsample_tail_stays_lazy`: `result = backend.subsample(frame, tail=2)` → assert `isinstance(result, nw.LazyFrame)`
    - `test_subsample_both_head_and_tail`: `result = backend.subsample(frame, head=2, tail=2)` → assert `isinstance(result, nw.LazyFrame)`
    - `test_subsample_ibis_tail_raises`: skip if ibis not installed; build ibis-backed nw.DataFrame; assert `pytest.raises(NotImplementedError, match="tail=")` when calling `backend.subsample(ibis_frame, tail=2)`
    - `test_subsample_no_params_returns_unchanged`: `result = backend.subsample(frame)` → assert `result is frame`

    For `test_failure_cases_metadata_ibis`:
    - Skip if ibis not installed
    - Build `failure_cases_df = nw.from_native(ibis.memtable({"x": [-1, -3]}), eager_or_interchange_only=False)`
    - Build a minimal `SchemaError` stub using `types.SimpleNamespace` or direct `SchemaError(...)` construction to set `failure_cases`, `check_output=None`, `check_index=0`, `check=None`, `reason_code=SchemaErrorReason.DATAFRAME_CHECK`, `schema=SimpleNamespace(name="x", __class__=type("Column", (), {"__name__": "Column"})())`
    - Call `NarwhalsSchemaBackend().failure_cases_metadata("test_schema", [schema_error])`
    - Assert `isinstance(result.failure_cases, ibis.Table)` — this is RED because the current implementation always returns `pl.DataFrame`

    Mark all new tests with `pytest.mark.xfail(strict=True)` so the suite stays GREEN (they are expected to fail until Plan 02/03 implement the changes).

    Wait — do NOT use xfail. The standard pattern in this project is to write tests that directly assert the new behavior. They will be RED (failing) after this plan, and Plans 02/03 will make them GREEN. This is the correct RED baseline approach. The VALIDATION.md explicitly says "Status: ⬜ pending" — these tests should fail, not be marked xfail.
  </action>
  <verify>
    <automated>pytest tests/backends/narwhals/test_components.py -x -q --no-header 2>&1 | tail -20</automated>
  </verify>
  <done>
    New tests exist in test_components.py. The subsample and failure_cases_metadata tests FAIL (RED) with assertion errors — not import errors or syntax errors. Existing passing tests in the file continue to pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update type assertions in test_e2e.py to match Phase 6 contracts</name>
  <files>tests/backends/narwhals/test_e2e.py</files>
  <action>
    Update four assertion blocks in test_e2e.py to match the Phase 6 failure_cases type contracts:

    1. **TestBuiltinChecksPolars.test_greater_than_fails_failure_cases_type** (line ~188):
       - Remove `import narwhals.stable.v1 as nw` (if only used here)
       - Change `assert isinstance(fc, nw.DataFrame)` → `assert isinstance(fc, pl.DataFrame)`
       - Reason: SchemaError.failure_cases becomes native after boundary unwrapping in Plan 03

    2. **TestBuiltinChecksIbis.test_greater_than_fails_failure_cases_type** (line ~261):
       - Change from: `assert isinstance(fc, nw.DataFrame)` + `assert isinstance(nw.to_native(fc), ibis.Table)`
       - Change to: `assert isinstance(fc, ibis.Table)` (native directly — no nw wrapper)
       - Reason: SchemaError.failure_cases is unwrapped at boundary

    3. **TestBuiltinChecksIbis.test_greater_than_fails_failure_cases_values** (line ~277):
       - Find the assertion that accesses failure data from `fc`
       - Update to use `fc.execute()` or `fc.to_pandas()` directly (since fc is now native ibis.Table)
       - Previously may have used `nw.to_native(fc).execute()` — simplify to `fc.execute()`

    4. **TestLazyValidationIbis.test_ibis_lazy_failure_cases_is_dataframe** (line ~633):
       - Rename method to `test_ibis_lazy_failure_cases_is_ibis_table`
       - Change assertion from `isinstance(fc, pl.DataFrame)` → `isinstance(fc, ibis.Table)`
       - Update the error message in the assert if needed

    Read the full test file before editing to identify exact line numbers and context. Make minimal changes — only touch the assertions that need updating. Do not restructure the file.

    After editing, run the test file. Expect the four updated tests to FAIL (RED) because the implementation hasn't changed yet. Existing tests should continue to pass.
  </action>
  <verify>
    <automated>pytest tests/backends/narwhals/test_e2e.py -x -q --no-header 2>&1 | tail -20</automated>
  </verify>
  <done>
    The four updated test assertions exist in test_e2e.py with the Phase 6 type contracts. The updated tests FAIL (RED) with assertion errors. All other tests in the file continue to pass.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `pytest tests/backends/narwhals/test_components.py -q` — new tests are RED, existing tests GREEN
- `pytest tests/backends/narwhals/test_e2e.py -q` — updated assertions are RED, other tests GREEN
- `grep "_is_ibis_result" pandera/backends/narwhals/base.py` — still present (not yet deleted — that's Plan 02)
</verification>

<success_criteria>
All new tests and updated assertions exist with the correct Phase 6 contracts. They fail (RED) against the current implementation — not due to import/syntax errors. No regressions in currently-passing tests.
</success_criteria>

<output>
After completion, create `.planning/phases/06-eliminate-unnecessary-materialization-lazy-first-failure-cases-and-check-output/06-01-SUMMARY.md`
</output>
